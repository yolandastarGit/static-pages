# 角色权限说明文档

> **文档来源：** 基于《AI 智能 CRM 产品需求文档（PRD_v1.md）》提取整理，不新增、不修改权限规则。  
> **适用版本：** PRD v1.1.x（对齐 2026-07-15）。相对前版再对齐：字典管理三层治理（业务可配/受控预置/系统基础）、父子最多 2 级、AI 服务商→模型与 AI 业务场景进字典、来源渠道与录入方式分立（§23.5）；AI 能力「业务关联」按场景路由（§21.6）；高意向读跟进阶段「是否计入高意向」（与允许转客户分立）。历史要点保留：线索编辑与客户画像、转派 T1–T3、单一站点 `site_id`、协同人严格只读、WhatsApp 仅业务员绑定、通知 8 场景等。  
> **角色说明：** **外贸业务员即业务员角色**（PRD 中统一表述为「业务员」）。

---

## 文档用途

| 受众 | 用途 |
| :--- | :--- |
| 研发 | 实现菜单可见性、数据范围过滤、按钮级操作鉴权、消息查看只读边界 |
| 测试 | 编写角色权限矩阵用例、越权/边界测试、消息只读测试 |
| 产品/项目经理 | 向甲方说明各角色职责边界 |
| 实施 | 配置用户授权站点、角色分配、站点主邮箱绑定与菜单对齐 |

---

## 权限模型总原则

系统权限按「**站点权限 + 数据负责人 + 角色操作权限**」共同控制（PRD §4）。

| 权限维度 | 说明 |
| :--- | :--- |
| 站点权限 | 用户只能访问自己授权站点范围内的数据 |
| 负责人权限 | 业务员仅管理本人负责的数据 |
| 角色权限 | 不同角色拥有不同的数据查看、编辑、分配、管理能力 |
| 消息查看权限 | 运营专员、协同人、系统管理员可查看授权范围内业务员的邮箱与 WhatsApp 沟通记录，但**仅限只读**（见第五节） |

---

## 权限基础口径（全角色通用）

### 站点与数据权限

| 术语 | 含义 | 是否参与权限判断 |
| :--- | :--- | :--- |
| **站点**（`site_id`） | 线索/客户/合同等业务数据的归属站点；**数据权限判断、业务管理、获客/经营分析的统一站点依据**（与 PRD §2.4 一致；已废弃「来源站点 / 所属站点」双字段拆分） | **是，权限唯一依据** |
| **授权站点**（`user_site_permissions`） | 用户被授权可访问的站点集合 | 用于限定用户可见站点上限 |
| **邮箱关联站点**（`mail_site_id`） | 邮箱对应站点：业务员个人邮箱=业务员唯一授权站点；站点主邮箱=站点所绑定运营人员的业务邮箱（在站点配置指定）| 不直接用于权限判断；邮件线索的站点取此值 |

> **v1.0.2 起线索创建即有明确站点**，已删除「站点状态（待补充/已补充）」字段与「补充站点」功能；权限与统计均基于统一的 `site_id`，不再维护独立的 `source_site_id`。

**数据可见性判定公式（当前 MVP）：**

```
数据可见 = 数据站点 ∈ 用户授权站点
        + 角色数据粒度（本人 / 负责站点 / 授权站点只读 / 全部）
```

### 操作权限类型

PRD 定义的操作类型包括：新建、编辑、删除、分配、转客户、导出（CSV）、**导入（Excel/CSV：站点运营数据模块、线索批量导入、客户批量导入、合同批量导入）**、配置。各模块具体操作以模块权限矩阵为准。

### 渠道账号绑定规则（v1.0.2）

| 账号类型 | 业务员 | 运营专员 | 协同人 | 系统管理员 |
| :--- | :---: | :---: | :---: | :---: |
| 邮箱（个人邮箱，可多个） | ✅ | ✅ | ❌ | —（运维用途） |
| 站点主邮箱（站点配置绑定，1 站点 1 邮箱） | ❌ | ✅（由系统管理员在站点配置指定） | ❌ | 配置 |
| WhatsApp（仅 1 个/人） | ✅ | ❌ | ❌ | ❌ |

> **两类邮箱：** ① 业务员个人邮箱——有效询盘线索直接归业务员本人；② 站点主邮箱（运营专员绑定，系统管理员在【站点管理·新增/编辑】设为站点对外接收询盘的邮箱，站点↔主邮箱 1:1、不可重复绑定）——线索归该运营专员、由其手动转派给业务员、不进公海。

---

## 一、菜单权限

> **图例：** ☑️ = 菜单可见可进入；☑️只读 = 可见且仅只读；— = 菜单不可见。协同人业务菜单一律只读（另默认可导出，见 PRD §4.4）。与 PRD §4.3 一致。

| 菜单 | 系统管理员 | 运营专员 | 协同人 | 业务员 |
| :--- | :---: | :---: | :---: | :---: |
| 工作台 | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 沟通中心 · 邮件 | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 沟通中心 · WhatsApp | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 线索中心 · 线索列表 | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 线索中心 · 公海池 | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 客户中心 · 客户列表 | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 客户中心 · 合同中心 | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 分析中心 · 销售经营 | ☑️ | ☑️ | ☑️只读 | — |
| 分析中心 · 获客分析 | ☑️ | ☑️ | ☑️只读 | — |
| 分析中心 · 客户经营 | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 站点中心 · 站点管理 | ☑️ | — | — | — |
| 站点中心 · 站点运营数据 | ☑️ | ☑️只读 | ☑️只读 | — |
| AI 能力管理 | ☑️ | — | — | — |
| 通知管理 · 通知场景 | ☑️ | — | — | — |
| 系统管理 · 用户管理 | ☑️ | — | — | — |
| 系统管理 · 角色管理 | ☑️ | — | — | — |
| 系统管理 · 菜单管理 | ☑️ | — | — | — |
| 系统管理 · 字典管理 | ☑️ | — | — | — |
| 系统管理 · 系统参数 | ☑️ | — | — | — |
| 系统管理 · 集成配置 | ☑️ | — | — | — |
| 系统管理 · 系统日志 | ☑️ | — | — | — |
| 个人中心（Header 头像下拉，不占侧边菜单） | ☑️ | ☑️ | ☑️ | ☑️ |
| 通知收件（Header 铃铛站内信，不占侧边菜单） | ☑️ | ☑️ | ☑️ | ☑️ |

> **补充：** ① 通知管理仅为通知场景/推送渠道**配置**（仅系统管理员）；业务角色经铃铛/钉钉收通知，站内信无独立承接页（§22）。② 运营/协同可见「站点运营数据」只读，不可见「站点管理」（§20A）。③ 分析中心二级差异对齐 §17/§18/§19。④ 系统参数为单页 4 Tab：登录安全 / 密码设置 / 通用参数 / 业务规则（转派参考匹配键代码写死，不进参数页）。⑤ 线索来源四类见 PRD §9.1；消息【生成线索】字段同新增线索（§13.2.1，含 WhatsApp、关注点）。⑥ 线索【编辑】字段与详情业务资料对齐（含客户画像）；站点/状态/阶段/时间只读；改负责人同 §13.1.2「变更负责人」权限（§13.3）。

**不占侧边菜单的公共入口：** 登录页、线索详情（新开页签，非抽屉）、客户/合同详情、跟进日志页签、写邮件页、个人中心弹窗、系统级状态页（404/403/500 等）。

---

## 二、数据权限

| 角色 | 数据范围 | 站点范围 | 可查看对象 | 限制说明 |
| :--- | :--- | :--- | :--- | :--- |
| **业务员** | **仅本人数据** | 授权站点（**仅单站点**） | 本人负责的线索、客户、合同、跟进记录；本人绑定邮箱的邮件；本人绑定 WhatsApp 的会话 | 负责人=本人；数据站点须在授权站点内 |
| **运营专员** | **负责站点内全部业务员数据** | 授权站点（=负责站点，可多站点） | 负责站点内所有线索、客户、合同、跟进记录；公海池线索（负责站点范围）；负责站点内业务员邮箱/WhatsApp（**只读**） | 当前版本负责站点=授权站点；团队管理未启用 |
| **协同人** | **授权站点范围内全部数据（严格只读）** | 授权站点（可多站点） | 授权站点内线索、客户、合同、跟进记录；公海池线索（只读）；授权站点内业务员邮箱/WhatsApp（**只读**） | **仅查看/导出，不可写**；授权站点外不可见 |
| **系统管理员** | **全部数据** | 全部站点（不受授权站点限制） | 全公司线索、客户、合同、跟进记录、公海池、站点、用户、全部消息等 | 含已停用站点历史数据；查看业务员消息仅只读 |

**适用范围：** 线索、客户、合同、跟进记录（§4.2）。  
**公海池适用独立规则**（见第六节）；**消息（邮箱/WhatsApp）查看与操作边界见第五节。**

---

## 三、操作权限矩阵

> 图例：✅ 允许　❌ 不允许　⚠️ 条件允许（条件见「说明」列）

| 模块 | 功能/操作 | 系统管理员 | 协同人 | 运营专员 | 业务员 | 说明 |
| :--- | :--- | :---: | :---: | :---: | :---: | :--- |
| **工作台** | 查看工作台 | ✅ | ✅ | ✅ | ✅ | 数据范围按 §4.2 |
| | 站点筛选 | ✅ | ✅（只读） | ✅ | ❌ | 业务员单站点不展示筛选 |
| | 待分配/超期回收卡片跳转公海 | ✅ | ✅（只读） | ✅ | ❌ | 协同人跳转后仅查看 |
| | 异常站点卡片跳转站点管理 | ✅ | ⚠️ | ⚠️ | ❌ | 跳转目标页仅系统管理员可操作（见冲突说明） |
| **沟通中心·邮件** | 查看本人邮箱邮件 | ✅ | — | — | ✅ | 账号本人 |
| | 查看业务员邮箱（授权范围） | ✅（只读） | ✅（只读） | ✅（只读，负责站点） | ❌ | 运营/协同/管理员只读监督 |
| | 写邮件/回复/转发/删除 | ❌（对业务员邮箱） | ❌ | ❌（对业务员邮箱） | ✅（本人） | **仅账号本人可操作** |
| | 站点主邮箱收发处理 | 配置 | ❌ | ✅（本人绑定） | — | 站点主邮箱归运营专员 |
| | 转派给业务员 | ❌ | ❌ | ✅（站点主邮箱，T1–T3） | ❌ | 仅站点主邮箱；无线索可只投递 |
| | 生成线索（人工兜底） | ✅ | ❌ | ✅ | ✅ | 字段同新增线索（§13.2.1，含 WhatsApp、关注点；邮箱/电话/WhatsApp 至少一个）；协同人只读不可 |
| | 批量 AI 提炼 | ✅ | 只读查看 | ✅（本人邮箱） | ✅（本人邮箱） | — |
| **沟通中心·WhatsApp** | 查看本人 WhatsApp | ✅ | — | — | ✅ | 账号本人（仅业务员可绑） |
| | 查看业务员 WhatsApp（授权范围） | ✅（只读） | ✅（只读） | ✅（只读，负责站点） | ❌ | 只读监督 |
| | 回复/发送/删除 | ❌ | ❌ | ❌ | ✅（本人） | **仅账号本人可操作** |
| | 生成线索（人工兜底） | ✅ | ❌ | ❌ | ✅（本人） | 字段同新增线索；WA 电话只读；含 WhatsApp/关注点 |
| **线索中心·线索列表** | 查看线索列表 | ✅ | ✅（只读） | ⚠️ | ⚠️ | 按各自数据范围；公海「待分配」不进本列表（§9.1） |
| | 新增线索 | ✅ | ❌ | ⚠️ | ⚠️ | 协同人不可；字段含 WhatsApp、关注点；邮箱/电话/WhatsApp 至少一个（§13.2） |
| | 编辑 | ✅ | ❌ | ⚠️ | ⚠️（本人） | **含客户画像**（工商/参展/风险）及业务资料；站点/状态/阶段/时间只读；**改负责人仅运营/管理员**（同变更负责人，§13.3）；协同人只读 |
| | 打标签 | ✅ | ❌ | ⚠️ | ⚠️（本人） | 协同人只读 |
| | 录入跟进 | ✅ | ❌ | ⚠️ | ⚠️（本人） | 协同人只读；下次跟进默认可空 |
| | 转客户（单条） | ✅ | ❌ | ⚠️ | ⚠️（本人） | 协同人只读 |
| | 转客户（批量） | ✅ | ❌ | ✅ | ❌ | 业务员不可批量；协同人只读 |
| | 标记异常 | ✅ | ❌ | ⚠️ | ⚠️（本人） | 协同人只读 |
| | 变更负责人 | ✅ | ❌ | ⚠️（负责站点内） | ❌ | **业务员不可**；与编辑中改负责人同权（§13.1.2 / §13.3） |
| | 关联邮件/WhatsApp | ✅ | ❌（写操作） | ⚠️ | ⚠️（本人） | 协同人仅查看关联 |
| | 回收至公海 | ✅ | ❌ | ⚠️ | ❌ | 协同人只读（角色边界见冲突说明） |
| | 批量导出 | ✅ | ✅ | ✅ | ✅ | 导出权限范围内数据 |
| | 批量导入 | ✅ | ❌ | ⚠️ | ⚠️（本人） | 模板字段同 §13.2（含 WhatsApp、关注点）；邮箱/电话/WhatsApp 至少一个；协同人只读不可导入（§9.7、§13.1.2）|
| **线索中心·公海池** | 查看公海线索 | ✅ | ✅（只读） | ⚠️ | ⚠️（只读） | 仅「待分配」等公海数据 |
| | 分配（单/批量） | ✅ | ❌ | ✅ | ❌ | 协同人只读不可分配 |
| | 标记异常 | ✅ | ❌ | ✅ | ❌ | 协同人只读不可 |
| | 查看详情 | ✅ | ✅（只读） | ⚠️ | ⚠️ | — |
| **线索详情** | 查看详情（工作区**新开页签**，非抽屉） | ✅ | ✅（只读） | ⚠️ | ⚠️ | 同线索列表数据范围；入口见 §11；基础信息 + 页签（跟进日志/关联合同/客户画像） |
| | 查看客户画像页签 | ✅ | ✅（只读） | ⚠️ | ⚠️ | **只读引用**询盘联系人侧画像（§7.9 / §11.5.3）；编辑不在详情 |
| | 录入跟进 / 查看关联消息 / 转客户 | ✅ | ❌（关联消息可只读进入） | ⚠️ | ⚠️（本人；转客户除外条件见 §13.1.2） | **详情顶部仅此 3 按钮**；编辑/打标签/标记异常/变更负责人仅在列表或公海 |
| **跟进日志** | 查看全局跟进日志 | ✅ | ✅（只读） | ⚠️ | ⚠️ | 按 §4.2 注入数据范围；列表顶部【跟进日志】新开页签 |
| | 录入跟进 | ✅ | ❌ | ⚠️ | ⚠️ | **协同人不可录入跟进** |
| | 导出 | ✅ | ✅ | ✅ | ✅ | 上限 10000 条 |
| **客户中心·客户列表** | 查看客户列表 | ✅ | ✅（只读） | ⚠️ | ⚠️ | 按 §4.2 |
| | 新建客户 | ✅ | ❌ | ⚠️ | ⚠️ | 协同人不可；业务员创建后本人为负责人 |
| | 编辑/打标签/变更负责人/添加联系人 | ✅ | ❌ | ⚠️ | ⚠️ | 协同人只读；添加联系人见 §14.7.2a |
| | 查看详情 | ✅ | ✅（只读） | ⚠️ | ⚠️ | **新开页签**，详情只读 |
| | 导出 | ✅ | ✅ | ✅ | ✅ | CSV，权限范围内数据，上限 10000（§14.7.5）|
| | 批量导入 | ✅ | ❌ | ⚠️ | ⚠️（本人）| 模板同客户字段；命中（站点+企业名称）跳过、未命中新建；协同人只读不可导入（§14.7.6）|
| **客户详情** | 查看各标签页 | ✅ | ✅（只读） | ⚠️ | ⚠️ | 客户信息/线索记录/联系人/合同/AI画像；**页内无写操作** |
| | 增删改（资料/标签/联系人/合同/画像） | ❌ | ❌ | ❌ | ❌ | 写操作仅在客户列表 / 合同中心 |
| **合同中心** | 查看合同列表/详情 | ✅ | ✅（只读） | ⚠️ | ⚠️ | 按合同负责人及继承的客户站点 |
| | 新增合同 | ✅ | ❌ | ⚠️ | ⚠️ | 协同人不可新增 |
| | 编辑合同 | ✅ | ❌ | ⚠️ | ⚠️ | 协同人只读 |
| | 终止合同 | ✅ | ❌ | ⚠️ | ⚠️ | 协同人不可 |
| | 作废合同 | ✅ | ❌ | ⚠️ | ⚠️ | 协同人不可 |
| | 导出 | ✅ | ✅ | ✅ | ✅ | CSV，权限范围内数据，上限 10000（§16.6.5）；导入本期不启用 |
| **分析中心·销售经营** | 查看页面 | ✅ | ✅（只读） | ⚠️ | ❌ | **不面向业务员** |
| | 站点/时间筛选 | ✅ | ✅ | ⚠️ | — | 按站点统计 |
| **分析中心·获客分析** | 查看页面 | ✅ | ✅（只读） | ⚠️ | ❌ | **不面向业务员** |
| **分析中心·客户经营** | 查看页面 | ✅ | ✅（只读） | ⚠️ | ⚠️ | 业务员为简化版（仅本人负责客户） |
| **站点中心·站点管理** | 站点列表/详情 | ✅ | ❌ | ❌ | ❌ | **仅系统管理员** |
| | 新增/编辑站点 | ✅ | ❌ | ❌ | ❌ | 一次保存：基本信息+运营人员+主邮箱+AI 开关；仅站点 ID 不可改；状态用启用/停用 |
| | 启用/停用站点 | ✅ | ❌ | ❌ | ❌ | — |
| | 回收规则/AI 规则/运营人员与主邮箱 | ✅ | ❌ | ❌ | ❌ | **并入新增/编辑**；不单列【配置】；回收规则二期预留 |
| **站点中心·站点运营数据** | 查看 | ✅ | ✅（只读，授权站点） | ✅（只读，授权站点） | ❌ | §20A |
| | 新增/编辑/删除/导入/导出 | ✅ | ❌ | ❌ | ❌ | **仅系统管理员维护** |
| **AI 能力管理** | 查看/新增/编辑/启停/删除 AI 能力（含业务关联配置） | ✅ | ❌ | ❌ | ❌ | **仅系统管理员**；业务关联规则见第九章 |
| **通知管理** | 查看通知场景配置列表（推送渠道/对象/模板；非收件箱） | ✅ | ❌ | ❌ | ❌ | §22 面向系统管理员（菜单可见性见冲突说明）；收件经 Header 铃铛 |
| | 编辑通知配置 | ✅ | ❌ | ❌ | ❌ | 渠道/对象/内容/状态 |
| **系统管理** | 用户/角色/菜单/字典/系统参数/集成配置/日志 | ✅ | ❌ | ❌ | ❌ | **仅系统管理员** |
| **个人中心** | 我的资料/账号绑定/修改密码 | ✅ | ✅ | ✅ | ✅ | 全部角色 |
| | 邮箱绑定（多邮箱，不选站点；每邮箱填密码/授权码） | —（运维） | ❌ | ✅ | ✅ | 业务员个人邮箱 / 运营专员邮箱；协同人不绑；协议见集成配置 |
| | WhatsApp 绑定（仅一个，不绑站点） | ❌ | ❌ | ❌ | ✅ | **仅业务员** |

---

## 四、按角色权限边界说明

### 系统管理员权限说明

#### 角色定位

全公司系统与数据管控角色，负责站点、用户、角色、字典、AI、集成配置、系统参数；拥有全部站点 + 全部数据 + 全部消息（消息只读）权限。

#### 可查看数据

- 全公司所有站点的线索、客户、合同、跟进记录、公海池数据
- 全部邮箱与 WhatsApp 沟通记录（**只读**）
- 全部站点列表及配置（含已停用站点历史数据）、站点运营数据
- 全部分析中心数据（销售经营、获客分析、客户经营）
- 系统管理全部配置数据

#### 可执行操作

- **系统管理：** 用户/角色/菜单/字典/系统参数（**Tab**：登录安全/密码设置/通用参数/业务规则）/集成配置/系统日志的全部 CRUD 与导出
- **字典管理：** 字典类型列表 → 字典项管理（不可新增/停用字典类型）；按 §23.5 分层维护字典项（含 AI 服务商/模型、跟进阶段扩展属性等）；变更写操作日志
- **站点管理：** 新增/编辑/启用停用站点；运营人员与主邮箱、AI 规则在新增/编辑中一次维护（不单列配置页）
- **站点运营数据：** 新增/编辑/删除/批量导入/导出
- **AI 能力管理：** 新增/编辑/启停/删除 AI 能力；服务商/模型从字典选择；配置业务关联（四场景互斥绑定，见第九章）
- **通知管理：** 编辑各业务场景推送渠道/对象/模板内容（配置页，非通知收件箱）
- **业务操作：** 权限矩阵中标注「全部」的线索/客户/合同操作（含公海分配、批量转客户、回收至公海、变更负责人等）

#### 不可执行/不适用事项

- ❌ **不可代业务员发送/回复/转发/删除**邮件或 WhatsApp 消息（查看业务员消息仅只读）
- 不能自助重置他人密码（需通过用户管理操作）
- 系统管理员账号受内置保护：不可被停用/删除/降权；系统至少保留一个启用的系统管理员

---

### 协同人权限说明

#### 角色定位

**只读协作角色**，在授权站点范围内查看业务数据与消息，用于业务监督与协作，**不是**业务执行或管理写入角色。

#### 可查看数据

- 授权站点内所有运营专员/业务员的线索、客户、合同、跟进记录
- 授权站点内公海池线索（只读）
- 授权站点内业务员的邮箱、WhatsApp 沟通记录（**只读**）
- 分析中心：销售经营、获客分析、客户经营（授权站点范围）
- 本人无 WhatsApp/站点主邮箱（协同人不绑定渠道账号）

#### 可执行操作

- **只读查看：** 线索、客户、合同、跟进记录、公海池、消息（授权站点范围内）
- **导出：** 权限范围内数据导出（默认允许）
- **分析：** 销售经营、获客分析、客户经营（查看）

#### 不可执行/不适用事项

- ❌ **不可新增/编辑/删除**线索、客户、合同
- ❌ **不可分配**公海线索、**不可变更负责人**、**不可标记异常**、**不可转客户**（含批量）
- ❌ **不可录入跟进**、不可打标签、不可修改状态
- ❌ **不可代发/回复/转发/删除**邮件或 WhatsApp 消息
- ❌ **不可访问**站点中心配置、AI 能力管理、系统管理、通知管理配置
- ❌ **不可查看/操作**授权站点范围外数据
- ❌ **不可配置**用户权限、角色、菜单、字典、系统参数、全局 AI 开关

---

### 运营专员权限说明

#### 角色定位

**站点业务运营管理角色**，管理其**负责站点**（当前版本=授权站点）范围内的业务数据，负责线索分配、公海运营、站点主邮箱询盘转派、站点经营数据查看。

#### 可查看数据

- 负责站点内所有业务员的数据（含本人）
- 负责站点内公海池全部线索
- 负责站点内业务员的邮箱、WhatsApp 沟通记录（**只读**）
- 分析中心：销售经营、获客分析、客户经营（负责站点范围）
- 本人绑定邮箱（含站点主邮箱）沟通数据

#### 可执行操作

- **线索管理：** 权限范围内全部线索操作（新增、编辑含客户画像与关注点、跟进、转客户含批量、标记异常、变更负责人及编辑中改负责人、回收至公海、导出、导入）
- **站点主邮箱转派：** 将站点主邮箱邮件/线索转派给业务员（转派 T1–T3，见第七节）
- **公海池：** 分配（单/批量）、标记异常、查看详情
- **客户管理：** 新建客户、编辑、打标签、变更负责人
- **合同管理：** 新增、编辑、终止、作废（权限范围内）
- **分析：** 销售经营、获客分析、客户经营

#### 不可执行/不适用事项

- ❌ **不可代业务员发送/回复/转发/删除**业务员邮箱或 WhatsApp 消息（对业务员消息仅只读）
- ❌ **转派已关联线索且负责人为业务员 A 时**仅可转给 A**（T2）；无线索转派不改动旧线索（T3）
- ❌ **不可查看**非授权（非负责）站点数据
- ❌ **不可绑定** WhatsApp（仅业务员可绑）
- ❌ **不可访问**站点中心配置、AI 能力管理、系统管理、通知管理配置
- 团队管理功能当前未启用（团队字段为预留）

---

### 业务员权限说明

#### 角色定位

**一线销售执行人员**，仅关联 1 个站点，负责本人线索/客户/合同的跟进与转化，是日常业务操作的主要执行者。

#### 可查看数据

- **仅本人为负责人**的线索、客户、合同、跟进记录
- 授权站点内公海池线索（**只读**）
- 本人绑定邮箱（可多个）的邮件、本人绑定 WhatsApp（1 个）的会话
- 分析中心·客户经营（**简化版，仅本人负责客户**）

#### 可执行操作

- **沟通中心：** 查看/回复/转发/删除本人邮箱邮件、查看/回复本人 WhatsApp 会话、AI 分析查看、人工兜底生成线索（字段同 §13.2，含 WhatsApp、关注点）
- **线索（本人负责）：** 新增线索、编辑（含客户画像/关注点等业务资料；**负责人字段只读**）、打标签、录入跟进、转客户（单条）、标记异常、关联邮件/WhatsApp、批量导出
- **客户（本人负责）：** 新建客户、编辑、打标签、维护联系人
- **合同（本人负责）：** 新增、编辑、终止、作废
- **跟进日志：** 查看本人范围、录入跟进、导出
- **个人中心：** 资料/账号绑定（邮箱多个、WhatsApp 1 个）/改密

#### 不可执行/不适用事项

- ❌ **公海池不可分配**（只读，仅可查看详情）
- ❌ **不可批量转客户**、**不可标记公海池异常**
- ❌ **不可变更负责人**，编辑弹窗内亦**不可改派负责人**（与 §13.1.2 一致；改派仅运营专员/系统管理员）
- ❌ **不可查看/管理**其他业务员数据与消息
- ❌ **不可访问**销售经营、获客分析、站点中心、AI 能力管理、系统管理
- 授权站点限制：**仅单站点**（个人邮箱线索归本人；WhatsApp 不绑固定站点）

---

## 五、消息查看与操作边界

> 运营专员、协同人、系统管理员可基于权限范围查看业务员的邮箱与 WhatsApp 沟通记录，但该能力仅用于业务监督、协作和管理，**不代表可代业务员处理消息**（PRD §7.3）。

| 操作 | 账号本人 | 运营专员 | 协同人 | 系统管理员 |
| :--- | :---: | :---: | :---: | :---: |
| 查看本人邮箱 / WhatsApp | ✅ | — | — | — |
| 查看授权范围内业务员邮箱 | ❌ | ✅（负责站点） | ✅（授权站点） | ✅（全部） |
| 查看授权范围内业务员 WhatsApp | ❌ | ✅（负责站点） | ✅（授权站点） | ✅（全部） |
| 回复业务员邮件 | ✅ | ❌ | ❌ | ❌ |
| 发送业务员 WhatsApp 消息 | ✅ | ❌ | ❌ | ❌ |
| 转发业务员邮件 | ✅ | ❌ | ❌ | ❌ |
| 删除业务员邮件 | ✅ | ❌ | ❌ | ❌ |
| 修改业务员消息已读状态 | ✅ | ❌ | ❌ | ❌ |
| 修改业务员消息回复状态 | ✅ / 系统自动 | ❌ | ❌ | ❌ |

> 非消息账号本人查看业务员邮箱或 WhatsApp 时，页面进入**只读模式**，隐藏或置灰回复、发送、转发、删除等操作按钮。

---

## 六、公海池权限说明

> 公海池为**模块特殊数据权限**，区别于 §4.2 常规数据粒度。按授权站点可见，分配权限独立判定。

| 角色 | 可见范围 | 是否可分配 | 是否可标记异常 | 是否可查看详情 |
| :--- | :--- | :---: | :---: | :---: |
| **业务员** | 授权站点内全部公海线索 | ❌ | ❌ | ✅（只读） |
| **运营专员** | 负责站点内全部公海线索 | ✅ | ✅ | ✅ |
| **协同人** | 授权站点范围内全部公海线索 | ❌ | ❌ | ✅（只读） |
| **系统管理员** | 全公司公海线索 | ✅ | ✅ | ✅ |

**补充规则：**

- 绑定账号渠道（邮件/WhatsApp）生成的线索创建即归属负责人、站点明确、状态「待跟进」，**不进入公海池**；后续业务人员可主动回收至公海。
- 公海池待分配线索不在线索列表展示；分配后移入线索列表。
- 业务员、协同人不展示「批量分配」「分配」「标记异常」按钮，仅「查看详情」（协同人默认可导出）。
- 分配/标记异常仅运营专员、系统管理员可执行。
- 分配目标负责人须为该线索站点下状态「可用」的销售人员。
- 手动回收（线索列表「回收至公海」）适用状态见 §10.7；回收后 `owner = null`，状态→待分配。

---

## 七、站点主邮箱线索归属与转派（T1–T3）

> 站点主邮箱收到客户询盘、AI 识别为有效询盘后默认生成线索，初始负责人为该运营专员，由其手动转派给业务员（PRD §7.5.2、§27.1）。**未识别成线索的邮件也可转派**（仅投递）。业务员个人邮箱线索直接归业务员本人、无需转派。

### 线索归属

| 场景 | 系统规则 |
| :--- | :--- |
| 业务员个人邮箱收到有效询盘 | 生成线索，负责人 = 业务员本人，直接进入本人线索列表 |
| 站点主邮箱收到有效询盘 | 生成线索，初始负责人 = 站点主邮箱绑定的运营专员 |
| 运营转派（已关联线索） | 按 T1/T2 投递邮件并处理负责人 |
| 运营转派（未关联线索） | 按 T3 **仅投递邮件**；不建线索、不自动挂旧线索；业务员可后补【生成线索】 |
| 运营是否仍可查看 | 可以，运营仍可查看负责站点内该线索及其相关沟通记录 |

### 转派规则 T1–T3（运营专员转派时系统自动判断）

| 规则 | 判断条件 | 系统动作 |
| :---: | :--- | :--- |
| T1 | 当前邮件**已关联线索**，负责人是**当前运营** | 可选业务员 → 投递 + 负责人改为目标业务员；不新建线索 |
| T2 | 当前邮件**已关联线索**，负责人已是**业务员 A** | **目标锁定为 A，不允许换人** → 投递；负责人保持 A |
| T3 | 当前邮件**未关联线索** | **允许转派** → **仅投递**；不建线索、不自动关联旧线索。若同站点同发件邮箱已有业务员 A 的线索：目标**默认 A，可改选**（换人不改动旧线索）；提示不合并 |

**口径拆分：**

- **建线索：** 一封邮件一条线索（BR-021），入库不做按发件邮箱自动合并。
- **「站点 + 客户原始发件邮箱」：** 仅用于 T2 锁定同人 / T3 默认选人与提示，不是建线索去重键。
- 一封邮件最多关联一条线索；一条线索可关联多封邮件（转派投递件挂回、或人工关联等明确动作）。
- **转派仅存在于邮箱渠道，WhatsApp 无转派。**

---

## 八、负责人变更规则

| 场景 | 是否允许 | 说明 |
| :--- | :---: | :--- |
| 运营专员将站点主邮箱线索转派给业务员 | ✅ | 负责人由运营变更为目标业务员（T1） |
| 运营专员调整负责站点内线索负责人 | ✅ | 仅限负责站点范围内 |
| 运营专员转派**已关联线索**且负责人为业务员 A | 仅可转给 A | 目标锁定 A，不允许换成其他人（T2） |
| 运营专员无线索转派 | ✅（仅投递） | 不改动任何旧线索负责人（T3） |
| 业务员主动修改线索负责人 | ❌ | 业务员仅维护本人负责数据 |
| 协同人修改负责人 | ❌ | 协同人严格只读 |
| 系统管理员修改负责人 | ✅ | 全局权限 |

---

## 九、字典 / AI 与系统参数权限说明

### 字典管理配置

| 能力 | 系统管理员 | 协同人 | 运营专员 | 业务员 |
| :--- | :---: | :---: | :---: | :---: |
| 字典类型列表查看 / 字典项管理 | ✅ | ❌ | ❌ | ❌ |
| 新增字典类型 / 停用字典类型 | ❌ | ❌ | ❌ | ❌ | 类型系统预置固定（§23.5） |
| 业务可配字典项增删改启停、父子维护 | ✅ | ❌ | ❌ | ❌ |
| 受控/基础字典项改展示名与排序 | ✅ | ❌ | ❌ | ❌ | 不可增删启停项（线索/合同状态、登录方式、操作类型、录入方式、AI 业务场景） |
| 跟进阶段扩展属性（是否允许转客户、是否计入高意向） | ✅ | ❌ | ❌ | ❌ |

> **业务角色：** 仅消费字典下拉选项（筛选/表单），**不可**进入字典管理。AI 分析标签不进字典；人工线索/客户标签选项由字典维护。

### AI 能力配置

| 能力 | 系统管理员 | 协同人 | 运营专员 | 业务员 |
| :--- | :---: | :---: | :---: | :---: |
| AI 能力管理（新增/编辑/启停/删除，含业务关联） | ✅ | ❌ | ❌ | ❌ |
| 系统参数·业务规则「AI 功能启用」总开关（关闭后邮件/WhatsApp 分析、自动提取企业信息、批量 AI 提炼等全部停用）| ✅ | ❌ | ❌ | ❌ |
| 站点新增/编辑·AI 开关（原 AI 规则 Tab） | ✅ | ❌ | ❌ | ❌ | 已并入站点新增/编辑，仅开关 |

> **业务关联（§21.6，仅系统管理员可配）：** 四场景编码固定（字典 AI 业务场景，仅可改展示名/排序）。一条 AI 能力可多选绑定多个场景；每个场景同时仅一条**启用**能力占用；冲突时拦截保存/启用（不抢占）；停用立即释放占用。服务商/模型从字典选择（可先在字典增补）。调用条件：「AI 功能启用」开启 **且** 该场景存在绑定且启用的 AI 能力，且所引用服务商/模型字典项未停用，否则不调 AI（视同不可用）。

### AI 有效询盘识别（10 类判定）

AI 对邮件/WhatsApp 会话判定，命中以下任一类型即为有效询盘 → 自动生成线索（PRD §21.8）：产品咨询、价格咨询、样品需求、采购需求、交期咨询、定制需求、合作代理、项目询盘、复购/新需求、明确联系方式交换。自动回复/广告/无业务内容/垃圾信息为无效（不生成）。有效询盘识别分别路由至「邮件意向分析」「WhatsApp 意向分析」所绑定的启用能力（§21.6）。

### AI 分析结果查看

| 场景 | 权限规则 |
| :--- | :--- |
| 邮件 AI 智能分析面板 | 跟随邮件数据权限（本人可操作邮箱；运营/协同/管理员按只读查看范围） |
| WhatsApp AI 意向分析 | 跟随 WhatsApp 数据权限 |
| 线索详情·客户画像页签 | **只读**；跟随线索数据权限（§4.2）；内容引用询盘联系人侧画像（§7.9 / §11.5.3） |
| 线索列表【编辑】·客户画像 | 跟随线索【编辑】权限；写入询盘联系人侧；人工编辑后 AI 不覆盖（BR-017）；业务员不可借编辑改负责人 |
| 客户详情·AI 客户画像 | 跟随客户数据权限（§4.2）；详情只读展示 |

> **原则：** AI 分析结果查看权限**不单独扩展**，严格跟随对应业务数据的数据权限。客户画像**编辑不扩展独立权限位**，复用线索/客户编辑权限。业务角色**不可**配置字典、业务关联或 AI 服务商/模型；仅消费分析结果与下拉选项。

### AI 不可用时的业务兜底

| 角色 | 兜底行为 |
| :--- | :--- |
| 全部业务角色 | AI 失败**不阻断业务**；消息保留，可使用「生成线索（人工兜底）」手动创建线索（协同人只读除外） |
| 全部角色 | AI 面板显示灰占位/错误提示（引用 §29.7），不影响邮件查看与跟进 |
| 系统管理员 | 负责检查字典（服务商/模型/场景）、AI 能力配置（含业务关联）、系统参数「AI 功能启用」、API 端点异常处理 |

> **不可用判定（对齐 §21.5 / §21.6 / §23.5）：** 总开关关闭、当前场景无人绑定启用能力、绑定能力停用、所引服务商/模型字典项停用、Token 无效/调用失败等，均按 AI 不可用降级处理。

---

## 十、权限测试关注点

### 系统管理员

- [ ] 可查看全部站点线索/客户/合同数据，不受授权站点限制
- [ ] 可进入站点中心（含站点运营数据）、AI 能力管理、系统管理全部子菜单
- [ ] 可配置字典（类型列表→字典项；不可新增类型；跟进阶段扩展属性；AI 服务商→模型父子）
- [ ] 可配置 AI 能力（服务商/模型选自字典；业务关联四场景互斥、冲突拦截、停用释放）、系统参数「AI 功能启用」、通知场景、站点主邮箱（运营人员与主邮箱）
- [ ] 某场景无启用绑定能力或服务商/模型字典停用时，业务侧按 AI 不可用降级，管理员可进字典/AI 能力管理补配
- [ ] 可执行公海分配、批量转客户、变更负责人等「全部」权限操作
- [ ] **不可**代业务员回复/发送/删除邮件或 WhatsApp（查看业务员消息只读）
- [ ] 系统管理员账号不可被停用/删除

### 协同人

- [ ] 仅可查看授权站点内数据，范围外不可见
- [ ] **严格只读**：不可新增/编辑/删除/分配/转客户/变更负责人/标记异常/录入跟进
- [ ] **不可**代发/回复/转发/删除业务员邮件或 WhatsApp（仅只读查看）
- [ ] 可导出权限范围内数据
- [ ] **不可**进入系统管理、站点中心配置、AI 能力管理、通知管理配置
- [ ] 可查看销售经营/获客分析/客户经营（授权站点数据）
- [ ] 权限仅按数据站点（`site_id`）过滤，无独立「来源站点」字段

### 运营专员

- [ ] 仅可查看负责站点（=授权站点）内数据
- [ ] 可分配负责站点公海线索、新建线索/客户、批量转客户、回收至公海、变更负责人（负责站点内）
- [ ] 编辑线索时可改负责人（须变更说明），并可编辑客户画像
- [ ] 可将站点主邮箱邮件转派给业务员，验证 T1–T3（尤其 T2 锁定同人、T3 无线索仅投递且默认可改）
- [ ] **只读**查看负责站点内业务员邮箱/WhatsApp，操作按钮置灰
- [ ] **不可**查看非授权站点数据
- [ ] **不可**绑定 WhatsApp
- [ ] **不可**进入系统管理、站点中心配置、AI 能力管理

### 业务员

- [ ] 仅可查看本人为负责人的线索/客户/合同
- [ ] 可绑定多个个人邮箱、1 个 WhatsApp；仅可操作本人绑定账号消息
- [ ] 个人邮箱有效询盘线索直接归本人（无需转派）
- [ ] 可新增/编辑本人线索（含 WhatsApp、关注点、客户画像）；**编辑中负责人只读**
- [ ] 公海池**只读**，不可分配、不可标记异常
- [ ] **不可**批量转客户、**不可变更负责人**（列表更多与编辑弹窗均不可改派）
- [ ] **不可**进入销售经营、获客分析、站点中心、AI 能力管理、系统管理
- [ ] 可查看客户经营简化版（仅本人客户）
- [ ] 授权站点仅允许单站点配置
- [ ] 数据站点（`site_id`）参与权限判断（与 PRD §2.4 一致，无「来源站点」拆分）

### 线索编辑与客户画像（跨角色，对齐 §13.3 / §11.5.3）

- [ ] 详情页客户画像只读；编辑入口仅在线索列表【编辑】
- [ ] 有编辑权限角色可改工商/参展/风险；保存后详情页签同步；人工优先 AI 不覆盖（BR-017）
- [ ] 业务员编辑不可改站点/状态/阶段/时间/负责人；运营/管理员编辑可改负责人且须变更说明
- [ ] 新增/导入/消息生成线索：邮箱/电话/WhatsApp 至少一个；含关注点字段
- [ ] 公海「待分配」不出现在线索列表

---

## 十一、PRD 权限冲突/歧义说明（仅列出，未修改 PRD）

| # | 冲突/歧义 | 涉及章节 | 说明 |
| :--- | :--- | :--- | :--- |
| 1 | 通知管理菜单可见性（**已定，冲突消除**）| §4.3 / §22.1 | **已明确：通知管理（通知场景配置）菜单仅系统管理员可见**；业务角色仅作为通知接收方（Header 铃铛站内信 / 钉钉），无独立收件页面、不进入菜单、不可配置。PRD §4.3 与本文档菜单权限已同步。 |
| 2 | **异常站点卡片跳转 vs 站点中心权限** | §6.4 vs §20.1 | 工作台「异常站点」卡片对运营专员/协同人展示并跳转站点管理，但 §20.1 站点中心仅面向系统管理员。需确认跳转行为（403/只读/隐藏卡片）。 |
| 3 | 站点运营数据菜单可见性（**已定，冲突消除**） | §4.3 vs §20A | **已明确：** 运营/协同可见「站点运营数据」只读入口，不可见「站点管理」；写操作仅系统管理员。PRD §4.3 / §20A 与本文档已同步。 |
| 3b | 分析中心二级菜单按角色（**已定**） | §4.3 vs §17/§18/§19 | **已明确：** 业务员仅客户经营；运营/协同可见销售经营+获客分析+客户经营。PRD §4.3 已改为分角色表。 |
| 4 | **线索「回收至公海」操作角色** | §9.6 vs §10.7 vs §13.1.2 | §9.6 列出「回收至公海」但未标注角色；§13.1.2 权限矩阵未单列。本文档按管理类操作口径：运营专员/系统管理员可执行，业务员与协同人不可。 |
| 5 | **系统管理员业务操作参与** | §13.1.2 等 | 权限矩阵中系统管理员对业务操作均为「全部」，PRD 未明确禁止系统管理员参与一线业务。本文档按矩阵归纳：系统管理员可执行业务操作，但消息仅只读。 |

---

## 附录：权限规则唯一来源索引

| 规则类型 | PRD 章节 |
| :--- | :--- |
| 权限模型总原则、角色定义与数据权限粒度 | §4、§4.1、§4.2 |
| 菜单权限 | §4.3 |
| 操作权限类型（含导入） | §4.4 |
| 站点权限、渠道账号绑定 | §4.5 |
| 消息查看与操作边界 | §7.3 |
| 邮件线索归属（两类邮箱） | §7.5、§7.10 |
| 站点主邮箱转派规则 T1–T3 | §7.5.2 |
| 发件人规则（回复锁定 / 空白可切换） | §7.6.7 |
| 消息人工生成线索（同新增线索字段） | §13.2.1 |
| 线索来源四类 | §9.1 |
| 线索编辑字段与客户画像人工编辑 | §13.3、§11.5.3、§7.9、BR-017 |
| 负责人变更规则 | §13.8 |
| 线索数据权限 | §9.2 |
| 线索操作权限矩阵 | §13.1.2 |
| 线索生成与归属 | §27.1 |
| 公海池特殊权限 | §10.2 |
| 客户数据权限 | §14.2 |
| 合同数据权限 | §16.2 |
| 站点新增/编辑·运营人员与主邮箱（站点主邮箱） | §20.3、§20.5.3 |
| 站点运营数据模块 | §20A |
| 账号绑定（邮箱 / WhatsApp 仅业务员） | §24.3 |
| 字典管理（分层、父子、预置、扩展属性） | §23.5 |
| AI 能力、业务关联路由与有效询盘识别 | §21.1、§21.3、§21.6、§21.8、§23.7（AI 功能启用）|
| 高意向口径 / 允许转客户 | §23.5、BR-038b、BR-038c |
| 来源渠道与录入方式 | §23.5、§9.7、BR-057 |
| 系统参数 4 Tab | §23.6、§23.7 |
| 通知场景（8 项） | §22.4 |
| 站点与渠道绑定异常 | §29.11 |
| 业务规则 BR-008/020/028/052/053 | §28 |
| 异常与权限失败 | §29.4 |

---

*文档结束*
