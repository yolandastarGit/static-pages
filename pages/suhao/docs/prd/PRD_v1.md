# AI 智能 CRM 产品需求文档（重构版 v1）

## 目录

> 点击章节标题可跳转至对应内容。

- [1. 文档信息](#1-文档信息)
  - [1.0 本次变更说明（2026-07-17）](#10-本次变更说明2026-07-17)
  - [1.0.1 变更说明（2026-07-15）](#101-变更说明2026-07-15)
  - [1.1 MVP 范围与延后清单](#11-mvp-范围与延后清单)
- [2. 产品整体说明](#2-产品整体说明)
  - [2.1 产品定位](#21-产品定位)
  - [2.2 核心目标](#22-核心目标)
  - [2.3 业务边界](#23-业务边界)
  - [2.4 对象关系](#24-对象关系)
- [3. 菜单与模块结构](#3-菜单与模块结构)
- [4. 角色与权限体系](#4-角色与权限体系)
  - [4.1 角色定义](#41-角色定义)
  - [4.2 数据权限粒度](#42-数据权限粒度)
  - [4.3 菜单权限](#43-菜单权限)
  - [4.4 操作权限类型](#44-操作权限类型)
  - [4.5 站点权限](#45-站点权限)
  - [4.6 动态权限运行机制](#46-动态权限运行机制)
  - [4.7 预置权限点总表](#47-预置权限点总表)
    - [4.7.1 菜单权限点](#471-菜单权限点)
    - [4.7.2 按钮权限点（业务）](#472-按钮权限点业务)
  - [4.8 预置角色专用业务硬约束](#48-预置角色专用业务硬约束)
- [5. 登录与认证](#5-登录与认证)
  - [5.1 模块概述](#51-模块概述)
  - [5.2 页面结构](#52-页面结构)
  - [5.3 登录方式与字段](#53-登录方式与字段)
  - [5.4 业务规则](#54-业务规则)
  - [5.5 登录失败提示](#55-登录失败提示)
  - [5.6 MVP 范围](#56-mvp-范围)
- [6. 工作台](#6-工作台)
  - [6.1 模块概述](#61-模块概述)
  - [6.2 页面结构](#62-页面结构)
  - [6.3 数据权限](#63-数据权限)
  - [6.4 卡片定义](#64-卡片定义)
  - [6.5 快捷入口](#65-快捷入口)
  - [6.6 异常处理](#66-异常处理)
- [7. 沟通中心 · 邮件](#7-沟通中心-邮件)
  - [7.1 模块概述](#71-模块概述)
  - [7.2 页面结构](#72-页面结构)
  - [7.3 数据权限（消息查看与操作边界）](#73-数据权限消息查看与操作边界)
  - [7.4 邮件列表字段](#74-邮件列表字段)
  - [7.5 邮件正文（中栏）](#75-邮件正文中栏)
  - [7.6 写邮件页签](#76-写邮件页签)
  - [7.7 附件规则](#77-附件规则)
  - [7.8 批量 AI 提炼](#78-批量-ai-提炼)
  - [7.9 AI 智能分析面板（右栏）](#79-ai-智能分析面板右栏)
  - [7.10 业务规则](#710-业务规则)
  - [7.11 异常处理](#711-异常处理)
- [8. 沟通中心 · WhatsApp](#8-沟通中心-whatsapp)
  - [8.1 模块概述](#81-模块概述)
  - [8.2 页面结构](#82-页面结构)
  - [8.3 联系人列表字段](#83-联系人列表字段)
  - [8.4 会话正文（中栏）](#84-会话正文中栏)
  - [8.5 详情面板（右栏）](#85-详情面板右栏)
  - [8.6 异常处理](#86-异常处理)
- [9. 线索中心 · 线索列表](#9-线索中心-线索列表)
  - [9.1 模块概述](#91-模块概述)
  - [9.2 数据权限](#92-数据权限)
  - [9.3 页面结构](#93-页面结构)
  - [9.4 筛选条件](#94-筛选条件)
  - [9.5 表格字段](#95-表格字段)
  - [9.6 操作按钮](#96-操作按钮)
  - [9.7 批量导出 / 批量导入](#97-批量导出-批量导入)
  - [9.8 异常处理](#98-异常处理)
- [10. 线索中心 · 公海池](#10-线索中心-公海池)
  - [10.1 模块概述](#101-模块概述)
  - [10.2 数据权限](#102-数据权限)
  - [10.3 页面结构](#103-页面结构)
  - [10.4 筛选条件](#104-筛选条件)
  - [10.5 表格字段](#105-表格字段)
  - [10.6 操作按钮](#106-操作按钮)
  - [10.7 公海回收规则](#107-公海回收规则)
  - [10.8 异常处理](#108-异常处理)
- [11. 线索详情页](#11-线索详情页)
  - [11.1 模块概述](#111-模块概述)
  - [11.2 页面结构](#112-页面结构)
  - [11.3 基础信息字段](#113-基础信息字段)
  - [11.4 操作按钮区](#114-操作按钮区)
  - [11.5 标签页内容](#115-标签页内容)
  - [11.6 异常处理](#116-异常处理)
- [12. 线索中心 · 跟进日志](#12-线索中心-跟进日志)
  - [12.1 模块概述](#121-模块概述)
  - [12.2 数据权限](#122-数据权限)
  - [12.3 页面结构](#123-页面结构)
  - [12.4 筛选条件](#124-筛选条件)
  - [12.5 表格字段](#125-表格字段)
  - [12.6 业务规则](#126-业务规则)
  - [12.7 异常处理](#127-异常处理)
- [13. 线索操作](#13-线索操作)
  - [13.1 入口与权限矩阵](#131-入口与权限矩阵)
  - [13.2 新增线索](#132-新增线索)
  - [13.3 编辑](#133-编辑)
  - [13.4 录入跟进](#134-录入跟进)
  - [13.5 打标签](#135-打标签)
  - [13.6 转客户](#136-转客户)
  - [13.7 标记异常](#137-标记异常)
  - [13.8 变更负责人](#138-变更负责人)
  - [13.9 查看关联消息](#139-查看关联消息)
- [14. 客户中心 · 客户列表](#14-客户中心-客户列表)
  - [14.1 模块概述](#141-模块概述)
  - [14.2 数据权限](#142-数据权限)
  - [14.3 页面结构](#143-页面结构)
  - [14.4 筛选条件](#144-筛选条件)
  - [14.5 表格字段](#145-表格字段)
  - [14.6 客户创建路径](#146-客户创建路径)
  - [14.7 客户操作](#147-客户操作)
  - [14.8 异常处理](#148-异常处理)
- [15. 客户详情页](#15-客户详情页)
  - [15.1 模块概述](#151-模块概述)
  - [15.2 页面结构](#152-页面结构)
  - [15.3 基础信息字段](#153-基础信息字段)
  - [15.4 标签页内容](#154-标签页内容)
  - [15.5 异常处理](#155-异常处理)
- [16. 客户中心 · 合同中心](#16-客户中心-合同中心)
  - [16.1 模块概述](#161-模块概述)
  - [16.2 数据权限](#162-数据权限)
  - [16.3 页面结构](#163-页面结构)
  - [16.4 筛选条件](#164-筛选条件)
  - [16.5 表格字段](#165-表格字段)
  - [16.6 合同操作](#166-合同操作)
  - [16.7 合同状态流转](#167-合同状态流转)
  - [16.8 合同详情页](#168-合同详情页)
  - [16.9 异常处理](#169-异常处理)
- [17. 分析中心 · 销售经营](#17-分析中心-销售经营)
  - [17.1 模块概述](#171-模块概述)
  - [17.2 页面结构](#172-页面结构)
  - [17.3 筛选条件](#173-筛选条件)
  - [17.4 指标卡](#174-指标卡)
  - [17.5 分析区](#175-分析区)
  - [17.7 数据更新与异常](#177-数据更新与异常)
- [18. 分析中心 · 获客分析](#18-分析中心-获客分析)
  - [18.1 模块概述](#181-模块概述)
  - [18.2 页面结构](#182-页面结构)
  - [18.3 顶部指标卡](#183-顶部指标卡)
  - [18.4 消息量趋势](#184-消息量趋势)
  - [18.5 来源渠道分布](#185-来源渠道分布)
  - [18.6 国家/地区分布](#186-国家地区分布)
  - [18.7 意向产品分析](#187-意向产品分析)
  - [18.8 客户关注点分析](#188-客户关注点分析)
  - [18.9 站点价值分析](#189-站点价值分析)
  - [18.10 异常](#1810-异常)
- [19. 分析中心 · 客户经营](#19-分析中心-客户经营)
  - [19.1 模块概述](#191-模块概述)
  - [19.2 页面结构](#192-页面结构)
  - [19.3 指标卡](#193-指标卡)
  - [19.4 图表](#194-图表)
  - [19.5 客户画像](#195-客户画像)
  - [19.6 业务员简化版视图](#196-业务员简化版视图)
  - [19.7 异常](#197-异常)
  - [19.8 模块指标边界说明](#198-模块指标边界说明)
- [20. 站点中心 · 站点管理](#20-站点中心-站点管理)
  - [20.1 模块概述](#201-模块概述)
  - [20.2 列表页](#202-列表页)
  - [20.3 新增/编辑站点弹窗](#203-新增编辑站点弹窗)
  - [20.4 站点详情页](#204-站点详情页)
  - [20.5 字段与规则说明（原配置页字段权威，入口已并入 §20.3）](#205-字段与规则说明原配置页字段权威入口已并入-203)
  - [20.6 站点停用连锁影响](#206-站点停用连锁影响)
  - [20.7 异常](#207-异常)
- [20A. 站点中心 · 站点运营数据](#20a-站点中心-站点运营数据)
  - [20A.1 模块概述](#20a1-模块概述)
  - [20A.2 字段定义](#20a2-字段定义)
  - [20A.3 列表页](#20a3-列表页)
  - [20A.4 新增/编辑弹窗](#20a4-新增编辑弹窗)
  - [20A.5 批量导入](#20a5-批量导入)
  - [20A.6 权限](#20a6-权限)
  - [20A.7 异常](#20a7-异常)
- [21. AI 能力管理](#21-ai-能力管理)
  - [21.1 模块概述](#211-模块概述)
  - [21.2 AI 能力列表](#212-ai-能力列表)
  - [21.3 AI 能力字段](#213-ai-能力字段)
  - [21.4 AI 输出结构与同步](#214-ai-输出结构与同步)
  - [21.5 AI 降级规则](#215-ai-降级规则)
  - [21.6 业务关联](#216-业务关联)
  - [21.7 操作与异常](#217-操作与异常)
  - [21.8 AI 有效询盘识别与自动生成线索（业务流程）](#218-ai-有效询盘识别与自动生成线索业务流程)
- [22. 通知管理](#22-通知管理)
  - [22.1 模块概述](#221-模块概述)
  - [22.2 通知场景配置页](#222-通知场景配置页)
  - [22.3 编辑通知场景弹窗](#223-编辑通知场景弹窗)
  - [22.4 通知场景清单（系统内置）](#224-通知场景清单系统内置)
  - [22.5 Header 铃铛（站内信接收，不占菜单）](#225-header-铃铛站内信接收不占菜单)
  - [22.6 异常](#226-异常)
- [23. 系统管理](#23-系统管理)
  - [23.1 模块概述](#231-模块概述)
  - [23.2 用户管理](#232-用户管理)
  - [23.3 角色管理](#233-角色管理)
  - [23.4 菜单管理](#234-菜单管理)
  - [23.5 字典管理](#235-字典管理)
    - [23.5.1 治理分层](#2351-治理分层)
    - [23.5.2 页面形态](#2352-页面形态)
    - [23.5.3 字典类型字段](#2353-字典类型字段)
    - [23.5.4 字典项字段](#2354-字典项字段)
    - [23.5.5 父子层级规则（一期）](#2355-父子层级规则一期)
    - [23.5.6 管理与引用规则](#2356-管理与引用规则)
    - [23.5.7 字典类型总表与预置项](#2357-字典类型总表与预置项)
  - [23.6 系统参数](#236-系统参数)
  - [23.7 Tab「业务规则」（属系统参数页）](#237-tab业务规则属系统参数页)
  - [23.8 集成配置](#238-集成配置)
    - [23.8.1 Tab1 邮箱服务配置](#2381-tab1-邮箱服务配置)
    - [23.8.2 Tab2 钉钉应用配置](#2382-tab2-钉钉应用配置)
    - [23.8.3 Tab3 文件存储](#2383-tab3-文件存储)
  - [23.9 系统日志](#239-系统日志)
  - [23.10 系统级状态页](#2310-系统级状态页)
- [24. 个人中心](#24-个人中心)
  - [24.1 模块概述](#241-模块概述)
  - [24.2 我的资料](#242-我的资料)
  - [24.3 账号绑定](#243-账号绑定)
  - [24.4 修改密码](#244-修改密码)
  - [24.5 异常](#245-异常)
- [25. 标签体系](#25-标签体系)
  - [25.1 标签分类总览](#251-标签分类总览)
  - [25.2 线索标签](#252-线索标签)
  - [25.3 客户标签](#253-客户标签)
  - [25.4 询盘标签（消息 · AI 分析标签）](#254-询盘标签消息-ai-分析标签)
- [26. 状态流转](#26-状态流转)
  - [26.1 线索状态机](#261-线索状态机)
  - [26.2 合同状态机](#262-合同状态机)
  - [26.3 客户潜质分级（设计说明）](#263-客户潜质分级设计说明)
  - [26.4 站点状态机](#264-站点状态机)
  - [26.5 用户状态机](#265-用户状态机)
- [27. 数据流转与同步规则](#27-数据流转与同步规则)
  - [27.1 线索生成与归属](#271-线索生成与归属)
  - [27.2 AI 信息同步](#272-ai-信息同步)
  - [27.3 联系人归集（BR-035）](#273-联系人归集br-035)
  - [27.4 客户负责人变更同步](#274-客户负责人变更同步)
  - [27.5 合同归属](#275-合同归属)
  - [27.6 回复状态与待回复判定](#276-回复状态与待回复判定)
  - [27.7 统一取数口径（最近签约合同 / 成交金额）](#277-统一取数口径最近签约合同-成交金额)
  - [27.8 时间范围筛选统一交互规则](#278-时间范围筛选统一交互规则)
- [28. 业务规则汇总（BR 索引）](#28-业务规则汇总br-索引)
- [29. 异常处理与兜底规范](#29-异常处理与兜底规范)
  - [29.1 通用空态](#291-通用空态)
  - [29.2 通用加载态](#292-通用加载态)
  - [29.3 通用错误态](#293-通用错误态)
  - [29.4 权限校验失败](#294-权限校验失败)
  - [29.5 并发与冲突](#295-并发与冲突)
  - [29.6 系统级状态页](#296-系统级状态页)
  - [29.7 AI 服务异常（AI 异常唯一权威定义）](#297-ai-服务异常ai-异常唯一权威定义)
  - [29.8 数据删除规则（软删除唯一权威定义）](#298-数据删除规则软删除唯一权威定义)
  - [29.9 操作日志与审计](#299-操作日志与审计)
  - [29.10 GDPR 合规](#2910-gdpr-合规)
  - [29.11 站点与渠道绑定异常](#2911-站点与渠道绑定异常)
- [30. 对象字段模型](#30-对象字段模型)
  - [30.1 线索 Lead](#301-线索-lead)
  - [30.2 客户 Customer](#302-客户-customer)
  - [30.3 客户联系人 Contact](#303-客户联系人-contact)
  - [30.4 合同 Contract](#304-合同-contract)
  - [30.5 对象规则与异常流程（引用）](#305-对象规则与异常流程引用)
  - [30.6 实现口径说明（研发对齐）](#306-实现口径说明研发对齐)
  - [30.7 站点运营数据 SiteOperationData](#307-站点运营数据-siteoperationdata)

---
## 1. 文档信息

| 项 | 内容 |
| :--- | :--- |
| 产品名称 | AI 智能 CRM |
| 文档版本 | v1.1.2 |
| 更新时间 | 2026-07-17 |
| 当前状态 | 待评审 |

> **本版说明：** 延后项在正文中以 `【二期/延后】` 标注、不删除内容，便于二期回滚。

### 1.0 本次变更说明（2026-07-17）

#### 变更点

1. **邮箱接入分层：** 【集成配置 → 邮箱服务】仅维护**全局邮件协议**（IMAP/SMTP 主机、端口、SSL、拉取间隔）；**取消主账号用户名/密码**；多邮箱账号共用该协议。
2. **邮箱凭据随绑定：** 个人中心邮箱绑定新增「密码/授权码」；一邮箱一凭据；收发/拉取使用「全局协议 + 该绑定凭据」。

#### 影响范围

| 范围 | 章节 / 产物 | 影响说明 |
| :--- | :--- | :--- |
| 集成配置 | §23.8.1 | 全局协议；无主账号；无测试连接 |
| 个人中心 | §24.3.1 | 绑定字段含密码/授权码 |

### 1.0.1 变更说明（2026-07-15）

#### 变更点

1. **字典三层治理：** 业务可配 / 受控预置 / 系统基础；页面改为「字典类型列表 → 字典项管理」；不可新增/停用字典类型；编码创建后不可改；一期不做展示颜色、不做「恢复默认」。
2. **父子层级：** 通用最多 2 级；业务表单仅可选叶子（无子项视为叶子）；行业/国家预置仍扁平，允许管理员自建父子；历史父级值保留展示。
3. **新增/调整字典类型：** 录入方式、联系角色、AI 服务商、AI 模型、AI 业务场景；跟进方式扩充（保留会议 + 线上会议/线下拜访/其他）；客户关注选项补齐完整预置 + 展示分类（仅 UI 分组）；客户潜质分级改为高潜客户 / 潜在客户 / 一般客户（新建默认潜在客户）。
4. **跟进阶段扩展属性：** 保留「是否允许转客户」（不自动转化）；新增「是否计入高意向」（与转客户分立）；一期不做「是否为终止阶段」。
5. **来源渠道 ≠ 录入方式：** 取消来源渠道「手动」；线索落库 `entry_method`（列表新建 / 导入 / 消息 AI·人工兜底映射见 §23.5.7）；导入未填来源渠道留空（BR-057）。
6. **AI 能力对接字典：** 服务商/模型从字典选择（可增服务商及其下模型；取消「自定义」手填）；业务关联四场景编码固定，仅可改展示名/排序；服务商/模型停用后能力保留但视为不可用并降级。
7. **高意向口径统一：** 工作台 / 销售经营等读「是否计入高意向」，不得按阶段名称硬编码（BR-038c）。
8. **规则与模型：** 新增 BR-038c、BR-057；§30 线索增加 `entry_method`，调整来源渠道/潜质/联系角色取值说明。

#### 影响范围

| 范围 | 章节 / 产物 | 影响说明 |
| :--- | :--- | :--- |
| 字典与系统管理 | §3、§23.5、菜单「字典管理」 | 页面形态、分层规则、预置项、父子与引用规则 |
| AI 能力 | §21.3、§21.5、§21.6 | 服务商/模型/场景改挂字典；路由仍按场景编码；降级条件含字典停用 |
| 线索创建与导入 | §9.1、§9.7、§13.2、§13.2.1、§27.1 | 录入方式写入；导入来源渠道空值；取消来源「手动」 |
| 跟进与转客户 | §13.4、§13.6、§26.1 相关引用 | 跟进方式预置；阶段双扩展属性；转客户规则不变语义 |
| 工作台 / 分析 | §6.4、§17.4、§17.5、§19（二期高意向客户数口径） | 高意向统计改读扩展属性；潜质分级展示名变更 |
| 客户与联系人 | §14.7、§15.4.3、§26.3 | 潜质默认与枚举；联系角色进字典 |
| 对象模型 / BR | §30.1–§30.3、§28（BR-038b/c、BR-057） | 字段与业务规则索引 |
| 配套文档 | 一期功能需求清单.xls、角色权限说明文档.md | 已同步字典/AI/录入方式/高意向相关条目与权限说明 |
| 未改 | 原型（Publishing space / pages） | **代码未落地本变更**，需另排期改造 |

### 1.1 MVP 范围与延后清单

> 以下为本版明确**延后至二期/后续优化**的功能点，正文对应位置均以 `【二期/延后】` 标注。未列出的模块与功能默认属 MVP 范围。

| 分类 | 延后项 | 影响章节 | 说明 |
| :--- | :--- | :--- | :--- |
| 登录安全 | 登录失败锁定、账号锁定时长 | §5.4、§5.5、§23.6.1 | MVP 不做失败锁定，登录失败仅统一提示 |
| 登录安全 | 单端登录限制 | §23.6.1 | MVP 允许多设备同时登录 |
| 登录安全 | 会话刷新间隔 | §23.6.1 | MVP 仅保留登录超时 |
| 登录安全 | 首次登录强制改密 | §5.4、§23.6.2 | MVP 不强制首次改密 |
| 公海池 | 公海**自动回收**（系统参数天数 + 回收规则 + 定时任务） | §10.7、§20.5.1、§23.7、§26.1 | MVP 仅保留**手动回收 + 手动分配** |
| 工作台 | 「超期回收线索」异常卡片（依赖自动回收） | §6.4 | 随自动回收一并延后 |
| 系统日志 | 「配置变更」Tab | §3、§23.9 | MVP 仅保留登录日志、操作日志 2 Tab |
| 全局搜索 | Header 全局搜索面板 | 菜单结构、框架 | MVP 不提供入口，延后二期 |
| 跟进日志 | 全局跟进日志「对象类型」抽象（客户/合同对象视图）、顶部统计栏、对象类型/对象关键词筛选与表头字段 | §12.1、§12.3–§12.5 | MVP 全局跟进日志以**线索侧**视图交付（筛选/表头对齐当前原型口径）；对象类型模型与统计栏延后二期 |

> **【待定 / 不纳入一期验收】** 以下功能点在需求清单中占位，**待甲方资料补齐并写入 PRD 后再定级排期**；本期不做验收：
>
> | 分类 | 待定项 | 说明 |
> | :--- | :--- | :--- |
> | 沟通中心 | AI 客服列表 | PRD 未定义功能与字段；需求清单 R16 优先级=待定 |
> | 数据分析 | AI 数据分析 | PRD 未定义；需求清单 R81 优先级=待定 |

**MVP 保留（明确不砍）：** 账号密码登录 + 钉钉扫码登录、站内信 + 钉钉推送、批量导入（线索/客户/合同/站点运营数据）、批量 AI 提炼、通知 8 场景、客户活跃度判定（默认 90 天）与客户经营活跃/沉默指标、分析中心三页（销售经营/获客分析/客户经营，角色可见性见 §4.3）。

---

## 2. 产品整体说明

### 2.1 产品定位

面向销售团队的客户关系管理平台，围绕「消息接入（邮件/WhatsApp） → 线索识别 → 跟进转化 → 客户沉淀 → 合同成交 → 经营分析」闭环，为业务员、运营专员、协同人、系统管理员提供分层协作能力。AI 作为平台能力嵌入各业务场景，不可用时系统按规则正常运行。

### 2.2 核心目标

1. 邮件与 WhatsApp 双渠道**消息**统一接入，实现多渠道集中管理，避免线索遗漏。
2. 线索全生命周期管理（识别、归属、跟进、转客户、回收）。
3. 客户档案与联系人持续沉淀，合同录入与成交追溯。
4. 灵活的业务规则配置与权限管理
5. 销售经营、获客、客户经营三维经营分析。

### 2.3 业务边界

- **覆盖：** 消息沟通（邮件/WhatsApp）、线索管理、客户/联系人/合同管理、经营分析、站点管理、系统管理。

### 2.4 对象关系

> **消息定义：** 消息指系统从沟通渠道接入的客户沟通内容，包含邮件消息和 WhatsApp 消息。消息用于沟通记录展示、AI 分析、线索识别、回复状态判断和客户互动沉淀。通知、站内信、钉钉推送不属于消息，统一归入通知体系。

**核心业务对象定义（术语唯一权威）：**

| 对象 | 定义 | 关键特点 |
| :--- | :--- | :--- |
| 询盘联系人 | 来自消息渠道（邮件、WhatsApp 等）主动发起沟通的人 | 是线索来源；**不属于客户资产**；可未识别所属企业 |
| 所属企业 | 系统根据询盘联系人信息识别出的企业或个体工商户名称 | 属**线索阶段信息**（字段名：所属企业/企业名称）；**不是独立业务对象**；**不等同于客户名称**；有值时可在转客户时作为客户名称来源 |
| 线索 | 销售机会对象 | 包含：询盘联系人、所属企业、询盘内容、AI 分析结果、跟进记录；**无线索名称字段，展示用线索编号** |
| 客户 | 已确认进入 CRM 客户管理体系的业务主体 | 客户资产的归属主体；归属一个站点；**MVP 客户唯一性 =（站点 + 客户名称）**；客户名称按统一规则生成（企业/个体工商户取企业名称，个人取询盘联系人姓名）；可拥有多个历史线索（客户 1:N 线索），线索转客户后保留历史关联 |
| 客户联系人 | 客户主体下维护的联系人 | 来源：线索转客户/关联时，**询盘联系人转换/归集为客户联系人**；**必属于且仅属于一个客户**（禁止独立联系人、禁止跨客户共享） |

> **客户名称口径：** 客户列表中的「客户名称」用于展示客户主体名称。若客户主体为企业或个体工商户，客户名称取企业名称；若客户主体为个人，客户名称取询盘联系人姓名。MVP **不强制**设置客户主体类型字段，系统按可识别到的企业名称或询盘联系人姓名生成客户名称；若无法识别客户名称，转客户时需人工补充。客户名称是客户档案的主要展示字段，**不等同于**线索阶段「所属企业 / 企业名称」。MVP **不设独立企业（Company）对象**，无企业管理菜单；跨站点同名多档案由「站点 + 客户名称」唯一性解释。

**对象层级（唯一权威）：
   ** 询盘联系人 → 线索（Lead）→ 关联客户（Customer）→ 客户联系人（Contact）；
   客户资产侧为 客户（Customer）→ 客户联系人（Contact）。

```
客户：**站点 + 客户名称 唯一确定一个客户**（同客户名称跨站点可多档案）
站点 1:N 消息（邮件/WhatsApp） / 线索 / 客户 / 客户联系人
邮箱账号两类：业务员个人邮箱（归业务员本人）、站点主邮箱（站点绑定运营人员、以其业务邮箱作主邮箱，一站点一主邮箱，见 §20.5、§24.3.1）；WhatsApp 账号仅业务员绑定、不绑固定站点（业务员恒单站点，见 §24.3.2）
线索 1:1 询盘联系人；线索含所属企业字段（线索阶段信息，非独立对象）
线索 N:1 客户（一线索至多关联一个客户）；线索 N:1 站点（线索创建即有明确站点，见 §27.1）；线索 1:N 消息（邮件/WhatsApp） / 跟进记录 / 合同
客户 1:N 客户联系人 / 合同 / 线索
询盘联系人 --转客户/关联线索归集--> 客户联系人
合同 N:1 客户 / 线索（合同不设独立站点，继承客户站点）
```

**站点术语（唯一权威，全系统只引用不重复定义）：** 站点是 CRM 中**业务运营与数据隔离的基础单位**（用于用户业务归属、业务数据归属、消息渠道配置、AI 能力配置、业务规则配置），**不是组织架构，不扩展部门/区域/公司组织树**。

| 术语 | 字段 | 归属对象 | 含义与用途 |
| :--- | :--- | :--- | :--- |
| 站点 | `site_id` | 线索 / 客户 / 合同 | 业务数据归属站点；**数据权限判断、业务管理、获客/经营分析的统一站点依据** |
| 授权站点 | `user_site_permissions` | 用户 | 用户拥有访问权限的站点范围；属**权限模型**，非业务字段 |
| 邮箱关联站点 | `mail_site_id` | 邮箱账号 | 邮箱对应站点：业务员个人邮箱=业务员唯一授权站点；站点主邮箱=站点配置中为站点绑定运营人员并选定其业务邮箱所对应的站点（一站点一主邮箱）；邮件线索站点取此站点（见 §20.5、§24.3.1、§27.1） |

> **关键口径：** ① 权限与统计均基于**站点**（`site_id`）；② 邮件线索：站点 = 收件邮箱对应站点；WhatsApp 线索：站点 = 业务员唯一授权站点；**线索创建即有明确站点（不存在站点待补充）**；③ 变更负责人/公海分配均不改站点；④ 合同继承客户站点；⑤ 邮箱对应站点 / WhatsApp 线索站点**不受页面站点筛选器影响**。

---

## 3. 菜单与模块结构

| 一级菜单 | 二级菜单 | 页面 |
| :--- | :--- | :--- |
| 工作台 | — | 工作台 |
| 沟通中心 | 邮件 | 邮件三栏页、写邮件页签、批量 AI 提炼弹窗 |
| 沟通中心 | WhatsApp | WhatsApp 三栏会话页 |
| 线索中心 | 线索列表 | 线索列表、线索详情页、跟进日志页签、各操作弹窗 |
| 线索中心 | 公海池 | 公海池列表、分配弹窗 |
| 客户中心 | 客户列表 | 客户列表、客户详情页、各操作弹窗 |
| 客户中心 | 合同中心 | 合同列表、合同详情页、各操作弹窗 |
| 分析中心 | 销售经营 | 销售经营分析页 |
| 分析中心 | 获客分析 | 获客分析页 |
| 分析中心 | 客户经营 | 客户经营分析页 |
| 站点中心 | 站点管理 | 站点列表、站点详情页、新增/编辑弹窗 |
| 站点中心 | 站点运营数据 | 站点运营数据列表、新增/编辑弹窗、批量导入弹窗 |
| AI 能力管理 | — | AI 能力列表、新增/编辑抽屉 |
| 通知管理 | 通知场景 | 通知场景配置列表、编辑通知场景弹窗（配置推送渠道/对象/模板内容/启停；**不是**通知收件箱） |
| 系统管理 | 用户管理 | 用户列表、新增/编辑弹窗 |
| 系统管理 | 角色管理 | 角色列表 |
| 系统管理 | 菜单管理 | 菜单树 |
| 系统管理 | 字典管理 | 字典类型列表 + 字典项管理 |
| 系统管理 | 系统参数 | 登录安全 / 密码设置 / 通用参数 / 业务规则 4tab |
| 系统管理 | 集成配置 | 邮箱/钉钉/文件存储 3 Tab |
| 系统管理 | 系统日志 | 登录/操作 2 Tab（配置变更 Tab【二期/延后】） |

**不占侧边菜单的入口：**

| 入口 | 路径 | 说明 |
| :--- | :--- | :--- |
| 登录页 | /login | 独立路由 |
| 线索详情 | /leads/detail/:id（工作区页签） | 新开页签；列表/公海/消息等入口共用；不占侧边菜单 |
| 客户详情 | /customers/detail/:id（工作区页签） | 新开页签；列表/消息等入口共用；默认只读（**AI 客户画像可编辑**，§15.4.5）；不占侧边菜单 |
| 通知收件（站内信） | Header 头像左侧铃铛 | 查看本人收到的通知内容（已读/未读等）；**无独立侧边菜单页、无单独承接路由页** |
| 个人中心 | Header 头像下拉 | 我的资料/账号绑定/修改密码 |
| 状态页 | 系统级 | 404/403/500/维护中/网络异常/Loading |
| 全局导航 | 系统框架 | 顶部栏/侧边栏/面包屑 |

---

## 4. 角色与权限体系

> 本章为全系统**权限体系唯一权威定义**。系统权限按「**站点权限 + 角色数据权限粒度 + 角色菜单/按钮权限**」共同控制。  
> **运行时一律按权限标识（权限码）判定菜单可见性与按钮/接口可否操作，禁止前端/后端按角色名称或角色编码写死按钮显隐与鉴权。**  
> 下文「预置四角色矩阵」仅表示**预置角色的默认权限包**；用户实际权限以其所属角色在【角色管理】中的**当前勾选**为准（保存后即时生效，无需发版）。  
> 业务模块数据范围统一引用 §4.2；模块特殊规则（如公海池分配、消息账号本人）见各章及 §4.8。

> **权限模型总原则：**
>
> | 权限维度 | 说明 |
> | :--- | :--- |
> | 站点权限 | 用户只能访问自己授权站点范围内的数据（系统管理员数据档=全部时不受限） |
> | 数据权限粒度 | 角色上配置的一档：仅本人 / 负责站点 / 授权站点（只读） / 全部（§4.2）；创建自定义角色时必选 |
> | 菜单权限 | 角色勾选的菜单权限点；决定侧栏/路由是否可见可进 |
> | 按钮权限 | 角色勾选的按钮权限点；决定按钮显隐/置灰与接口鉴权 |
> | 消息查看边界 | 监督类只读查看与「账号本人可操作」等见 §7.3、§4.8；不得用角色名硬编码替代权限码 |

### 4.1 角色定义

系统内置 **4 个预置角色**（可改展示名称，权限勾选规则见 §23.3），并支持管理员**新增自定义角色**。

| 角色 | 类型 | 默认数据权限 | 职责（预置默认语义） |
| :--- | :--- | :--- | :--- |
| 业务员 | 预置 | 仅本人 | 一线销售；默认可跟进本人线索/客户/合同；默认可绑定多邮箱与 1 个 WhatsApp（硬约束见 §4.8）；不管理他人数据 |
| 运营专员 | 预置 | 负责站点 | 站点运营；默认可管理负责站点业务数据、公海分配、站点主邮箱转派（§4.8）；可只读查看负责站点内业务员消息 |
| 协同人 | 预置 | 授权站点（只读） | 只读协作；默认菜单/按钮包以查看+导出为主，无写操作；禁止系统配置类菜单（默认不勾选） |
| 系统管理员 | 预置 | 全部 | 全公司系统与数据管控；**权限勾选只读保护**（§23.3）；消息对业务员账号仅只读 |
| 自定义角色 | 自定义 | 创建时必选四档之一 | 仅可获得§4.7 中已预置的菜单/按钮权限点 + 所选数据档；**不能**获得 §4.8 所列预置角色专用能力 |

> 用户**仅可绑定一个角色**（§23.2）。预置四角色的详细默认菜单见 §4.3，默认按钮见 §4.7。

### 4.2 数据权限粒度

数据权限由**用户所属角色上配置的数据权限档位 + 授权站点 +（未来可选）团队**共同确定。**每个角色（含自定义）必须且仅能选择以下一档**。当前版本团队管理未启用。适用范围：线索/客户/合同/跟进记录。

| 粒度 | 预置角色默认 | 判定规则（当前版本，团队未启用） | 未来启用团队 |
| :--- | :--- | :--- | :--- |
| 仅本人 | 业务员 | 本人为负责人，且数据站点在授权站点内 | 不受团队影响，仍为本人数据 |
| 负责站点 | 运营专员 | 负责站点（=授权站点）内所有业务员数据（含本人） | 所属团队内业务员数据 |
| 授权站点（只读） | 协同人 | 授权站点范围内所有运营专员/业务员数据，**数据层只读**（写操作另受按钮权限约束；预置协同人默认不勾选写按钮） | 授权站点范围内数据（只读语义保持） |
| 全部 | 系统管理员 | 全公司所有站点数据，不受授权站点限制 | 全部站点 + 全部团队 + 全部数据 |

- **站点定义：** 业务运营与数据隔离基础单位，用于邮件/线索/客户/合同的数据归属（术语权威见 §2.4）。
- **权限唯一站点依据：** 数据可见性 = 数据「站点」(`site_id`) ∈ 用户「授权站点」(`user_site_permissions`) + 角色数据档位（档位=全部时不限制站点）。
- **团队定义：** 团队是**销售管理单元**，仅用于销售数据管理范围扩展；**不是**部门/区域/公司组织树等组织架构。团队为可选权限维度，当前版本仅预留字段，不提供团队管理菜单/新增页面/维护流程/配置后台。（属于二期）
- **角色与操作：** 可执行操作由角色勾选的**按钮权限点**决定（§4.6、§4.7），不以角色名称判断。
- **团队为空兜底规则：** 用户未配置所属团队时，系统按授权站点 + 角色数据档位计算数据权限。二期功能
- **预置协同人默认包：** 默认不勾选写类按钮；授权站点范围内以查看/导出为主（见 §4.7）。管理员若给其他角色勾选写按钮，则以勾选为准。
- **公海池数据权限适用独立规则**（见第 10 章）；分配等操作仍受 `lead-pool:assign` 等按钮权限控制。

### 4.3 菜单权限

> **权威来源：** 用户可见菜单 = 其角色在【角色管理】中勾选的菜单权限点（§4.6）。下表仅为**预置四角色的默认菜单包**（初始化/重置默认时使用）。  
> **图例：** ☑️ = 默认勾选（可见可进入）；☑️只读 = 默认勾选菜单且默认不勾选该模块写按钮；— = 默认不勾选。  
> 菜单权限码见 §4.7「菜单」类。

| 菜单 | 菜单权限码 | 系统管理员 | 运营专员 | 协同人 | 业务员 |
| :--- | :--- | :---: | :---: | :---: | :---: |
| 工作台 | `dashboard:view` | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 沟通中心 · 邮件 | `mail:view` | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 沟通中心 · WhatsApp | `whatsapp:view` | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 线索中心 · 线索列表 | `lead:view` | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 线索中心 · 公海池 | `lead-pool:view` | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 客户中心 · 客户列表 | `customer:view` | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 客户中心 · 合同中心 | `contract:view` | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 分析中心 · 销售经营 | `analysis-sales:view` | ☑️ | ☑️ | ☑️只读 | — |
| 分析中心 · 获客分析 | `analysis-acquisition:view` | ☑️ | ☑️ | ☑️只读 | — |
| 分析中心 · 客户经营 | `analysis-customer:view` | ☑️ | ☑️ | ☑️只读 | ☑️ |
| 站点中心 · 站点管理 | `site:view` | ☑️ | — | — | — |
| 站点中心 · 站点运营数据 | `site-operation-data:view` | ☑️ | ☑️只读 | ☑️只读 | — |
| AI 能力管理 | `ai-capability:view` | ☑️ | — | — | — |
| 通知管理 · 通知场景 | `notification-scene:view` | ☑️ | — | — | — |
| 系统管理 · 用户管理 | `system-user:view` | ☑️ | — | — | — |
| 系统管理 · 角色管理 | `system-role:view` | ☑️ | — | — | — |
| 系统管理 · 菜单管理 | `system-menu:view` | ☑️ | — | — | — |
| 系统管理 · 字典管理 | `system-dict:view` | ☑️ | — | — | — |
| 系统管理 · 系统参数 | `system-param:view` | ☑️ | — | — | — |
| 系统管理 · 集成配置 | `system-integration:view` | ☑️ | — | — | — |
| 系统管理 · 系统日志 | `system-log:view` | ☑️ | — | — | — |
| 个人中心（Header，不占侧边菜单） | （登录即有，无菜单码） | ☑️ | ☑️ | ☑️ | ☑️ |
| 通知收件（Header 铃铛） | （登录即有，无菜单码） | ☑️ | ☑️ | ☑️ | ☑️ |

> **补充：** ① 个人中心 MVP 为头像下拉弹窗（§24）。② 通知管理仅为通知场景/推送渠道**配置**；收件经铃铛/钉钉（§22）。③ 「只读」= 有菜单权限但默认无该模块写按钮。④ 自定义角色默认无任何勾选，由管理员配置。

### 4.4 操作权限类型

操作权限落地为 §4.7 **按钮权限码**。业务上可归类为：新建、编辑、删除、分配、转客户、导出、导入、配置等。各模块矩阵见对应章节，均须标注权限码，且声明「以角色实际勾选为准」。

### 4.5 站点权限

- 用户仅可查看已授权站点数据；站点筛选下拉仅展示授权站点（数据档=全部时为全部站点），**含「已停用」站点（带标记），可查看停用站点历史数据**。
- **授权站点个数：** 数据档=仅本人时用户通常单站点（预置业务员强制单站点）；负责站点 / 授权站点（只读）/ 全部 可多站点。用户管理校验：预置业务员角色仅允许单站点。
- **渠道账号绑定：** 属 §4.8 预置角色专用硬约束（非按钮勾选可赋予自定义角色）。

### 4.6 动态权限运行机制

```
预置权限资源（菜单/按钮权限点，随版本入库）
    → 角色管理：为角色勾选菜单 + 按钮，并选择数据权限档位
    → 用户管理：用户绑定唯一角色 + 授权站点
    → 登录成功：下发该用户菜单权限集、按钮权限集、数据档位、授权站点
    → 前端：按权限码控制菜单与按钮；后端：按权限码鉴权接口
```

| 规则 | 说明 |
| :--- | :--- |
| 配置即时生效 | 角色权限勾选保存后，**已登录用户下次请求/刷新权限或重新登录**后按新权限生效（实现可选：保存后主动刷新权限缓存）；**无需发版** |
| 禁止角色硬编码 | 不得使用 `role === 'sales'` / `collaborator` 等控制按钮；一律 `hasPerm('lead:export')` 等形式 |
| 权限点来源 | MVP **不支持**在菜单管理后台新建权限点；新按钮能力须发版（前端入口 + 预置权限点种子数据）后，再在角色管理勾选分配 |
| 系统管理员保护 | 预置系统管理员角色的菜单/按钮勾选**只读不可改**；不可删除/停用/降权该角色；系统至少保留一名启用的系统管理员用户 |
| 自定义角色边界 | 可勾选 §4.7 全部普通权限点；**不可**获得 §4.8 专用能力 |
| 无权限表现 | 见 §29.4 |

### 4.7 预置权限点总表

> **权威说明：** 本表为 MVP 预置权限资源清单。列「管/运/协/业」表示预置四角色**默认是否勾选**（✓=默认勾选，—=默认不勾选）。系统管理员行对业务按钮默认全 ✓（消息代发类除外，见说明）。自定义角色以角色管理勾选为准。  
> **类型：** M=菜单，B=按钮。

#### 4.7.1 菜单权限点

| 权限码 | 名称 | 类型 | 管 | 运 | 协 | 业 |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: |
| `dashboard:view` | 工作台 | M | ✓ | ✓ | ✓ | ✓ |
| `mail:view` | 邮件 | M | ✓ | ✓ | ✓ | ✓ |
| `whatsapp:view` | WhatsApp | M | ✓ | ✓ | ✓ | ✓ |
| `lead:view` | 线索列表 | M | ✓ | ✓ | ✓ | ✓ |
| `lead-pool:view` | 公海池 | M | ✓ | ✓ | ✓ | ✓ |
| `customer:view` | 客户列表 | M | ✓ | ✓ | ✓ | ✓ |
| `contract:view` | 合同中心 | M | ✓ | ✓ | ✓ | ✓ |
| `analysis-sales:view` | 销售经营 | M | ✓ | ✓ | ✓ | — |
| `analysis-acquisition:view` | 获客分析 | M | ✓ | ✓ | ✓ | — |
| `analysis-customer:view` | 客户经营 | M | ✓ | ✓ | ✓ | ✓ |
| `site:view` | 站点管理 | M | ✓ | — | — | — |
| `site-operation-data:view` | 站点运营数据 | M | ✓ | ✓ | ✓ | — |
| `ai-capability:view` | AI 能力管理 | M | ✓ | — | — | — |
| `notification-scene:view` | 通知场景 | M | ✓ | — | — | — |
| `system-user:view` | 用户管理 | M | ✓ | — | — | — |
| `system-role:view` | 角色管理 | M | ✓ | — | — | — |
| `system-menu:view` | 菜单管理 | M | ✓ | — | — | — |
| `system-dict:view` | 字典管理 | M | ✓ | — | — | — |
| `system-param:view` | 系统参数 | M | ✓ | — | — | — |
| `system-integration:view` | 集成配置 | M | ✓ | — | — | — |
| `system-log:view` | 系统日志 | M | ✓ | — | — | — |

#### 4.7.2 按钮权限点（业务）

| 权限码 | 名称 | 所属菜单 | 管 | 运 | 协 | 业 | 备注 |
| :--- | :--- | :--- | :---: | :---: | :---: | :---: | :--- |
| `lead:add` | 新增线索 | 线索列表 | ✓ | ✓ | — | ✓ | |
| `lead:edit` | 编辑线索 | 线索列表 | ✓ | ✓ | — | ✓ | 含客户画像编辑 |
| `lead:tag` | 打标签 | 线索列表 | ✓ | ✓ | — | ✓ | |
| `lead:follow` | 录入跟进 | 线索列表/详情/跟进日志 | ✓ | ✓ | — | ✓ | |
| `lead:convert` | 转客户（单条） | 线索列表/详情 | ✓ | ✓ | — | ✓ | |
| `lead:convert-batch` | 转客户（批量） | 线索列表 | ✓ | ✓ | — | — | |
| `lead:mark-invalid` | 标记异常（列表） | 线索列表 | ✓ | ✓ | — | ✓ | |
| `lead:change-owner` | 变更负责人 | 线索列表 | ✓ | ✓ | — | — | |
| `lead:view-related-msg` | 查看关联消息 | 线索详情 | ✓ | ✓ | ✓ | ✓ | 消息内操作仍受 §7.3 |
| `lead:export` | 批量导出线索 | 线索列表 | ✓ | ✓ | ✓ | ✓ | |
| `lead:import` | 批量导入线索 | 线索列表 | ✓ | ✓ | — | ✓ | |
| `lead:recycle` | 回收至公海 | 线索列表 | ✓ | ✓ | — | — | |
| `lead-pool:assign` | 公海分配 | 公海池 | ✓ | ✓ | — | — | |
| `lead-pool:mark-invalid` | 公海标记异常 | 公海池 | ✓ | ✓ | — | — | |
| `follow:export` | 导出跟进日志 | 跟进日志 | ✓ | ✓ | ✓ | ✓ | |
| `customer:add` | 新建客户 | 客户列表 | ✓ | ✓ | — | ✓ | |
| `customer:edit` | 编辑客户（含标签） | 客户列表 | ✓ | ✓ | — | ✓ | |
| `customer:change-owner` | 变更客户负责人 | 客户列表 | ✓ | ✓ | — | ✓ | |
| `customer:add-contact` | 添加联系人 | 客户列表 | ✓ | ✓ | — | ✓ | |
| `customer:export` | 导出客户 | 客户列表 | ✓ | ✓ | ✓ | ✓ | |
| `customer:import` | 批量导入客户 | 客户列表 | ✓ | ✓ | — | ✓ | |
| `contract:add` | 新增合同 | 合同中心 | ✓ | ✓ | — | ✓ | |
| `contract:edit` | 编辑合同 | 合同中心 | ✓ | ✓ | — | ✓ | |
| `contract:void` | 作废合同 | 合同中心 | ✓ | ✓ | — | ✓ | |
| `contract:export` | 导出合同 | 合同中心 | ✓ | ✓ | ✓ | ✓ | |
| `contract:import` | 批量导入合同 | 合同中心 | ✓ | ✓ | — | ✓ | |
| `mail:compose` | 写邮件/回复/转发/删除 | 邮件 | ✓ | ✓ | — | ✓ | **另受账号本人约束**（§4.8） |
| `mail:generate-lead` | 生成线索（人工兜底） | 邮件 | ✓ | ✓ | — | ✓ | |
| `mail:batch-ai` | 批量 AI 提炼 | 邮件 | ✓ | ✓ | — | ✓ | |
| `whatsapp:send` | 发送/回复/删除 WA | WhatsApp | — | — | — | ✓ | **另受账号本人+仅预置业务员可绑**（§4.8） |
| `whatsapp:generate-lead` | WA 生成线索 | WhatsApp | ✓ | — | — | ✓ | |
| `site:add` | 新增站点 | 站点管理 | ✓ | — | — | — | |
| `site:edit` | 编辑站点 | 站点管理 | ✓ | — | — | — | |
| `site:enable` | 启用/停用站点 | 站点管理 | ✓ | — | — | — | |
| `site-operation-data:add` | 新增运营数据 | 站点运营数据 | ✓ | — | — | — | |
| `site-operation-data:edit` | 编辑运营数据 | 站点运营数据 | ✓ | — | — | — | |
| `site-operation-data:delete` | 删除运营数据 | 站点运营数据 | ✓ | — | — | — | |
| `site-operation-data:import` | 导入运营数据 | 站点运营数据 | ✓ | — | — | — | |
| `site-operation-data:export` | 导出运营数据 | 站点运营数据 | ✓ | — | — | — | |
| `ai-capability:add` | 新增 AI 能力 | AI 能力管理 | ✓ | — | — | — | |
| `ai-capability:edit` | 编辑 AI 能力 | AI 能力管理 | ✓ | — | — | — | |
| `ai-capability:enable` | 启停 AI 能力 | AI 能力管理 | ✓ | — | — | — | |
| `ai-capability:delete` | 删除 AI 能力 | AI 能力管理 | ✓ | — | — | — | |
| `notification-scene:edit` | 编辑通知场景 | 通知场景 | ✓ | — | — | — | |
| `system-user:add` | 新增用户 | 用户管理 | ✓ | — | — | — | |
| `system-user:edit` | 编辑用户 | 用户管理 | ✓ | — | — | — | |
| `system-user:reset-pwd` | 重置密码 | 用户管理 | ✓ | — | — | — | |
| `system-user:enable` | 启用/停用用户 | 用户管理 | ✓ | — | — | — | |
| `system-role:add` | 新增角色 | 角色管理 | ✓ | — | — | — | |
| `system-role:edit` | 编辑角色/配权 | 角色管理 | ✓ | — | — | — | |
| `system-role:delete` | 删除角色 | 角色管理 | ✓ | — | — | — | |
| `system-menu:edit` | 修改菜单 | 菜单管理 | ✓ | — | — | — | |
| `system-dict:edit` | 维护字典项 | 字典管理 | ✓ | — | — | — | |
| `system-param:edit` | 编辑系统参数 | 系统参数 | ✓ | — | — | — | |
| `system-integration:edit` | 编辑集成配置 | 集成配置 | ✓ | — | — | — | |
| `system-log:export` | 导出系统日志 | 系统日志 | ✓ | — | — | — | |

### 4.8 预置角色专用业务硬约束

> 以下能力**不能**通过给自定义角色勾选普通按钮权限获得；仅预置角色（或账号本人）按规则生效。实现时按**场景规则**校验，仍禁止仅用角色名散落判断（应集中在规则服务/常量策略中，并与预置角色编码绑定）。

| 硬约束 | 适用 | 说明 |
| :--- | :--- | :--- |
| WhatsApp 账号绑定 | 仅预置「业务员」 | 1 个/人；其他预置角色与自定义角色均不可绑定（§24.3.2） |
| 邮箱绑定 | 预置业务员/运营专员 | 协同人不可绑；自定义角色默认不可绑（除非后续发版扩展，本期不开放） |
| 站点主邮箱【转派给业务员】 | 仅预置「运营专员」 | T1–T3，见 §7.5.2；对应能力不进入自定义角色可选项 |
| 消息代发/回复/转发/删除 | **仅账号本人** | 即使用户有 `mail:compose` / `whatsapp:send`，查看他人邮箱/会话时仍只读（§7.3） |
| 系统管理员保护 | 预置「系统管理员」 | 角色权限勾选只读；不可删/停用/降权；至少一名启用管理员用户 |
| 预置业务员单站点 | 预置「业务员」 | 用户管理强制仅 1 个授权站点 |

---

## 5. 登录与认证

### 5.1 模块概述

- **定位：** 统一身份认证入口，登录成功后按用户唯一角色加载**菜单权限码、按钮权限码、数据权限档位、授权站点**（§4.6）并进入工作台。
- **面向角色：** 全部角色（含自定义角色）。

### 5.2 页面结构


| 区域 | 内容 |
| :--- | :--- |
| 品牌区 | Logo + "AI 智能 CRM" |
| 登录方式 Tab | 账号密码登录 \| 钉钉扫码登录 |
| 表单区 | 账号输入框 + 密码输入框 + 记住账号复选框 |
| 操作区 | 【登录】按钮 + "忘记密码"链接（提示"请联系系统管理员重置密码"，不自助重置） |
| 底部 | 版权信息 |

### 5.3 登录方式与字段

#### 5.3.1 账号密码登录（默认）

| 字段 | 类型 | 必填 | 校验 / 错误提示 |
| :--- | :--- | :--- | :--- |
| 登录账号 | 文本 | 是 | 非空，"请输入登录账号" |
| 登录密码 | 密码（可显隐） | 是 | 非空，"请输入登录密码" |
| 记住账号 | 复选框 | 否 | 选中后下次自动填充账号（本地存储） |

#### 5.3.2 钉钉扫码登录

无需账号密码，依赖集成配置（钉钉应用配置）钉钉凭证发起 OAuth。

### 5.4 业务规则

- 登录流程：校验账号存在 → 校验密码 → 校验账号状态 → 生成 Token(JWT) → 初始化权限 → 记录登录日志 → 进入工作台。
- **【二期/延后】登录失败锁定：** MVP 不做失败锁定，登录失败仅按 §5.5 统一提示"用户名或密码错误"。二期方案：同一账号连续登录失败达「登录失败锁定次数」（默认 5 次，见 §23.6）时锁定账号，锁定期为「账号锁定时长」（默认 30 分钟）；锁定期内禁止登录，成功登录后失败计数清零；锁定期满自动解锁，系统管理员重置密码亦可解锁；参数为 0 时不启用锁定。
- 钉钉扫码：已绑定→自动登录；未绑定→提示"当前钉钉员工未绑定系统用户，请联系系统管理员"；账号停用→禁止登录。
- 登录成功初始化：用户信息（ID/姓名/角色/授权站点）+ **菜单权限码集合 + 按钮权限码集合 + 数据权限档位**（来自用户唯一角色的当前勾选，见 §4.6）+ 进入工作台。前端/后端据此动态控制，**禁止按角色名写死**。
- 退出登录：清 Token/Session → 销毁前端状态 → 重定向登录页；无二次确认，有未保存编辑时轻提示。
- 会话：登录创建 Session + 返回 JWT，每次请求携带 Token 鉴权；Session 仅用于日志与超时。
- 登录超时：默认 1440 分钟（系统参数可配），超时弹"您的会话已超时，请重新登录"并跳登录页。
- 同时登录：允许多设备同时登录（**【二期/延后】单端登录限制**见 §23.6.1）。
- 密码策略：初始密码由系统管理员分配（默认 123456，首次登录不强制改）；长度 ≥ 8 位可配；字符类型可配。
- 登录日志：记录时间、账号、IP、方式、结果（含失败原因）、浏览器、操作系统（BR-055）。

### 5.5 登录失败提示

| 场景 | 提示 |
| :--- | :--- |
| 用户不存在 / 密码错误 | 用户名或密码错误（统一提示，防账号枚举） |
| 用户停用 | 当前账号已停用，请联系系统管理员 |
| 钉钉未绑定 | 当前钉钉员工未绑定系统用户，请联系系统管理员 |
| 网络异常 | 网络连接异常，请稍后重试 |

### 5.6 MVP 范围

- **支持：** 账号密码登录、钉钉扫码登录、登录日志、用户改密、系统管理员重置密码、登录超时退出、记住账号。

---

## 6. 工作台

### 6.1 模块概述

- **定位：** 用户每日工作入口，核心回答"我今天需要处理什么？"；聚焦待办事项、异常提醒与当前个人业务状态。
- **职责边界：** **负责**——待办事项、异常提醒、当前个人业务状态；**不负责**——深度经营分析、历史趋势分析、渠道分析、客户价值分析（上述统一在分析中心：销售经营 / 获客分析 / 客户经营）。
- **面向角色：** 全部角色。

### 6.2 页面结构

| 区域 | 内容 |
| :--- | :--- |
| 顶部筛选栏 | 站点筛选（多站点角色） |
| 今日待办区 | 待回复线索、邮件未读、跟进超时、新增线索、待分配 |
| 我的业务状态区 | 在跟线索、高意向线索、负责客户（仅本人总数，无环比/趋势） |
| 异常提醒区 | 无效线索、丢失线索、异常站点、AI 服务不可用 |
| 快捷入口 | 各角色高频业务页面入口 |

### 6.3 数据权限

数据范围遵循 §4.2。工作台特有的站点筛选展示：

| 角色 | 站点筛选 |
| :--- | :--- |
| 业务员 | 不展示（单站点） |
| 运营专员 | 展示，可选负责站点 |
| 协同人 | 展示，可选授权站点（只读） |
| 系统管理员 | 展示，可选全部站点 |

### 6.4 卡片定义

**（一）今日待办**

| 卡片 | 定义 | 点击跳转 | 适用角色 |
| :--- | :--- | :--- | :--- |
| 待回复线索 | 本人为负责人、权限范围内状态="待跟进"的线索数  | 线索列表（状态=待跟进） | 全部 |
| 邮件未读 | 本人绑定邮箱未读邮件数（仅邮件） | 邮件（筛选未读） | 全部 |
| 跟进超时 | 权限范围内**已填写**下次跟进时间且下次跟进时间<今日、关联线索未终态的跟进/线索数 | 线索列表（按下次跟进<今日过滤；无线下次跟进时间的不计入） | 全部 |
| 新增线索 | 本人为负责人、创建时间在所选时间范围内的线索数 | 线索列表（按时间范围过滤创建时间） | 全部 |
| 待分配 | 权限范围内状态="待分配"的线索数 | 公海池 | 运营专员/协同人/系统管理员 |

**（二）异常提醒**

| 卡片 | 定义 | 点击跳转 | 适用角色 |
| :--- | :--- | :--- | :--- |
| 无效线索 | 权限范围内状态=无效的线索数 | 线索列表（按状态过滤） | 全部 |
| 丢失线索 | 权限范围内状态=丢失的线索数 | 线索列表（按状态过滤） | 全部 |
| 异常站点 | 权限范围内处于异常状态的站点数 | 站点管理 | 系统管理员/运营专员/协同人 |
| AI 服务不可用 | AI 能力全部不可用（未配置/未启用/调用失败）时的提示（引用 §29.7）| AI 能力管理（管理员）| 全部（仅提示，不计数）|

**（三）业务状态**

> 仅统计**本人**数据、仅展示总数（无环比/趋势），经营与趋势分析在分析中心。

| 卡片 | 定义 | 点击跳转 | 适用角色 |
| :--- | :--- | :--- | :--- |
| 在跟线索 | 本人为负责人、非终态的线索总数 | 线索列表 | 全部 |
| 高意向线索| 本人为负责人、当前跟进阶段「是否计入高意向=是」的线索数（字典扩展属性，§23.5；不得按阶段名称硬编码） | 线索列表（按阶段过滤） | 全部 |
| 负责客户 | 本人为负责人的客户总数 | 客户列表 | 全部 |


### 6.5 快捷入口

| 入口 | 跳转 | 适用角色 |
| :--- | :--- | :--- |
| 邮件  | 沟通中心 | 全部 |
| WhatsApp | 沟通中心 | 全部 |
| 线索列表 | 线索中心→线索列表 | 业务员/运营专员/协同人 |
| 公海池 | 线索中心→公海池 | 运营专员/协同人/系统管理员 |
| 客户列表 / 合同中心 | 客户中心 | 业务员/运营专员/协同人 |
| 销售经营 / 获客分析 | 分析中心 | 协同人/运营专员 |
| 客户经营 | 分析中心→客户经营 | 业务员/运营专员/协同人 |
| 站点管理 / 用户管理 / 系统管理 | 站点中心 / 系统管理 | 系统管理员 |


### 6.6 异常处理

引用《全局异常处理规范》§29.1–§29.3。模块特有：单卡数据无数据/失败时该卡主数值显"--"，不影响其他卡片。

---

## 7. 沟通中心 · 邮件

### 7.1 模块概述

- **定位：** 统一管理邮箱邮件，提供查看、AI 智能分析能力；**线索由 AI 自动识别有效询盘后自动生成（主流程），人工生成线索为异常兜底**（见 §21.8、§27.1）。邮箱来源两类：① **业务员个人邮箱**（业务员在【个人中心 → 账号绑定】自绑，可多个）；② **站点主邮箱**（系统管理员在【站点管理·新增/编辑】为站点绑定运营人员，并以该运营人员的业务邮箱作为站点对外接收询盘的主邮箱，见 §20.5、§24.3.1）。
- **面向角色：** 业务员（本人绑定邮箱的邮件，可收发处理）；运营专员（站点主邮箱邮件 + **只读**查看负责站点内业务员邮箱）；协同人/系统管理员（授权/全部范围内**只读**查看，见 §7.3 消息查看与操作边界）。

### 7.2 页面结构

#### 7.2.1 三栏布局

| 区域 | 宽度 | 内容 |
| :--- | :--- | :--- |
| 左栏 | 固定 240px | 邮件列表 |
| 中栏 | 自适应，最小 400px | 邮件正文（只读） |
| 右栏 | 占比 40%，可折叠 | AI 智能分析面板 |

#### 7.2.2 顶部导航

- 第一行：邮箱切换器（下拉，默认首次添加的邮件账号，切换后进收件箱并选中首封）、【写邮件】、搜索框（当前邮箱内搜正文/主题/发件人/收件人；**输入防抖 300ms 后自动查询、无需按 Enter，清空搜索框恢复完整列表**）、批量操作（批量已读、批量 AI 提炼、批量删除）。
- 第二行：文件夹 Tab（收件箱[默认]、已发送、草稿箱、垃圾箱）。
- 第三行：邮件时间范围筛选（见 §7.2.3）。

#### 7.2.3 邮件时间范围筛选

| 字段 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 邮件时间 | 日期区间（开始/结束） | 不限制 | 按当前文件夹语义筛选；允许只填开始或只填结束；开始不晚于结束；空值表示不限制 |

- **业务含义（按文件夹）：**
  - 收件箱：按**收件时间**筛选。
  - 已发送：按**发送时间**筛选。
  - 草稿箱：按**最后保存时间**筛选。
  - 垃圾箱：按**原邮件时间**筛选（即原收件/发送时间，不使用删除时间；当前版本不单独维护删除时间字段）。
- **叠加规则：** 邮箱切换器优先限定当前邮箱范围；文件夹 Tab 与邮件时间筛选叠加生效；时间筛选只影响列表查询，不改变邮箱归属、不改变权限范围。
- **重置规则：** 点击重置时**保留当前邮箱和当前文件夹**，仅重置邮件时间筛选条件（恢复默认"不限制"）。

### 7.3 数据权限（消息查看与操作边界）

> **权限码：** 写邮件/回复等受 `mail:compose`、`mail:generate-lead`、`mail:batch-ai`、`whatsapp:send`、`whatsapp:generate-lead` 控制（§4.7）；**另必须满足 §4.8「仅账号本人可操作」**——即使勾选了 `mail:compose`，查看他人邮箱时仍只读。  
> **【转派给业务员】** 为预置运营专员专用硬约束（§4.8），不进入自定义角色可选项。下表为预置角色默认行为说明。

- **预置业务员：** 可见并**可操作**（查看/回复/转发/删除/标记）本人绑定邮箱的邮件；运营通过【转派给业务员】投递的邮件自动进入本人绑定邮箱【收件箱】，业务员通过**线索负责人**感知归属，无需理解底层投递机制。
- **预置运营专员：** 可操作**站点主邮箱**（本人绑定）邮件；对负责站点内**业务员邮箱仅只读查看**，不可代回复/发送/转发/删除。
- **预置协同人：** 授权站点范围内业务员邮箱**仅只读查看**（默认不勾选 `mail:compose`）。
- **预置系统管理员：** 全部邮箱**只读查看**（不可代业务员发送/回复）。
- **自定义角色：** 按所勾选权限码 + 数据档位；无 §4.8 转派/绑 WA 能力；消息写操作仍受「账号本人」约束。

> 非消息账号本人查看业务员邮箱时，页面进入**只读模式**，隐藏或置灰回复、发送、转发、删除等操作按钮（§4.8）。

**消息查看与操作边界（跨渠道通用；含邮件消息与 WhatsApp 消息；预置角色默认）：**

| 操作 | 账号本人 | 运营专员 | 协同人 | 系统管理员 |
| :--- | :---: | :---: | :---: | :---: |
| 查看本人绑定渠道消息（邮箱 / WhatsApp） | 是 | — | — | — |
| 查看授权范围内业务员邮箱 | 否 | 是（负责站点） | 是（授权站点） | 是（全部） |
| 查看授权范围内业务员 WhatsApp | 否 | 是（负责站点） | 是（授权站点） | 是（全部） |
| 回复业务员邮件 | 本人可操作 | 否 | 否 | 否 |
| 发送业务员 WhatsApp 消息 | 本人可操作 | 否 | 否 | 否 |
| 转发业务员邮件 | 本人可操作 | 否 | 否 | 否 |
| 删除业务员邮件 | 本人可操作 | 否 | 否 | 否 |
| 修改业务员消息已读状态 | 本人可操作 | 否 | 否 | 否 |
| 修改业务员消息回复状态 | 本人可操作 / 系统自动 | 否 | 否 | 否 |

> 转派规则见 §7.5.2（T1–T3）；站点主邮箱线索归属与转派见 §27.1。

**写邮件与代发边界（邮箱；预置角色默认，按钮另受 `mail:compose` 勾选约束）：**

| 角色 | 写邮件 | 回复/转发本人邮件 | 查看他人邮件 | 代发他人邮件 | 发件邮箱范围 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| 业务员 | 是 | 是 | 否 | 否 | 本人绑定邮箱 |
| 运营专员 | 是 | 是 | 是，只读 | 否 | 本人绑定邮箱、站点主邮箱（见 §7.6.7） |
| 协同人 | 否 | 否 | 是，只读 | 否 | — |
| 系统管理员 | 仅限本人已绑定邮箱 | 仅限本人已绑定邮箱 | 是，只读 | 否 | 本人绑定邮箱（若有） |

> 查看业务员邮箱时进入**只读模式**，不展示【写邮件】【回复】【转发】【删除】等代发操作。写邮件页详细规则见 §7.6。

### 7.4 邮件列表字段

#### 7.4.1 列表字段与展示

| 字段 | 展示规则 |
| :--- | :--- |
| 复选框 | 支持多选，最左侧 |
| 发件人/收件人 | 收件箱显示发件人（未读加粗）；已发送显示收件人（不加粗）；垃圾箱按原邮件类型 |
| 主题 | 邮件主题 |
| 邮件摘要 | 正文前 100 字符，超长"..." |
| 时间 | 今天时分、历史月/日、跨年 YYYY/MM/DD |

- 默认按时间倒序；点击列表项中栏刷新正文。
- **草稿箱**字段：收件人（空显"无主题草稿"）、收件邮箱、最后保存时间、操作（继续编辑/删除）；默认按最后保存时间倒序，20 条/页。

#### 7.4.2 各文件夹操作

| 文件夹 | 操作 | 结果 |
| :--- | :--- | :--- |
| 收件箱 | 点击详情自动标记已读；批量标记已读；删除、批量删除；批量 AI 提炼 | 删除移至垃圾箱 |
| 已发送 | 查看详情、转发、删除、批量删除 | 转发规则见 §7.6.12 |
| 草稿箱 | 继续编辑、删除（二次确认）、批量删除 | 软删除（不物理清理；**【二期/延后】** 30 天恢复/自动清理机制） |
| 垃圾箱 | 恢复、永久删除（二次确认）、批量恢复、清空垃圾箱 | 恢复移回原文件夹（软删除，历史保留； 超 30 天自动物理清理） |


### 7.5 邮件正文（中栏）

#### 7.5.1 基础信息

主题、发件人、时间、收件人、站点、正文（只读）、附件（可下载，失败提示不影响查看）。

> 线索自动生成归属、转派投递等业务规则见 **§7.10**（BR-008、BR-053）；转派操作与 T1–T3 见 §7.5.2。

#### 7.5.2 操作按钮

| 操作 | 前置条件 | 结果 |
| :--- | :--- | :--- |
| 生成线索（人工兜底）| 邮件未关联线索（AI 未自动生成 / AI 不可用 / 无法识别时使用，非主流程）| 弹【确认生成线索】弹窗；**字段集与新增线索一致**（见 §13.2 / §13.2.1）；AI 成功有值则预填可改，**AI 不可用/失败则业务字段全部不预填**；站点/来源/负责人等**默认带入但可改**；确认后按表单所选归属；已关联则提示线索编号并跳转 |
| 转派给业务员（运营专员）| 运营专员在**站点主邮箱**查看邮件时 | 弹窗选择目标业务员（本站点启用销售）并确认；**一步完成**：系统自动将邮件投递至目标业务员绑定邮箱 + 按**转派规则 T1–T3**（见下）处理线索负责人；**运营无需再手动转发**；转派不进公海（BR-008、BR-020、BR-053）。**仅站点主邮箱展示此按钮**；业务员个人邮箱线索归本人、无需转派。**未关联线索也可转派**（仅投递，见 T3） |
| 回复 | — | 打开写邮件页签，规则见 §7.6.11 |
| 转发 | — | 打开写邮件页签，规则见 §7.6.12；**纯邮件协作，不变更线索负责人，不触发转派逻辑**（与【转派给业务员】独立） |
| 删除 | — | 移至垃圾箱，已生成线索不同步删除 |

**转派机制（用户无感知，BR-053）：**

- **用户侧：** 运营仅点击【转派给业务员】并选择业务员；**不出现**写邮件/转发页签，**不要求**运营二次手动转发；页面**不展示**「转发」「Fwd:」等投递相关标签。
- **系统侧：** 确认后自动完成邮件投递（底层转发至目标业务员绑定邮箱）+ 按 T1–T3 处理负责人；投递件内部标记为转派来源，**不触发 AI 再建线索**；若当前邮件**已关联线索**则投递件关联该线索（BR-021 例外，见 §7.10）；若**未关联线索**则投递件亦不建线索、不自动挂旧线索。
- **业务员侧：** 在本人绑定邮箱收件箱收到该邮件，与普通来件展示一致；可通过线索列表/详情中的**负责人**确认归属；**无线索的转派件**可在邮件上使用【生成线索（人工兜底）】成为有效线索。

**站点主邮箱转派规则 T1–T3（运营专员点击转派时系统自动判断）：**

| 规则 | 判断条件 | 系统动作 |
| :---: | :--- | :--- |
| T1 | 当前邮件**已关联线索**，且线索负责人为**当前运营** | 弹窗可选本站点业务员 → 投递邮件至目标业务员绑定邮箱 + 将该线索负责人变更为目标业务员；**不新建线索** |
| T2 | 当前邮件**已关联线索**，且线索负责人已是**业务员 A** | 转派目标**锁定为 A，不允许换成其他人** → 投递邮件至 A 绑定邮箱；负责人保持 A；**不新建线索**。弹窗展示说明：「该邮件已关联线索，负责人为 [A]，仅可转派给本人」 |
| T3 | 当前邮件**未关联线索** | **允许转派**（不因无线索拦截）→ **仅投递邮件**；**不新建线索**；**不按发件邮箱自动关联已有旧线索**。业务员收到后可自行【生成线索】。若按「站点 + 客户原始发件邮箱」匹配到已有线索且负责人为业务员 A：目标业务员**默认选中 A，允许运营改选其他人**（本邮件尚未挂线索，换人不改动旧线索负责人）；并提示「该发件人在本站点已有线索，负责人 [A]（仅提示，不自动合并）」 |

> **口径拆分（唯一权威，消除与「一邮件一线索」的歧义）：**
> - **建线索：** 有效询盘仍按 BR-021 **一封邮件一条线索**；**入库不做**按发件邮箱自动合并/去重。同发件人多封有效询盘 = 多条线索（可并存）。
> - **转派：** 管「邮件投递给谁」+「若本邮件已挂线索则如何处理负责人」；**不**用匹配键把未挂线索的邮件并进旧线索。
> - **「站点 + 客户原始发件邮箱」：** 仅用于转派时的**提示 / 默认选人（T3）/ 有线索时锁定同人（T2）**，**不是**建线索去重键，**不是**自动并线索条件。
> - **不使用**收件邮箱、运营人员作为「同人」匹配依据；客户名称仅作参考展示，不自动合并。
> - 一封邮件最多关联一条线索；一条线索可关联多封邮件（仅限：转派投递件挂回原线索、或人工关联线索等明确动作，对齐 BR-021、BR-022）。
> - 转派仅存在于邮箱渠道，WhatsApp 无转派。

### 7.6 写邮件页签

- **页面定位：** 支持用户在系统内撰写并发送邮件，用于业务沟通、客户回复、线索跟进等场景。
- **权限：** 写邮件、回复、转发、代发边界见 **§7.3**；发件邮箱选择与切换见 §7.6.7。
- **【转派给业务员】不经过本页签**，由 §7.5.2 独立处理。

##### 7.6.1 页面入口

| 入口 | 说明 |
| :--- | :--- |
| 邮件列表 →【写邮件】 | 打开空白邮件撰写页 |
| 邮件详情 →【回复】 | 打开邮件撰写页，自动带入原邮件收件人、主题和原邮件内容 |
| 邮件详情 →【转发】 | 打开邮件撰写页，自动带入原邮件正文和附件，收件人为空 |

---

##### 7.6.2 页面布局

邮件撰写页采用单页编辑布局，包含收件人区域、主题区域、正文编辑器、底部发件人区域。

| 区域 | 内容 |
| :--- | :--- |
| 收件人区域 | 收件人、抄送 |
| 主题区域 | 邮件主题输入 |
| 编辑工具栏 | 撤销、重做、图片、插入、导入文档、日程、表情、格式、字体、字号、加粗、斜体、下划线、删除线、字体颜色、背景色、列表、缩进、引用、代码等 |
| 正文编辑区 | 邮件正文富文本编辑 |
| 底部操作区 | 发件人邮箱、发送按钮、保存草稿、取消 |

---

##### 7.6.3 字段说明

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :---: | :--- |
| 收件人 | 邮箱地址输入 / 联系人选择 | 是 | 支持输入一个或多个邮箱地址；支持从联系人中选择；多个收件人以标签形式展示 |
| 抄送 | 邮箱地址输入 / 联系人选择 | 否 | 点击“抄送”后展示抄送输入框 |
| 主题 | 文本输入 | 是 | 邮件主题，回复邮件时自动带入 `Re:` 前缀，转发邮件时自动带入 `Fwd:` 前缀 |
| 正文 | 富文本编辑器 | 是 | 邮件正文内容，支持文字、图片、链接、表格、附件引用等内容编辑 |
| 发件人 | 下拉选择 / 只读 | 是 | **回复/转发**：锁定为当前查看邮箱所属邮箱，不可切换。**空白写邮件**：可在本人可用发件邮箱中选择（见 §7.6.7） |
| 附件 | 文件上传 | 否 | 支持上传图片、文档等常见文件格式 |

---

##### 7.6.4 收件人区域规则

| 场景 | 规则 |
| :--- | :--- |
| 手动输入邮箱 | 输入后校验邮箱格式，格式正确后生成收件人标签 |
| 多个收件人 | 支持添加多个收件人 |
| 删除收件人 | 点击收件人标签上的删除图标移除 |
| 邮箱格式错误 | 输入框下方提示“请输入正确的邮箱地址” |
| 收件人为空发送 | 阻止发送，提示“请填写收件人” |
| 抄送  | 默认收起，点击后展开输入区域 |

---

##### 7.6.5 主题规则

| 场景 | 规则 |
| :--- | :--- |
| 新写邮件 | 主题默认为空 |
| 回复邮件 | 自动填充原邮件主题，并添加 `Re:` 前缀 |
| 转发邮件 | 自动填充原邮件主题，并添加 `Fwd:` 前缀 |
| 主题为空发送 | 阻止发送，提示“请填写邮件主题” |

---

##### 7.6.6 正文编辑器

**工具栏功能：**

| 功能 | 说明 |
| :--- | :--- |
| 撤销 / 重做 | 支持撤销或恢复最近编辑操作 |
| 图片 | 支持插入本地图片 |
| 插入 | 支持插入链接、分割线等内容 |
| 导入文档 | 支持从本地文档导入正文内容 |
| 日程 | 支持插入日程信息 |
| 表情 | 支持插入表情 |
| 格式 | 支持切换正文格式 |
| 字体 | 支持选择字体 |
| 字号 | 支持选择字号 |
| 加粗 | 设置文字加粗 |
| 斜体 | 设置文字斜体 |
| 下划线 | 设置文字下划线 |
| 删除线 | 设置文字删除线 |
| 字体颜色 | 设置文字颜色 |
| 背景色 | 设置文字背景色 |
| 列表 | 支持有序列表、无序列表 |
| 缩进 | 支持增加或减少缩进 |
| 引用 | 支持插入引用样式 |
| 代码 | 支持插入代码样式文本 |

**正文规则：**

- 正文为空时，输入区显示占位文案“输入正文”。
- 正文支持富文本编辑。
- 正文内容为空时不允许发送。
- 若为回复邮件，正文下方可带入原邮件内容。
- 若为转发邮件，正文中自动带入原邮件正文信息。

---

##### 7.6.7 发件人规则

> **总原则：** 发件邮箱与「当前查看邮箱上下文」对齐，避免回复/转发时换通道导致客户回信落到另一邮箱、再触发新入站线索链。**空白写邮件**允许在本人可用邮箱中选择

| 场景 | 规则 |
| :--- | :--- |
| **回复 / 转发** | 发件人 = **当前正在查看的该邮件所属邮箱**（即来信所在邮箱 / 列表当前邮箱上下文）；**锁定不可切换**（只读展示） |
| **空白写邮件**（点【写邮件】，非回复/转发） | **不锁定**；可在底部发件人区域于**本人可用发件邮箱**中切换；默认优先当前列表已选邮箱，否则取默认/首个可用邮箱 |
| 当前用户仅绑定一个邮箱 | 空白写邮件时默认该邮箱，不展示切换或仅只读展示 |
| 当前用户无可用发件邮箱 | 禁用发送按钮，提示“暂无可用发件邮箱，请先绑定邮箱” |
| 发件邮箱异常 | 禁用发送按钮，提示“当前发件邮箱不可用，请检查邮箱配置” |
| 运营站点主邮箱 | 在**站点主邮箱上下文**下回复/转发时，发件人锁定为主邮箱；空白写邮件时，若用户对该主邮箱有发送权限，可将其选为发件人 |
| 查看他人邮箱 | 不允许使用他人邮箱发送，见 §7.3；只读查看时不提供回复/转发代发 |

> **与线索关系：** 回复/转发锁定发件邮箱，降低「换邮箱发出 → 客户回信进另一邮箱 → 新入站再建线索」的割裂风险。空白写信可选邮箱，仍须注意选错通道可能带来后续多线索；不因此自动合并线索（BR-021）。

---

##### 7.6.8 操作按钮

| 按钮 | 说明 | 前置条件 |
| :--- | :--- | :--- |
| 发送 | 发送当前邮件 | 收件人、主题、正文、发件人均已填写且校验通过 |
| 保存草稿 | 将当前邮件保存为草稿 | 当前邮件存在收件人、主题、正文或附件中的任一内容 |
| 取消 | 关闭邮件撰写页 | 无条件 |
| 添加附件 | 上传邮件附件 | 文件格式和大小符合系统限制 |

---

##### 7.6.9 发送规则

| 校验项 | 规则 |
| :--- | :--- |
| 收件人 | 必填，且邮箱格式正确 |
| 主题 | 必填 |
| 正文 | 必填 |
| 发件人 | 必填，且邮箱状态可用 |
| 附件 | 文件大小和格式需符合系统限制 |

**发送成功：**

- Toast 提示“邮件发送成功”。
- 邮件记录进入已发送列表。
- 若为回复邮件，将当前被回复邮件的回复状态更新为“已回复”。
- 若邮件关联线索，则同步更新线索**最近消息时间**（该线索最近一条关联消息的时间）。
- 若从线索或客户详情进入，发送成功后对应沟通记录中新增该邮件。

**发送失败：**
- Toast 提示“邮件发送失败，请重试”。
- 保留当前撰写内容，不清空表单。
- 若失败原因可识别，展示具体原因，如“发件邮箱不可用”“收件人地址无效”“附件上传失败”。

---

##### 7.6.10 草稿规则

| 场景 | 规则 |
| :--- | :--- |
| 点击保存草稿 | 保存当前邮件内容为草稿 |
| 未点击保存草稿直接关闭 | 不自动保存 |
| 已保存草稿后继续编辑 | 需再次点击保存草稿才更新草稿内容 |
| 保存成功 | Toast 提示“草稿已保存” |
| 保存失败 | Toast 提示“草稿保存失败，请重试” |

**草稿保存范围：**

| 内容 | 是否保存 |
| :--- | :---: |
| 收件人 | 是 |
| 抄送 | 是 |
| 主题 | 是 |
| 正文 | 是 |
| 附件 | 是 |
| 发件人邮箱 | 是 |

---

##### 7.6.11 回复邮件规则

| 项目 | 规则 |
| :--- | :--- |
| 收件人 | 默认填入原邮件发件人 |
| 主题 | 默认填入 `Re: 原邮件主题` |
| 正文 | 正文编辑区为空，原邮件内容展示在引用区域 |
| 附件 | 默认不带入原邮件附件 |
| 发送成功 | 将原邮件回复状态更新为“已回复” |
| 回复状态影响范围 | 仅更新当前邮件，不影响同一线索下其他未回复消息 |

---

##### 7.6.12 转发邮件规则

| 项目 | 规则 |
| :--- | :--- |
| 收件人 | 默认空 |
| 主题 | 默认填入 `Fwd: 原邮件主题` |
| 正文 | 自动带入原邮件正文 |
| 附件 | 默认带入原邮件附件 |
| 发送成功 | 不改变原邮件回复状态 |

---

##### 7.6.13 附件规则（撰写）

| 项目 | 规则 |
| :--- | :--- |
| 上传方式 | 支持点击上传或拖拽上传 |
| 支持格式 | MVP 支持图片、PDF、Word、Excel 等常见格式 |
| 大小限制 | 按系统附件规则限制 |
| 上传失败 | 提示“附件上传失败，请重试” |
| 删除附件 | 发送前可删除已上传附件 |
| 发送中 | 附件上传未完成时不允许发送 |

> 邮件正文区附件的展示与下载规则见 §7.7。

---

##### 7.6.14 权限控制

写邮件、回复、转发、代发权限见 **§7.3**；发件邮箱选择与切换见 §7.6.7。

---

##### 7.6.15 异常状态与提示

| 场景 | 处理方式 |
| :--- | :--- |
| 无可用发件邮箱 | 禁用发送按钮，提示“暂无可用发件邮箱，请先绑定邮箱” |
| 发件邮箱异常 | 提示“当前发件邮箱不可用，请检查邮箱配置” |
| 收件人为空 | 提示“请填写收件人” |
| 收件人格式错误 | 提示“请输入正确的邮箱地址” |
| 主题为空 | 提示“请填写邮件主题” |
| 正文为空 | 提示“请输入邮件正文” |
| 附件上传失败 | 提示“附件上传失败，请重试” |
| 邮件发送失败 | 提示“邮件发送失败，请重试” |
| 邮件发送中关闭页面 | 弹窗确认“邮件正在发送，关闭后可能导致发送失败，是否确认关闭？” |
| 未保存内容关闭页面 | 弹窗确认“当前邮件尚未保存，关闭后内容将丢失，是否确认关闭？” |

### 7.7 附件规则

展示于正文下方（文件名/大小/图标）；单附件≤100MB，数量上限9 个；点击文件名下载；仅收件人/发件人/抄送人可下载；系统日志记录"附件下载"。下载失败按链接失效/文件丢失/网络超时（可重试）/类型不支持分别提示。

### 7.8 批量 AI 提炼

- 触发：列表多选 → 【批量 AI 提炼】；仅同一邮箱、单次≤50 封；已读/未读均可、同封可重复提炼（指可重复生成 PDF，**不等于**消息详情「重新分析」；MVP **不提供** AI 智能分析面板的重新分析入口）。
- 弹窗只读展示：已选邮件数、主题、发件人、发件时间、影响范围（"分析 N 封，预计 X 秒"）。
- 流程：确认 → Loading（不可重复提交）→ 生成 PDF 自动下载至本地（系统内不单独展示结果）；失败 Toast"AI 提炼失败，请重试"。
- **与客户画像：** 批量提炼过程中，若询盘联系人**尚无**客户画像则按 §7.9 生成并落在询盘联系人资料；若**已有**客户画像则跳过生成、使用存量（不覆盖）。意向产品/询盘标签仅在该消息有效询盘并生成线索时按 §7.9 / §25 写入线索。

### 7.9 AI 智能分析面板（右栏）

当系统**收到**邮件或 WhatsApp 消息时，对**当前消息自动执行一次** AI 分析，结果持久化后在右侧「AI 智能分析」面板展示。**打开消息详情仅展示已有分析结果，不因打开而重复调用 AI。** MVP **不提供**「重新分析」入口（批量 AI 提炼见 §7.8，与本面板重新分析无关）。

消息发起人统一称为**询盘联系人**。

AI 仅作为辅助能力。AI 未启用、调用失败、只生成部分结果、无法识别所属企业或客户画像部分为空时，**不阻断**消息查看、人工/自动生成线索、线索跟进、关联客户或转客户（BR-015）。

#### 分析内容（一级四项）

| 一级模块 | 说明 | 数据流转 |
| :--- | :--- | :--- |
| 意向总结 | 根据当前消息总结询盘联系人需求、采购意向与关注点（建议 100–200 字） | **仅属当前消息的 AI 分析结果**（随消息分析结果保存并展示）；**不写入线索字段，不写入客户** |
| 意向产品 | 识别当前消息中的意向产品 | 有效询盘并**生成线索时**写入线索「意向产品」字段；**不写入客户** |
| 客户画像 | **统一模块**，内含二级：① 企业工商信息 ② 过往参展信息 ③ 企业风险提示。三者**不是**相互独立的一级 AI 模块 | 分析结果落在**询盘联系人资料**（可关联所属企业信息）；线索详情**引用展示**（不另建可被消息反复覆盖的线索画像副本）。生成/复用规则见下 |
| 询盘标签 | 根据当前消息生成的标签（产品/需求/行业/采购阶段等；即原「AI 分析标签」） | 有效询盘并**生成线索时**按 §25 / BR-016a **追加**写入线索标签（同名去重；人工删过的同名禁止再追加）；**不写入客户** |

**客户画像 · 企业工商信息交互：** 识别所属企业工商资料；若按「当前站点 + 客户名称 / 邮箱域名」匹配到已有客户，可展示【查看已有客户档案】；**仅提示，不阻止生成线索、不创建重复客户**。

#### 客户画像字段
**① 企业工商信息**
企业名称、官网/企业域名、 linkedin公司主页，所属行业、成立年份、国家/地区、员工人数区间、年收入或收入区间

**② 过往参展信息**
参展次数、最近参展时间、参展年份、展会名称、活跃度/趋势

**③ 企业风险提示**
根据当前公司网上的风险信息


#### 客户画像生成与复用（询盘联系人侧，BR-016c）

1. 客户画像在**消息 AI 分析阶段**生成（不是等到转客户才首次生成）。
2. **首次**：该询盘联系人尚无客户画像时，AI 生成并保存到询盘联系人资料。
3. **存量优先**：只要该询盘联系人**过往已有**客户画像，后续消息分析**跳过生成、只用存量**，新结果**不覆盖**。
4. **人工优先**：业务人员已编辑过的客户画像，以人工为准，AI **不覆盖**（BR-017）。
5. 无法识别所属企业时，客户画像允许部分为空，不阻止生成线索或转客户。

#### 生成线索时同步

- 意向产品 → 线索「意向产品」字段。
- 询盘标签 → 追加写入线索标签（规则见 §25）。
- 客户画像 → 线索详情通过询盘联系人资料**关联展示**（带入可见的客户画像）；不把意向总结写入线索。

#### 转客户 / 关联已有客户时同步（BR-036）

- 询盘联系人归集为客户联系人（BR-035）。
- **客户侧尚无客户画像记录** → 将询盘联系人侧客户画像写入客户「客户画像」。
- **客户侧已有客户画像记录（只要有过即算）** → **整包不碰、绝不覆盖**（不做补空）。
- **国家/地区、所属行业（客户标准字段）预填：** 取自客户画像·企业工商信息中的「国家/地区」「所属行业」；写入前与字典（客户域→国家/地区、行业）对齐，**匹配不上则留空**（不写自由文本、不强制「其他」）。
  - **新建客户：** 预填上述两字段（无值则空）；随后可在客户列表【编辑】修改。
  - **关联已有客户：** 仅当客户该字段为空时预填；**已有值不覆盖**。
- 意向总结、意向产品、线索标签 / 询盘标签 → **不写入客户**。

权威字段同步总表见 §21.4；标签细则见 §25。

### 7.10 业务规则

- **AI 自动识别并生成线索（主流程，BR-019a/BR-020）：** 收到邮件 → AI 自动判定是否为有效询盘（标准见 §21.8）→ 有效则自动提取字段并**自动创建线索**（无需人工点击），无效不生成；每封邮件独立生成线索（一消息一线索 BR-021，同封多需求合并 BR-023）。
- **询盘联系人影子对象（BR-012）：** 首次收到发件人邮件时按姓名/邮箱自动创建询盘联系人影子对象（尚未归集为客户联系人），已有则关联不重复；归客户后可联动展示客户名；归集规则见 27.3。
- **生成线索归属（BR-008，两类邮箱）：** ① **业务员个人邮箱**：站点=该邮箱对应站点（业务员唯一授权站点），负责人=**业务员本人**，直接进入本人线索列表；② **站点主邮箱（运营人员业务邮箱）**：站点=该站点主邮箱对应站点（`mail_site_id`，见 §20.5），负责人=**该站点绑定的运营人员**（归属异常兜底见 §27.1），由运营人员点击【转派给业务员】一步完成投递与负责人变更、不进公海（转派 T1–T3、BR-053 见 §7.5.2）；**人工兜底生成归属当前操作人**；关闭 AI 时可人工生成线索。站点未绑定运营人员/主邮箱时其邮件不参与自动生成线索。
- **转派投递（BR-053）：** 站点主邮箱【转派给业务员】确认后，系统自动投递邮件至目标业务员绑定邮箱，并按 T1–T3 处理负责人（**未关联线索时仅投递、不建线索、不自动挂旧线索**）；已关联线索的投递件**不触发 AI 建线索**并关联原线索；页面不向用户展示「转发」相关标签。
- **消息回复状态（BR-019b，邮件实现）：** 按单条邮件消息维护，默认"未回复"；该邮件首次回复发送成功后仅当前邮件置"已回复"，不影响同一线索下其他未回复消息；口径见 §27.6（**不**驱动工作台「待回复线索」卡）。

### 7.11 异常处理

- **AI 能力异常：** 引用《全局异常处理规范》§29.7；AI 不可用时**不自动生成线索**，邮件消息保留，转由**人工兜底创建线索**；【生成线索】弹窗按 §13.2.1：**业务字段全部不预填**（含发件邮箱也不自动带入），由用户手填；站点/来源/负责人**默认带入但可改**，不阻断业务（见 §21.5、§21.8）。
- **加载/网络异常：** 引用 §29.2、§29.3（邮件加载失败提示"邮件加载失败，请刷新重试" + 【重新加载】）。
- **空态（模块特有）：** 收件箱"暂无邮件"、已发送"暂无已发送邮件"、草稿箱"暂无草稿"、垃圾箱"垃圾箱暂无邮件"、搜索"未找到相关邮件"。

---

## 8. 沟通中心 · WhatsApp

### 8.1 模块概述

- **定位：** 统一管理绑定的 WhatsApp 会话，提供会话查看回复、AI 智能分析面板；**线索由 AI 自动识别有效询盘后自动生成（主流程），人工生成为异常兜底**（见 §21.8、§27.1）。
- **面向角色与数据权限：** WhatsApp 账号**仅业务员可绑定**（1 个/人，见 §24.3.2）；**业务员**可查看并操作（回复/发送/删除）本人 WhatsApp 会话；**运营专员（负责站点）/协同人（授权站点）/系统管理员（全部）可只读查看**授权范围内业务员的 WhatsApp 会话，**不可代发/回复/删除**（消息查看与操作边界见 §7.3）。

### 8.2 页面结构

三栏布局：左栏联系人列表（选中高亮）、中栏会话正文（未选中显引导）、右栏详情面板（可折叠）。顶部：搜索框（按联系人名/企业名/消息内容；**输入防抖 300ms 后自动查询、无需按 Enter，清空搜索框恢复完整列表**）、最近消息时间范围筛选、刷新按钮。

#### 8.2.1 最近消息时间范围筛选

| 字段 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 最近消息时间 | 日期区间（开始/结束） | 不限制 | 按会话最近一条消息时间筛选；允许只填开始或只填结束；开始不晚于结束；空值表示不限制该时间条件 |

- **业务含义：** 按会话**最近一条消息时间**筛选会话列表（不区分消息方向）。
- **叠加规则：** 与搜索框叠加生效；时间筛选只影响当前会话列表查询，不改变 WhatsApp 线索归属规则、不改变权限范围。
- **重置规则：** 点击重置时恢复默认时间范围（不限制）。

### 8.3 联系人列表字段

| 字段 | 展示规则 |
| :--- | :--- |
| 头像 | 缩略图 |
| 联系人名称 | WhatsApp 名称 |
| 消息摘要 | 最近一条前 30 字符，超长"..." |
| 时间 | 今天时分、昨天"昨天"、更早月/日、跨年 YYYY/MM/DD |

空态：无会话"暂无 WhatsApp 会话"；搜索无结果"未找到相关会话"。

### 8.4 会话正文（中栏）

- 按时间正序；客户消息右对齐（蓝气泡），我方左对齐；
- 消息类型：文本（完整）、图片（缩略图可预览）、文件（名+大小，可下载）。
- 输入区：多行输入（Enter 发送、Shift+Enter 换行）、发送按钮、上传图片（jpg/png）、上传文件（pdf/docx/xlsx/xls/pptx/ppt/zip/rar）。
- 加载：默认最近 30 条，滚动到顶懒加载更早 30 条；默认定位最新；无消息"暂无消息记录"。
- 中栏空态：未选联系人显"请从左侧选择一个会话开始沟通"。

### 8.5 详情面板（右栏）

#### 8.5.1 询盘联系人基础信息

姓名、手机号、企业名称（所属企业，AI 提取）、地理位置（AI 提取）。已归客户时企业名/地理位置优先展示客户侧数据。

#### 8.5.2 站点规则

WhatsApp 账号**不绑定固定站点**，仅绑定业务员（见 §24.3.2、§8.1）。因**业务员恒为单一授权站点**，WhatsApp 线索站点必定明确、不存在"站点待补充"。

AI 识别有效询盘后自动生成正式线索，站点规则如下：

| 场景 | 站点 | 进入位置 |
| :--- | :--- | :--- |
| 业务员（单一授权站点） | 业务员唯一授权站点 | 线索列表 |

**自动生成线索归属该 WhatsApp 账号绑定且启用的业务员**（归属异常兜底见 §27.1）；**人工兜底生成归属当前业务员**。WhatsApp 线索站点不受页面站点筛选器影响。

> **关键口径：** WhatsApp 线索负责人 = WhatsApp 绑定业务员，创建即"待跟进"、**不进公海池**、站点明确。

#### 8.5.3 AI 智能分析面板

与邮件侧 §7.9 **同一套口径**（一级四项：意向总结、意向产品、客户画像、询盘标签；客户画像含企业工商信息 / 过往参展信息 / 企业风险提示）。收到 WhatsApp 消息时分析一次并持久化；打开会话仅展示已有结果；MVP 不提供重新分析入口。客户画像落询盘联系人资料、首次生成后复用存量、人工编辑优先（BR-016c、BR-017）；询盘标签写入线索见 §25 / BR-016a。字段同步总表见 §21.4。

#### 8.5.4 业务操作

| 操作 | 前置条件 | 结果 |
| :--- | :--- | :--- |
| 生成线索（人工兜底）| 会话未关联线索（AI 未自动生成 / AI 不可用 / 无法识别时使用，非主流程）| 弹【确认生成线索】弹窗；**字段集与新增线索一致**（见 §13.2 / §13.2.1）；已关联则提示线索编号并跳转 |
| 查看线索详情 | 会话已关联线索 | **新开页签**跳转线索详情（§11） |
| 查看客户 | 联系人已归客户 | **新开页签**跳转客户详情（§15） |

#### 8.5.5 生成线索弹窗字段

> **唯一权威字段表见 §13.2**；消息人工生成与新增线索**同一字段集**。本表仅补充 WhatsApp 默认值与少量差异（总规则见 §13.2.1）。**除 WhatsApp 会话电话外，不锁死字段**（来源可改为官网等）。

| 字段 | 相对 §13.2 | WhatsApp 差异 |
| :--- | :--- | :--- |
| 企业名称（所属企业） | 同：文本/选择已有客户 | 新客户有识别名则预填；命中已有客户则带入该客户名称并可改选其他客户；无则空 |
| 询盘联系人 | 同 | AI 有值预填可改；AI 失败不预填 |
| 邮箱 | 同（可空） | 有则预填；无则空。可用电话或 WhatsApp 满足「邮箱/电话/WhatsApp 至少一个」 |
| 电话 | 同 | **仅此项只读**：带入 WhatsApp 账号号码（计入「电话」）；不改会话号 |
| WhatsApp | 同 | 可预填为会话号（可与只读电话同值） |
| 站点 | 同 | **默认**业务员唯一授权站点，**可改**（权限范围内） |
| 来源渠道 | 同 | **默认** WhatsApp，**可改**（如改为官网询盘等；通道≠获客来源） |
| 负责人 | 同 | **默认**当前操作业务员，**可改**（权限范围内） |
| 意向产品 | 同 | AI 有值预填可改；AI 失败不预填 |
| 关注点 | 同 | 默认空，可填 |
| 备注 | 同 | 默认空，可填 |
| 线索标签 | 同 | AI 询盘标签有值可预填为标签候选，可改；AI 失败不预填 |

#### 8.5.6 提交成功

人工兜底生成线索提交成功：
按弹窗所选站点/负责人落库，线索状态=待跟进；来源渠道以表单最终值为准。
提交成功后 Toast“线索已生成”，会话更新为“已生成线索”，隐藏【生成线索（人工兜底）】，显示【查看线索详情】。
AI自动生成线索的归属规则见 §27.1，不适用本人工兜底提交规则。

### 8.6 异常处理

- **AI 能力异常：** 引用 §29.7；【生成线索】可用；按 §13.2.1 **业务字段全部不预填**；站点/来源/负责人默认带入可改；WA 电话只读仍带入。
- **加载/发送异常：** 消息/会话加载失败引用 §29.3（+【重新加载】）；消息发送失败旁红色错误图标，点击可重发/删除。
- **联系人已删除/匿名化（模块特有）：** 历史消息保留，名称显"已注销"，提示"该联系人已被删除"。

---

## 9. 线索中心 · 线索列表

### 9.1 模块概述

- **定位：** 销售跟进核心入口，管理有权限查看的全部线索。
- **面向角色：** 业务员、运营专员、协同人、系统管理员。
- **线索来源（四类）：** ① **AI 自动创建**（消息链路有效询盘自动生成，含邮件/WhatsApp，见 §21.8、§27.1）；② **消息侧人工生成**（邮件/WhatsApp【生成线索】兜底，字段同新增线索，见 §13.2.1）；③ **列表手动新建**（线索列表【新增线索】，见 §13.2）；④ **批量导入**（见 §9.7）。获客分析「生成线索数」仅计①+②（消息链路），不含③④（见 §18.3）。**录入方式**与上列对应：①②→`inquiry_conversion`，③→`manual_create`，④→`batch_import`（§23.5.7、BR-057）；与「来源渠道」分立。
- **与公海池关系：** 两个独立列表；公海池待分配线索不在线索列表展示；分配后移入线索列表；线索列表"回收至公海"将线索移回公海池；两者共用线索详情页（**新开页签**，§11）。

### 9.2 数据权限

数据范围遵循 §4.2；线索 `site_id` 须在授权站点内（线索创建即有明确站点，见 §2.4）。按钮受 `lead:*` / `lead-pool:*` 控制（§4.7、§13.1.2）；预置协同人默认不勾选写按钮、默认可 `lead:export`。**运行时以角色实际勾选为准**，禁止按角色名写死。

### 9.3 页面结构

| 区域 | 内容 |
| :--- | :--- |
| 顶部状态标签页 | 按状态快捷筛选 |
| 筛选条件栏 | 多维筛选 |
| 表格区 | 数据表格 + 操作 + 分页 |

**状态标签页：** 全部、待跟进、跟进中、已转客户、已成交、无效、丢失；与筛选区"状态"下拉联动。**线索状态与跟进阶段分别维护、互不映射**：线索状态取值与流转以 §26.1（线索状态机）为准，跟进阶段见 §23.5、§13.4，本页仅展示不重复定义。

### 9.4 筛选条件

| 字段 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 站点 | 下拉 | 全部可见站点 | 仅授权站点 |
| 创建时间 | 日期区间 | 本月 | 按线索创建时间；`createTimeStart`/`createTimeEnd` |
| 最近跟进时间 | 日期区间 | 不限制 | 按线索最近一次跟进记录时间；`lastFollowUpTimeStart`/`lastFollowUpTimeEnd`；空值不限制 |
| 下次跟进时间 | 日期区间 | 不限制 | 按线索下次跟进时间；用于今日待跟进/超期跟进/未来跟进筛选；`nextFollowUpTimeStart`/`nextFollowUpTimeEnd`；空值不限制 |
| 更新时间 | 日期区间 | 不限制 | 按线索更新时间（列表默认按更新时间倒序，故支持按更新时间筛选）；`updateTimeStart`/`updateTimeEnd`；空值不限制 |
| 线索关键词 | 文本 | 空 | 模糊搜线索编号/询盘联系人/企业名称/邮箱 |
| 状态 | 下拉 | 全部 | 按线索状态 |
| 阶段 | 下拉 | 全部 | 取后台完整阶段列表 |
| 意向产品 | 下拉 | 全部 | AI 产品类目集合，可检索 |
| 来源渠道 | 下拉多选 | 全部 | 取值见 §23.5（客户域→来源渠道）；消息链路系统仅自动写入邮件/WhatsApp |
| 负责人 | 下拉 | 全部 | 权限范围内成员 |
| 标签 | 下拉多选 | 全部 | 多选逻辑=包含任一（OR） |

操作按钮：查询、重置、展开/收起。

> **时间筛选口径：**
> - 各时间字段独立筛选、互不混用；允许只填开始或只填结束；开始不晚于结束；空值表示不限制该时间条件。
> - 时间筛选**只影响当前列表查询**，不改变用户权限范围、不改变站点权限。
> - **工作台"跟进超时"卡片跳转线索列表时**，带入"下次跟进时间 < 今日"筛选条件（即 `nextFollowUpTimeEnd` = 昨日，且仅展示非终态线索），不改变权限范围。
> - 重置时恢复线索列表默认时间范围（创建时间=本月，其余时间字段=不限制）。

### 9.5 表格字段

| 字段 | 展示规则 | 点击 |
| :--- | :--- | :--- |
| 复选框 | 最左侧 | — |
| 线索编号 | 如 LEAD-2026-0911 | — |
| 询盘联系人 | 询盘联系人姓名；已归客户增显客户名（可跳客户详情）；缺失显"匿名联系人" | 客户名跳转客户详情 |
| 来源 | 展示「来源渠道」字典项名称。**消息链路系统写入仅两种：** 邮件 →「邮件」、WhatsApp →「WhatsApp」，**不会**写成「其他」；官网询盘/展会/客户转介绍/其他等仅人工新建、导入或用户改选时出现（取值见 §23.5）。空值或已失效字典项显 `—` | — |
| 站点 | 站点名称 | — |
| 负责人 | 负责人姓名 | — |
| 状态 | 待跟进/跟进中/已转客户/已成交/无效/丢失，彩色徽章（定义见 26.1） | — |
| 阶段 | 取最新跟进记录阶段，无则"-" | — |
| 标签 | 线索标签（AI 来源+人工混排，AI 来源自 AI 分析标签写入），最多 3 个超出"+N"（悬停全部） | 点击标签按该标签筛选 |
| 关注点 | 线索当前关注点集合（字典：客户域→客户关注选项）；最多 3 个超出"+N"（悬停全部）；无则"-" | — |
| 意向产品 | 前 2 个，超出"+N"（悬停全部），未识别"-" | — |
| 最近跟进 | 最近跟进时间，无则"-" | — |
| 创建时间 | 完整日期时间 | — |
| 操作 | 跟进、打标、编辑、更多 | 见第 13 章 |

分页：默认按更新时间倒序；20 条/页；20/50/100；支持页码跳转。

### 9.6 操作按钮

#### 9.6.1 顶部操作

跟进日志（新开页签）、转客户（选中≥1，批量仅运营专员/系统管理员；协同人不可）、新增线索、批量导入、回收至公海（选中≥1，允许状态见 10.7）、批量导出。

#### 9.6.2 行操作列

跟进、打标签、编辑、回收至公海、查看详情、查看关联消息、转客户、标记异常、变更负责人。各操作详见第 13 章；终态置灰规则见各操作说明。

### 9.7 批量导出 / 批量导入

**批量导出：**

- 触发：多选 → 【批量导出】。
- 字段：导出范围（当前选中[默认]/当前筛选结果/当前页）、文件格式（只读 CSV）、导出上限（"最多 10000 条"）。
- 规则：超 10000 条提示"数据量过大，请缩小筛选范围"；成功下载本地，失败 Toast"导出失败，请重试"。

**批量导入：**

- **入口：** 线索列表顶部【批量导入】；先【下载导入模板】。**导入模板字段与线索列表表头一致**（表头不一致整单拒绝，提示"导入模板与系统字段不一致，请下载最新模板"）。
- **流程：** 下载模板 → 填写 → 上传（Excel/CSV）→ 系统校验 → 预览（错误行标红与原因）→ 确认导入。
- **必填校验：** 询盘联系人、邮箱/电话/WhatsApp 至少一个、站点（须在操作人权限范围内）；企业名称（所属企业）可选；关注点可选；来源渠道选填（取值见 §23.5）；**未填写来源渠道则留空**，不得写入「手动」（「手动」不是来源渠道）。
- **录入方式：** 系统自动写入 `entry_method=batch_import`（批量导入），用户不可改（§23.5）。
- **归属：** 导入线索须指定站点（`site_id`，须在操作人权限范围内）；负责人默认导入操作人，运营专员/系统管理员可在模板内指定权限范围内业务员；状态默认"待跟进"、**不进公海池**；匹配到已有客户仅提示、不阻止、不创建重复客户（对齐 §13.2、BR-034）。
- **模板字段：** 与 §13.2 新增线索字段集一致（含 WhatsApp、关注点、备注、来源渠道等；**不含**录入方式，由系统写入）。
- **结果汇总：** "成功 N 条、失败 K 条"；导入记录操作日志（操作类型=导入，见 §23.9、BR-054）。
- **权限：** 业务员（仅本人）、运营专员 / 系统管理员（权限范围内）可导入；**协同人不可（严格只读）**。

### 9.8 异常处理

引用 §29.1–§29.3。模块特有：无数据时"暂无线索" + 引导链接【前往公海池查看可分配线索】。

---

## 10. 线索中心 · 公海池

### 10.1 模块概述

- **定位：** 线索异常回收机制，集中管理超期未跟进、离职/解绑、手动收回的线索，等待重新分配。**非默认流转节点**：绑定账号渠道生成的线索创建即归属绑定业务员（状态"待跟进"），不进入公海池。
- **关键口径：** 消息渠道绑定账号生成的线索创建即归属负责人、站点明确、不进公海池；若后续业务人员主动回收至公海，按已有公海规则处理（见 §10.7）。
- **面向角色：** 运营专员、系统管理员（可分配）；协同人（授权站点范围内**只读**，不可分配）；业务员（只读）。

### 10.2 数据权限

> 公海池为**模块特殊数据权限**（区别于 §4.2 常规粒度）：按授权站点可见；分配/标记异常按钮受 `lead-pool:assign` / `lead-pool:mark-invalid` 控制（§4.7）。下表为**预置角色默认包**；运行时以勾选为准。

| 角色（预置默认） | 可见范围 | 分配（默认） |
| :--- | :--- | :--- |
| 业务员 | 授权站点内全部公海线索（只读） | 无（默认不勾选分配按钮） |
| 运营专员 | 负责站点内全部公海线索 | 有 |
| 协同人 | 授权站点范围内全部公海线索（只读） | 无 |
| 系统管理员 | 全公司公海线索 | 有 |

> 无 `lead-pool:assign` / `lead-pool:mark-invalid` 时不展示批量分配/分配/标记异常，仅"查看详情"；默认可导出另受 `lead:export` 等控制。

### 10.3 页面结构

筛选条件栏 + 工具栏（批量分配、刷新）+ 表格区。

### 10.4 筛选条件

| 字段 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 站点（核心维度） | 下拉 | 全部可见站点 | 仅授权站点 |
| 入池时间 | 日期区间 | 本月 | 按线索**进入公海池的时间**（`poolEnteredAt`）筛选；`poolTimeStart`/`poolTimeEnd`；允许只填开始或只填结束；空值不限制 |
| 线索关键词 | 文本 | 空 | 模糊搜线索编号/联系人/企业名称 |
| 来源渠道 | 下拉多选 | 全部 | 取值见 §23.5（客户域→来源渠道） |
| 意向产品 | 下拉 | 全部 | AI 产品类目集合 |
| 状态 | 下拉 | 全部 | 公海内取值：待分配/无效/丢失（定义见 §26.1）|
| 阶段 | 下拉 | 全部 | 取后台完整阶段列表 |
| 负责人 | 下拉 | 全部 | 权限范围内成员（待分配线索负责人为空）|
| 标签 | 下拉多选 | 全部 | 多选逻辑=包含任一（OR）|

> 公海池按站点分别管理，不同站点公海线索隔离。

> **入池时间与创建时间口径区别：** 入池时间=线索进入公海池的时间（`poolEnteredAt`）；创建时间=线索本身创建时间（`createdAt`）。**本页只保留入池时间筛选，不与创建时间混用**；如需按线索创建时间筛选，请在线索列表（§9.4）查看。重置时恢复公海池默认时间范围（入池时间=本月）。公海池权限模型不变。

### 10.5 表格字段

复选框、线索编号、线索联系人（缺失"匿名联系人"）、来源渠道、站点、线索状态（公海内展示待分配/无效/丢失，定义见 §26.1）、入池时间、入池原因（MVP：运营专员手动回收；**【二期/延后】** 超期回收、离职·解绑回收）、意向产品、操作（分配、标记异常、查看详情）。分页：更新时间倒序，20 条/页，20/50/100。

### 10.6 操作按钮

#### 10.6.1 操作

| 操作 | 前置条件 | 结果 |
| :--- | :--- | :--- |
| 分配（单/批量） | 状态="待分配" | 见 10.6.3 分配逻辑 |
| 标记异常 | 非终态 | 标记无效/丢失，见 13.7；标记后仍保留在公海池列表，状态展示对应终态 |
| 查看详情 | 无条件 | **新开页签**进入线索详情（§11） |

#### 10.6.2 分配弹窗字段

单个（线索编号、线索联系人、站点[只读]、目标负责人[下拉必填]、分配备注[可选]）；批量（已选线索数、目标负责人、分配备注）。目标负责人可选该线索站点下状态"可用"的销售人员。

#### 10.6.3 分配逻辑
1. 校验目标负责人"可用"（停用阻止，提示"该负责人已不可用，请重新选择"）。
2. 校验线索仍在公海池（并发已分配则"该线索已被其他操作分配，请刷新后重试"）。
3. 成功后：负责人更新为目标人 → 状态"待分配"→"待跟进" → 移出公海池 → 生成跟进记录（备注"由[操作人]从公海分配至[负责人]"）。
4. 无可用销售人员：下拉为空，提示"该站点暂无可用销售人员，请先在用户管理中配置"。
5. 批量部分失败：弹窗汇总成功/失败数量及原因。

### 10.7 公海回收规则

#### 10.7.1 回收来源

> **MVP 仅保留「运营专员手动回收」**；`【二期/延后】` 自动回收（系统参数天数 + 站点回收规则 + 定时任务）延后。

| 优先级 | 来源 | 说明 |
| :--- | :--- | :--- |
| 1 | 系统参数「未跟进自动回收天数」**【二期/延后】** | 二期：系统级，对所有站点强制生效，站点配置不可覆盖 |
| 2 | 站点配置「公海规则」**【二期/延后】** | 二期：仅系统参数未配置时生效 |
| 3 | 运营专员手动回收（**MVP**） | 兜底，随时可执行；MVP 唯一回收来源 |

#### 10.7.2 回收状态范围与固定规则

- **手动回收状态范围（MVP）：** 除"已成交"外全部可手动回收（待跟进/跟进中/已转客户/无效/丢失）；仅"已成交"排除（成交已闭环，不再回收公海）。
- **【二期/延后】自动回收状态范围：** 仅"待跟进""跟进中"（自动回收为"超时未跟进"驱动，仅对活跃跟进状态有意义）；已转客户、已成交、无效、丢失不自动回收。
- **系统固定规则：** 超时计算基准=最近跟进时间（自动回收口径，二期）；回收后不允许再次分配给同一负责人。
- **回收动作（负责人清空）：** 线索进入公海执行 `owner = null`（清空负责人），**保留历史跟进记录与历史操作记录**；重新分配后由分配动作重新生成负责人（见 §10.6.3）。
- **【二期/延后】自动回收执行：** 定时任务每日一次（如凌晨 2:00）；仅扫描启用站点（停用站点自动回收暂停）；线索状态待跟进/跟进中且最近跟进时间超阈值；回收后生成跟进记录（备注"系统自动回收至公海"）。

### 10.8 异常处理

引用 §29.1、§29.3、§29.5（并发冲突）。模块特有：无数据"公海池暂无线索"；分配相关校验与失败提示（目标负责人停用、并发已分配、批量部分失败、站点无销售人员）见 10.6.3。

---

## 11. 线索详情页

### 11.1 模块概述

- **定位：** 展示单条线索完整信息，跟进/状态流转核心操作页；线索列表与公海池共用。
- **打开方式：** **新开页签**（工作区多页签）；不占侧边菜单。从列表、公海、消息「查看线索」等入口跳转时新开或激活已有同线索详情页签（同线索重复打开复用同一页签）。
- **面向角色：** 业务员、运营专员、协同人、系统管理员（权限同 9.2）。

### 11.2 页面结构

顶部基础信息区 + 操作按钮区 + 标签页内容区（跟进日志 / 关联合同 / 客户画像）。

### 11.3 基础信息字段

线索编号（只读）、询盘联系人、邮箱（无则"-"）、WhatsApp（无则"-"）、电话（无则"-"）、意向产品（无则"-"）、标签（逗号分隔）、关注点（逗号分隔，无则"-"）、备注（无则"-"）、状态阶段（格式"状态·阶段"，如"跟进中·谈判阶段"，无阶段"-"）、负责人（姓名+头像）、站点（数据归属与权限依据，只读）、来源渠道、创建时间、最近跟进（无则"-"）、下次跟进（无则"-"）。

### 11.4 操作按钮区

详情顶部**仅保留 3 个按钮**（顺序固定）：**录入跟进**、**查看关联消息**、**转客户**。不提供编辑、打标签、标记异常、变更负责人、回收至公海等入口（上述操作仅在线索列表 / 公海池，见 §13.1.1）。行为见第 13 章。**协同人仅只读查看详情（默认可导出），录入跟进 / 转客户不展示或置灰；查看关联消息可按消息只读权限进入。**

### 11.5 标签页内容

#### 11.5.1 跟进日志

时间轴/表格展示本线索全生命周期记录（创建、状态变更、跟进记录），默认按跟进时间倒序，支持列排序（跟进时间、下次跟进时间）与分页（20 条/页）；记录不可改不可删（BR-031）；点击【录入跟进】弹窗；新增记录同步全局跟进日志。**展示字段与 §13.4 录入跟进对齐**（跟进时间、跟进人、跟进方式、跟进内容、客户关注、当前阶段、下次跟进时间、跟进附件；附件可下载/预览）。全局跟进日志页字段补齐**本期暂缓**，仍按 §12.5。

#### 11.5.2 关联合同

合同编号（跳合同详情）、合同名称、金额（¥）、状态（已签约/失效）、签约时间；空显"暂无关联合同"。

#### 11.5.3 客户画像

展示本线索关联**询盘联系人资料**上的客户画像（统一含：企业工商信息、过往参展信息、企业风险提示；口径见 §7.9）。数据在消息 AI 分析阶段生成并在生成线索时带入可见；线索侧**引用展示**，不另存可被消息覆盖的独立画像副本。手动创建且无 AI 画像时显"暂无客户画像数据，该线索为手动创建，暂无 AI 提取的企业信息"。**允许人工编辑**（入口：线索列表【编辑】，与业务资料同表单；详情页签只读展示，见 §13.3）；编辑后以人工为准，AI 不覆盖、有存量不重复生成（BR-017、BR-016c）。转客户 / 关联已有客户时同步规则见 §13.6、BR-036。

### 11.6 异常处理

引用 §29.3。模块特有：线索不存在时"线索不存在或已被删除" + 返回列表。

---

## 12. 线索中心 · 跟进日志

### 12.1 模块概述

- **入口：** 全局视图（线索列表顶部【跟进日志】→ 新开页签）；单线索视图（线索详情页内联）。**全系统仅此一套全局入口，不单列菜单项。**两视图数据互通。
- **功能（MVP）：** 全局视图以**线索侧**展示权限范围内跟进记录（可带出关联客户名）；单线索视图追溯单条线索过程。
- **【二期/延后】** 全局视图升级为线索/客户/合同三类对象统一抽象（对象类型筛选与表头）；顶部统计栏一并延后。
- **面向角色：** 全部角色。

### 12.2 数据权限

数据范围遵循 §4.2，按对象负责人判定；**协同人仅只读查看跟进记录，不可录入跟进**。打开页签按角色注入数据范围，顶部展示"当前显示：本人/负责站点/授权站点/全部"，不允许手动切换更广范围。

### 12.3 页面结构

页签标题 + 筛选条件栏 + 表格区。

**【二期/延后】统计信息栏：** 跟进日志总数、今日跟进数、今日计划跟进数（下次跟进时间=今日，仅数字）。MVP 不展示。

### 12.4 筛选条件

| 字段 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 跟进时间 | 日期区间 | 本月 | 按**实际录入跟进的时间**（`createdAt`）筛选；`followUpTimeStart`/`followUpTimeEnd`；允许只填开始或只填结束；空值不限制 |
| 下次跟进时间 | 日期区间 | 不限制 | 按**后续计划跟进时间**（`nextFollowAt`）筛选；`nextFollowUpTimeStart`/`nextFollowUpTimeEnd`；空值不限制 |
| 线索编号 | 文本 | 空 | MVP：按关联线索编号筛选 |
| 客户 | 文本 | 空 | MVP：按关联客户名称模糊筛选 |
| 跟进人（业务员） | 下拉 | 全部（主管）/本人（业务员） | — |
| 跟进方式 | 下拉 | 全部（取值见 §23.5 线索域→跟进方式） | — |
| 跟进状态（阶段） | 下拉 | 全部 | MVP 单选；**【二期/延后】** 多选 |
| 对象类型 | 下拉 | — | **【二期/延后】** 线索/客户/合同 |
| 对象关键词 | 文本 | — | **【二期/延后】** |

> **跟进时间与下次跟进时间不能混用**：跟进时间为实际跟进发生时间，下次跟进时间为计划时间；两字段独立筛选、各自携带开始/结束参数。重置时恢复默认（跟进时间=本月，下次跟进时间=不限制）。

### 12.5 表格字段

**MVP：** 跟进时间（倒序）、跟进人、关联线索编号（跳详情）、关联客户、跟进方式、跟进内容、当前跟进状态、下次跟进时间。分页 20 条/页，20/50/100。

**【二期/延后】完整对象表头：** 对象类型（图标+文字）、对象编号、对象名称、阶段、操作（详情弹窗）；以及跟进内容截断/悬停展开等增强。

> **与线索详情跟进日志差异：** 详情内跟进日志字段对齐 §13.4（含客户关注、附件），见 §11.5.1；**全局跟进日志页补齐客户关注/附件本期暂缓**。

### 12.6 业务规则

跟进记录仅新增，不修改历史、不删除（BR-031）；每次关键沟通或状态变化必须新增跟进记录（BR-030）。

### 12.7 异常处理

引用 §29.1、§29.3。模块特有：无记录"暂无跟进记录"、搜索无结果"未找到匹配的跟进记录"；导出上限 10000 条。

---

## 13. 线索操作

### 13.1 入口与权限矩阵

#### 13.1.1 入口矩阵

| 操作 | 列表顶部 | 列表操作列 | 列表更多 | 详情顶部 | 公海池 |
| :--- | :---: | :---: | :---: | :---: | :---: |
| 跟进日志 | ✓（新开页签） | — | — | — | — |
| 转客户 | ✓(批量，选中≥1) | ✓ | ✓ | ✓ | — |
| 新增线索 | ✓ | — | — | — | — |
| 批量导入 | ✓ | — | — | — | — |
| 回收至公海 | ✓(选中≥1) | ✓ | — | — | — |
| 批量导出 | ✓ | — | — | — | — |
| 编辑 | — | ✓ | — | — | — |
| 录入跟进 | — | ✓ | — | ✓ | — |
| 打标签 | — | ✓ | — | — | — |
| 标记异常 | — | — | ✓ | — | ✓ |
| 变更负责人 | — | — | ✓ | — | — |
| 查看关联消息 | — | — | ✓ | ✓ | — |
| 分配 | — | — | — | — | ✓(待分配) |

> **列表顶部口径：** 与 §9.6.1 一致，仅「跟进日志 / 转客户 / 新增线索 / 批量导入 / 回收至公海 / 批量导出」六项；录入跟进、打标签仅在操作列（及详情顶部的录入跟进）。
> **详情页口径：** 顶部操作**仅**「录入跟进 / 查看关联消息 / 转客户」三项；其余操作一律不在详情露出。

#### 13.1.2 权限矩阵

> **运行时权威：** 按钮是否展示/可点 = 用户角色是否勾选对应权限码（§4.6、§4.7）；数据范围另遵循 §4.2。下表为**预置四角色默认权限包**（初始化默认勾选），并给出权限码映射。自定义角色以实际勾选为准。  
> **预置协同人默认包：** 不勾选写类按钮；默认可导出与查看关联消息（只读进消息）。

| 操作 | 权限码 | 业务员 | 运营专员 | 协同人 | 系统管理员 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 新增线索 | `lead:add` | 仅本人 | 权限范围内 | ✗ | 全部 |
| 编辑 | `lead:edit` | 负责人本人 | 权限范围内 | ✗ | 全部 |
| 打标签 | `lead:tag` | 负责人本人 | 权限范围内 | ✗ | 全部 |
| 录入跟进 | `lead:follow` | 负责人本人 | 权限范围内 | ✗ | 全部 |
| 转客户（单条） | `lead:convert` | 负责人本人 | 权限范围内 | ✗ | 全部 |
| 转客户（批量） | `lead:convert-batch` | ✗ | ✓ | ✗ | ✓ |
| 标记异常（列表） | `lead:mark-invalid` | 负责人本人 | 权限范围内 | ✗ | 全部 |
| 标记异常（公海池） | `lead-pool:mark-invalid` | ✗ | ✓ | ✗ | ✓ |
| 变更负责人 | `lead:change-owner` | ✗ | 权限范围内 | ✗ | 全部 |
| 查看关联消息 | `lead:view-related-msg` | 负责人本人 | 权限范围内 | ✓（消息内只读，§7.3） | 全部 |
| 批量导出 | `lead:export` | ✓ | ✓ | ✓ | ✓ |
| 批量导入 | `lead:import` | 仅本人 | 权限范围内 | ✗ | 全部 |
| 回收至公海 | `lead:recycle` | ✗ | ✓ | ✗ | ✓ |
| 公海分配 | `lead-pool:assign` | ✗ | ✓ | ✗ | ✓ |

### 13.2 新增线索

> **定位：人工创建线索为异常兜底入口**（主流程为 AI 自动识别有效询盘并自动生成线索，见 §21.8、§27.1），仅用于 AI 服务不可用、AI 无法识别、历史数据补录、特殊业务场景。  
> **字段权威：** 下表为人工建线索**唯一字段集**；邮件/WhatsApp【生成线索（人工兜底）】与本表一致，渠道差异仅见 §13.2.1（默认值、WA 电话只读与预填策略）。

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 企业名称（所属企业） | 文本/选择已有客户 | 否 | 可输入新名或关联已有客户；**不等同于客户名称**；未识别时可空，后续转客户可用询盘联系人姓名生成客户名称 |
| 询盘联系人 | 文本 | 是 | 转客户时若无企业名称，可作为客户名称兜底来源 |
| 邮箱 | 文本 | 条件 | 与电话、WhatsApp **三者至少一个**必填 |
| 电话 | 文本 | 条件 | 与邮箱、WhatsApp **三者至少一个**必填 |
| WhatsApp | 文本 | 条件 | 与邮箱、电话 **三者至少一个**必填 |
| 站点 | 下拉 | 是 | 授权站点 |
| 来源渠道 | 下拉 | 否 | 取值见 §23.5（客户域→来源渠道） |
| 负责人 | 下拉 | 是 | 默认当前用户；运营专员/系统管理员可选权限内业务员 |
| 意向产品 | 下拉/文本 | 否 | — |
| 线索标签 | 多选/输入 | 否 | 人工标签；选项见字典（线索域→线索手动标签）；消息入口可将 AI 询盘标签作候选（见 §25） |
| 关注点 | 多选下拉 | 否 | 字典：客户域→客户关注选项；写入线索当前关注点集合 |
| 备注 | 多行文本 | 否 | — |

保存规则：询盘联系人必填、邮箱/电话/WhatsApp 至少一个；企业名称（所属企业）可选；选已有客户则关联（按站点+客户名称匹配，见 §2.4），输入新名仅作线索所属企业名（客户资产以转客户为准）；**匹配/选择到已有客户不阻止线索创建，且不创建重复客户（转客户时按 BR-034 关联已有客户）**；手动新增线索须选择站点（`site_id`）；成功后生成线索编号，状态默认"待跟进"；**录入方式**系统写入 `manual_create`（用户不可见/不可改，§23.5.7、BR-057）。

#### 13.2.1 消息人工生成线索（邮件 / WhatsApp，与新增线索同字段）

> 适用于沟通中心邮件【生成线索（人工兜底）】、WhatsApp【生成线索（人工兜底）】。**弹窗字段 = §13.2 全表**，不另做精简字段集。  
> **原则：** 与线索列表新增/编辑一致，**不锁死来源等业务字段**（消息通道 ≠ 获客来源；例如 WA 会话可将来源改为官网询盘）。

**默认值与编辑性：**

| 字段 | 邮件人工生成 | WhatsApp 人工生成 |
| :--- | :--- | :--- |
| 站点 | **默认** = 当前邮件所属邮箱对应站点，**可改**（权限范围内） | **默认** = 业务员唯一授权站点，**可改**（权限范围内） |
| 来源渠道 | **默认** = 邮件，**可改** | **默认** = WhatsApp，**可改** |
| 录入方式 | 系统写入 `inquiry_conversion`，不可改 | 同左 |
| 电话 | 可编辑（有则预填；AI 失败不预填） | **只读** = WhatsApp 账号号码（计入电话，可满足「邮箱/电话/WhatsApp 至少一个」）；不改会话身份号 |
| WhatsApp | 可编辑（有则预填） | **可预填**为会话号（与只读电话同源时可同值）；计入联系方式校验 |
| 关注点 | 可编辑（默认空或按 AI/人工预填） | 同左 |
| 负责人 | **默认** = 当前操作人，**可改**（权限范围内） | **默认** = 当前操作业务员，**可改**（权限范围内） |

**企业名称 / 已有客户（已确认）：**

- **识别为新客户（未命中已有客户）：** 有企业/主体名称则预填到「企业名称（所属企业）」文本；无则空。
- **命中已有客户：** 带入该**已有客户名称**（选中已有客户）；业务可编辑或改选其他客户。
- 无论预填与否，均保留「输入新名 / 选择已有客户」能力。

**预填总规则：**

| 条件 | 预填策略 |
| :--- | :--- |
| AI **成功**且字段有值 | 对应业务字段预填，均可改（WA 电话只读除外） |
| AI **不可用 / 失败**（方案乙） | **业务字段全部不预填**（含发件邮箱、联系人、企业名、意向产品、标签、备注等），由用户手填 |
| 站点 / 来源渠道 / 负责人 | **始终默认带入**（便于提交），**允许修改**；不属于「AI 预填」 |
| WhatsApp 电话 | **始终带入且只读**（会话身份） |

校验与保存：同 §13.2（询盘联系人必填、邮箱/电话/WhatsApp 至少一个；WhatsApp 会话场景下只读电话可满足电话侧）。成功后线索关联当前消息/会话，状态默认"待跟进"；站点/负责人/来源以表单最终提交值为准。

### 13.3 编辑

> **入口：** 线索列表操作列；**详情页不提供编辑入口**（详情仅展示）。  
> **原则：** 编辑字段与详情基础信息中的**业务资料**对齐；系统受控字段只读或不进表单。

| 可编辑 | 只读 / 不进编辑表单 |
| :--- | :--- |
| 企业名称（所属企业）、询盘联系人、邮箱、电话、WhatsApp、来源渠道、意向产品、线索标签、关注点（可增可删）、备注、负责人、**客户画像**（企业工商信息 / 过往参展信息 / 企业风险提示，与详情页签口径一致，§7.9） | 线索编号、站点、线索状态、跟进阶段、创建时间、最近跟进、下次跟进；跟进记录与关联合同不可在本弹窗改 |

- **客户画像：** 编辑写入**询盘联系人侧**画像（线索侧引用展示，不另存可被消息覆盖的副本）；人工编辑后以人工为准，后续 AI **不覆盖**（BR-017、BR-016c）。无存量画像时可在编辑中首次补录。
- **负责人：** 可在编辑中修改；与列表「更多 → 变更负责人」**两入口并存**，共用权限与可选人范围。若负责人发生变化，**须填写变更说明**，并生成跟进记录（备注文案同变更负责人：`变更负责人：[说明]`）。
- **关注点：** 编辑可增可删；与录入跟进的「追加去重」规则并存（见 §13.4）。
- **校验：** 询盘联系人非空；邮箱/电话/WhatsApp 至少一个；站点不在本表单修改故不作站点空校验。
- **成功后：** 更新信息（所属企业变更不自动重匹配客户）+ 提示"线索信息已更新" + 对变更字段生成跟进记录（备注"[字段]由[原值]变更为[新值]"，仅记变更字段；负责人变更按上条专属文案；客户画像变更记"更新了客户画像"）。**手动修改字段后 AI 不再覆盖（BR-017）。**
- **线索状态：** 不在编辑中修改；仅通过录入跟进 / 转客户 / 合同联动 / 标记异常 / 公海回收与分配等动作变更。
### 13.4 录入跟进

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 线索编号 | 只读 | — | 编号 + 客户名，如"LEAD-2026-0911 · Bella Home Deco" |
| 跟进方式 | 下拉 | 是 | 取值见 §23.5（线索域→跟进方式） |
| 跟进内容 | 多行文本 | 是 | 建议≥10 字（非强制） |
| 客户关注 | 多选下拉 | 否 | 字典：客户域→客户关注选项（§23.5，含展示分类仅 UI 分组）。保存时**合并追加**至线索当前关注点（同名去重、不覆盖、不删除已有项）；当次跟进记录亦保存所选关注点 |
| 当前阶段 | 下拉 | 条件必填 | 字典配置（线索域→跟进阶段），未配置提示"暂无跟进阶段配置，请联系系统管理员设置" |
| 下次跟进时间 | 日期 | 否 | **默认空**，可不填；填写后用于跟进超时统计（仅「已填且 < 今日」计入），与待回复无关 |
| 跟进附件 | 上传 | 否 | jpg/png/PDF/doc/docx/xls/xlsx；单个≤10MB，建议≤5 个 |

**阶段与状态联动**（状态流转结果以 §26.1 线索状态机为准）：

| 条件 | 系统动作 |
| :--- | :--- |
| 首次录入跟进（待跟进） | 状态→跟进中（BR-038a） |
| 跟进中继续录入（含进入允许转化的阶段） | 保持跟进中，**不自动创建客户**；是否转化由人工主动点击【转客户】（BR-038b） |
| 已转客户继续录入 | 保持已转客户 |
| 终态（已成交/无效/丢失） | 仅追加记录，不变更状态 |

> **阶段仅决定"是否允许转化"，绝不自动转化。** 是否允许触发【转客户】由字典·跟进阶段扩展属性「是否允许转客户」配置；高意向统计读「是否计入高意向」（与转客户属性分立）。**跟进阶段取值与扩展属性默认以字典 §23.5.7 为准，本模块不重复定义固定枚举**（见 §13.6、§23.5、BR-038b）。校验：跟进方式非空、跟进内容非空、当前阶段条件必填、附件格式/大小。成功后刷新记录、更新最近跟进时间。

### 13.5 打标签

> **入口：** 线索列表操作列 / 顶部批量；**详情页不提供打标签入口**。

- 单条弹窗：线索编号、当前标签（带删除）、添加标签（输入回车新增）、热门标签（当前站点 Top10，点击添加，见 25.2）。
- 批量弹窗：已选线索数、添加标签、热门标签；**仅批量新增，不支持删除**。
- 交互：中英文/数字/下划线，≤20 字；同一线索不可重复（"该标签已存在"）；单条删除悬停×二次确认。手动标签选项由字典（线索域→线索手动标签）维护，新输入同步写回字典。AI 来源、人工来源标签均可增删改。
- **AI 与人工规则：** 线索标签含 AI（询盘标签）与人工两类来源；追加/去重/禁止恢复已删同名标签等规则以 §25.2 为准（BR-016a、BR-017）。

### 13.6 转客户

- **定位：** 业务人员**人工主动**将线索沉淀为客户资产的业务动作（线索 → 客户 + 客户联系人 + 保留原线索），帮助业务人员快速完成"线索 → 客户资产"转换；与客户中心「变更负责人」完全隔离，不复用逻辑、不共用名称。
- **转客户规则（准入）：** 是否允许触发由**跟进阶段扩展属性「是否允许转客户」**决定（见 §23.5）；**仅"允许触发"，绝不自动转化**，最终是否转化由人工主动执行。**跟进阶段取值与扩展属性默认以字典 §23.5.7 为准（本模块不重复定义固定枚举）**。线索状态准入=待跟进/跟进中/无效/丢失（"已成交""已转客户""待分配"不支持；公海池"待分配"仅分配/标记异常）。单条：负责人本人/运营专员/系统管理员；批量：运营专员/系统管理员。**协同人只读，不可转客户。**
- **执行流程（点击【转客户】后）：**
  1. **生成客户名称并创建/关联客户：** 客户站点=线索站点。**客户名称生成规则：** ① 若线索已识别到企业名称或个体工商户名称（所属企业/企业名称有值），则客户名称取该企业名称；② 若未识别到企业名称，仅存在个人询盘联系人，则客户名称取询盘联系人姓名；③ 若企业名称与询盘联系人姓名**均为空**，则**不允许转客户**，需用户人工补充客户名称后重试。按生成后的**客户名称**在当前线索站点内**精确匹配**已有客户（命中则关联已有客户；未命中则创建新客户；历史多条时关联最近创建的一条，不弹窗；BR-034）。MVP 不强制区分企业 / 个体工商户 / 个人，不设置客户类型字段，不提供筛选或统计（见 §30.6）。
  2. **创建客户联系人：** 将询盘联系人转换/归集为该客户下的客户联系人（归集去重见 BR-035、§27.3）；若客户名称来源于询盘联系人姓名，该联系人仍作为客户联系人保存，不额外创建独立个人客户对象。
  3. **关联原线索：** 保留原线索并挂接到客户（客户 ├ 客户联系人 └ 历史线索）。
  4. **修改线索状态：** 跟进中 → 已转客户（**不再直接置"已成交"**；"已成交"由后续录入已签约合同并关联线索驱动，见 §26.1、BR-056）。
  5. **同步客户画像等 AI 信息**至客户（见 §7.9、§21.4、BR-036）：客户侧**尚无**客户画像记录时，将询盘联系人侧客户画像写入客户；客户侧**已有**客户画像记录时整包不碰、绝不覆盖；同时按 BR-036 将工商信息中的**国家/地区、所属行业**预填到客户标准字段（新建必预填、关联仅补空、字典匹配不上留空，可人工改）；意向总结/意向产品/线索标签不同步；无企业信息时工商信息可显"-" → 系统通知负责人。
- **系统记录（非跟进记录）：** **不强制填写跟进记录**；系统自动记录**客户转化事件**（示例："线索已转化为客户"｜操作人｜时间｜生成客户），属**系统操作记录**（写入操作日志 §23.9），不计入销售跟进记录。
- **结果：** 生成/关联客户 + 客户联系人 + 保留并关联原线索 + 线索状态→"已转客户"（状态流转见 §26.1）。客户名称校验通过后转客户为必然成功动作；客户名称为空时拦截并提示补充；其余异常按系统异常处理（BR-033）。
- **线索生命周期（方案 B）：** 转客户后**线索继续存在并作为业务跟进对象**，可查看历史信息、新增跟进记录、继续维护业务沟通；**系统不自动删除或关闭线索**（不存在"转客户后线索生命周期结束"）。
- **转客户后规则边界：** 允许——查看历史信息 / 查看 AI 分析结果 / 新增跟进记录 / 继续记录客户沟通；禁止——**状态回退（已转客户→跟进中/待跟进）** / **返回正常跟进准入链路** / **再次转客户（已转客户不再作为转化准入状态）**。（注：已转客户仍可经公海回收机制进入待分配，属异常回收，见 §10.7、§26.1，非跟进链路回退。）

### 13.7 标记异常

- **入口：** 线索列表更多、公海池操作列；**详情页不提供**。终态线索锁定，入口置灰。
- **字段：** 当前状态（只读）、异常类型（无效/丢失）、异常备注（必填）。
- **结果：** 状态变更为对应终态 + 生成跟进记录（备注"标记异常：[类型]-[备注]"）；公海池中标记后仍保留列表展示，不再支持分配。线索不物理删除（BR-037）。

### 13.8 变更负责人

- **定位：** 单条线索负责人 A→B；仅变更本条，不影响该客户其他历史线索及后续新线索归属。与公海分配不同（分配针对无负责人公海线索）。**站点主邮箱【转派给业务员】在投递邮件的同时按 T1–T3 处理负责人**（一步完成，BR-053；见 §7.5.2）；未关联线索也可转派（仅投递）；转派不进公海。
- **入口：** 仅线索列表更多；**详情页不提供**。
- **字段：** 线索编号（只读）、客户名称（只读）、分配状态（只读）、站点（只读）、当前负责人（只读）、新负责人（下拉必填，该线索站点销售人员，停用不展示）、变更备注（必填）。
- **校验：** 新负责人非空"请选择新负责人"；不可与当前相同"新负责人与当前负责人相同，无需变更"；备注非空。并发已变更"线索负责人已被其他操作变更，请刷新后重试"。
- **结果：** 更新负责人 + 状态不变 + 生成跟进记录（备注"由[原负责人]变更至[新负责人]"）+ 提示"线索负责人已变更为[新负责人]"（BR-025/BR-028）。

**负责人变更规则：**

| 场景 | 是否允许 | 说明 |
| :--- | :---: | :--- |
| 运营专员将自己负责的站点主邮箱线索转派给业务员 | 允许 | 负责人由运营变更为目标业务员 |
| 运营专员调整负责站点内线索负责人 | 允许 | 仅限运营负责站点范围内 |
| 运营专员转派**已关联线索**且负责人为业务员 A 的邮件 | 目标锁定为 A | 不允许换成其他人（转派 T2）；无线索转派不改动旧线索负责人（T3） |
| 业务员主动修改线索负责人 | 不允许 | 业务员仅维护本人负责数据 |
| 协同人修改负责人 | 不允许 | 协同人严格只读 |
| 系统管理员修改负责人 | 允许 | 全局权限 |

### 13.9 查看关联消息

> 线索可关联全部消息（含邮件与 WhatsApp）。入口文案统一为「**查看关联消息**」（详情顶部三按钮之一；列表更多同入口）。按线索已关联渠道跳转沟通中心对应页。

- **跳转规则：** 仅关联邮件 → 沟通中心·邮件（按线索关联消息过滤）；仅关联 WhatsApp → 沟通中心·WhatsApp（定位会话）；两者皆有 → **优先按来源渠道**（邮件→邮件页，WhatsApp→WhatsApp 页），同页内仍可再切另一渠道；皆无 → Toast「该线索暂无关联消息」。
- **关联邮件消息：** 顶部显"线索：[编号]-[名称]的邮件往来"，提供返回；无关联邮件显"该线索暂无关联邮件"；记录不存在提示"关联消息记录不存在或已被清理"（BR-022 一线索可关联多消息）。
- **关联 WhatsApp 消息：** 按会话 ID 定位并刷新详情；无会话显"该线索暂无关联 WhatsApp 会话"；不存在提示"关联的 WhatsApp 会话记录不存在或已被清理"。

---

## 14. 客户中心 · 客户列表

### 14.1 模块概述

- **定位：** 企业客户资产集中管理页面。**客户不设独立"客户状态"字段**；经营情况由**客户潜质分级**反映（字典维护、人工调整，见 §26.3、§23.5）。
- **面向角色：** 业务员、运营专员、协同人、系统管理员。

### 14.2 数据权限

数据范围遵循 §4.2；客户站点须在授权站点内。按钮受 `customer:*` 权限码控制（§4.7）；下表预置协同人默认不勾选写按钮、默认可 `customer:export`。**运行时以角色实际勾选为准**，禁止按角色名写死。

### 14.3 页面结构

筛选条件栏 + 表格区（操作 + 分页）。

### 14.4 筛选条件

| 字段 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 客户关键词 | 文本 | 空（模糊搜客户名/编号） |
| 国家/地区 | 下拉 | 全部 |
| 行业 | 下拉 | 全部 |
| 客户潜质分级 | 下拉 | 全部 |
| 负责人 | 下拉 | 全部 |
| 站点 | 下拉 | 全部 |
| 创建时间 | 日期区间 | 不限制 | 按客户创建时间；`createTimeStart`/`createTimeEnd`；允许只填开始或只填结束；空值不限制 |
| 最近跟进时间 | 日期区间 | 不限制 | 按客户最近一次关联跟进时间（取该客户名下线索/客户跟进记录最近一条）；`lastFollowUpTimeStart`/`lastFollowUpTimeEnd`；空值不限制 |

操作按钮：查询、重置、高级筛选。

> **口径说明：** 客户必须有站点，不需要站点状态时间类扩展；创建时间与最近跟进时间独立筛选、不混用；重置时恢复默认（均为不限制）。**最近跟进时间筛选若后端暂未支持，前端先保留控件和参数并标注"后端待支持"，不影响其他筛选。**

### 14.5 表格字段

| 字段 | 展示规则 | 点击 |
| :--- | :--- | :--- |
| 复选框 | 最左侧 | — |
| 客户名称+编号 | **客户名称** + CUS-YYYY-XXXX（企业/个体工商户取企业名称，个人取询盘联系人姓名；与列表「客户名称」口径一致，见 §2.4） | 跳客户详情 |
| 站点 | 客户仅属一个站点（创建时指定，数据归属与权限依据） | — |
| 国家/地区 | 国家名 | — |
| 行业 | 行业名 | — |
| 客户潜质分级 | 字典下拉（客户域→客户潜质分级，§23.5）；**仅人工维护**，新建未选默认「潜在客户」（`potential`）；系统不随线索/合同自动变更（见 §26.3） | — |
| 负责人 | 负责人姓名 | — |
| 客户标签 | 仅人工标签，超 3 个"+N" | — |
| 关联线索数 | 数字 | 跳线索列表（按客户 ID 过滤） |
| 关联合同数 | 数字 | 跳合同列表（按客户 ID 过滤） |
| 最近签约合同 | 编号（签约时间），取状态=已签约最近一份（口径见 27.7） | — |
| 操作 | 详情、编辑；更多：变更负责人、**添加联系人** | 见 14.7 |

列表顶部提供【批量导入】【导出】（CSV，按当前筛选/选中，见 §14.7.4、§14.7.5）。分页：创建时间倒序，20 条/页，20/50/100。

### 14.6 客户创建路径

| 路径 | 触发 | 表现 |
| :--- | :--- | :--- |
| 手动新建 | 客户列表【新建客户】 | 见 14.7.1 |
| 线索转客户 | 人工主动执行【转客户】时创建/关联 | 客户档案生成、客户联系人归集（BR-035）、保留并关联原线索、负责人继承、AI 信息按 BR-036 同步、线索状态→已转客户 |

### 14.7 客户操作

#### 14.7.1 新建客户

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 客户名称 | 文本 | 是 | 客户主体展示名称；企业/个体工商户填企业名称，个人填联系人姓名（见 §2.4 客户名称口径） |
| 国家/地区 | 下拉 | 否 | 字典 §23.5；转客户时可从工商画像预填（BR-036），支持【编辑】修改 |
| 行业 | 下拉 | 否 | 字典 §23.5（表单项文案可用「所属行业」）；转客户时可从工商画像预填（BR-036），支持【编辑】修改 |
| 客户潜质分级 | 下拉 | 否 | 取值字典 §23.5；未选默认「潜在客户」；**仅人工选择，系统不自动赋值变更** |
| 客户标签 | 下拉多选 | 否 | 仅人工；选项由字典（客户域→客户标签）维护（§25.3、BR-016b）；当前已选可×移除；已添加项在选择列表中置灰；字典无选项提示"暂无可选标签，请联系系统管理员在字典管理中维护" |
| 站点 | 下拉 | 是 | 数据归属与权限依据 |
| 关联已有线索 | 下拉搜索 | 否 | 该站点下未关联客户、且状态≠无效/丢失的线索，防同名拆分 |
| 客户联系人姓名/职位/邮箱/电话 | 文本 | 否 | 写入客户联系人 |

提交：校验客户名称/站点非空；**MVP 客户唯一性 =（站点 + 客户名称）**；同一站点内客户名称相同视为同一客户，校验"该客户已存在，请勿重复创建"；不同站点即使客户名称相同也允许各自建档；客户名称按统一规则生成（企业/个体工商户取企业名称，个人取询盘联系人姓名）；精确匹配、不做模糊匹配；创建后当前业务员成为负责人；关联线索按 14.8（一）规则；生成跟进记录（备注"新建客户"）。

#### 14.7.2 编辑客户资料

客户名称、站点只读；国家/行业/**客户潜质分级**/客户标签可编辑（潜质分级取值字典、仅人工改；标签仅人工，选项与交互同【新建客户】：当前标签带×移除、下拉多选添加、已添加置灰；字典无选项提示同新建）。**列表不提供独立【打标签】弹窗**；客户标签仅在本弹窗与【新建客户】中维护（规则见 §25.3、BR-016b）。**本弹窗不维护客户联系人**（新增见 §14.7.2a）。编辑保存生成跟进记录（备注统一为"更新了客户信息"，不写变更前后值）。客户本身不可删除（BR-043）。

#### 14.7.2a 添加联系人

- **入口：** 客户列表操作「更多」→【添加联系人】（**不在客户详情页提供**）。
- **权限：** 与【编辑】一致——业务员（本人负责客户）、运营专员 / 系统管理员（权限范围内）；**协同人不可**。
- **字段：** 姓名（必填）、职位、邮箱、电话、WhatsApp、联系角色（字典：客户域→联系角色，§23.5；预置含决策人/采购经理/关键联系人/执行联系人/技术联系人/财务联系人/其他）、主要联系人（勾选；同客户仅一位，设新则原自动取消）。
- **规则：** 邮箱/手机去重仅同客户内提示不阻止；保存成功后 toast「联系人已新增」，**留在客户列表，不跳转详情**。

#### 14.7.3 变更负责人

- **定位：** 已有客户 A→B；后续该客户新生成线索**固定**自动分配新负责人，历史线索不变（无系统参数开关）。与转客户完全隔离。
- **字段：** 客户名称（只读）、当前负责人（只读）、新负责人（下拉必填，该客户站点启用销售人员）、变更备注（必填）。
- **校验：** 新负责人启用（停用阻止"该负责人已不可用，请重新选择"）、不可与当前相同。
- **结果：** 更新负责人 + 后续新线索归新负责人 + 历史线索不变 + 生成跟进记录（备注"客户[名称]由[原负责人]变更为[新负责人]"）。批量：逐条执行，部分成功弹窗汇总。

#### 14.7.4 客户导出

- **入口：** 客户列表顶部【导出】。
- **字段：** 导出范围（当前选中[默认]/当前筛选结果/当前页）、文件格式（只读 CSV）、导出上限（"最多 10000 条"）。
- **规则：** 导出内容为当前权限范围内客户列表字段；超 10000 条提示"数据量过大，请缩小筛选范围"；成功下载本地，失败 Toast"导出失败，请重试"。
- **权限：** 业务员（本人范围）、运营专员（负责站点）、协同人（授权站点只读，默认可导出）、系统管理员（全部）均可导出；记录操作日志（操作类型=导出，见 §23.9、BR-054）。

#### 14.7.5 客户导入

- **入口：** 客户列表顶部【批量导入】；先【下载导入模板】。**导入模板字段与客户列表/新建客户字段一致**（客户名称、国家/地区、行业、客户潜质分级、客户标签、站点、客户联系人姓名/职位/邮箱/电话；表头不一致整单拒绝，提示"导入模板与系统字段不一致，请下载最新模板"）。
- **流程：** 下载模板 → 填写 → 上传（Excel/CSV）→ 系统校验 → 预览（错误行标红与原因）→ 确认导入。
- **匹配与归属：** 按客户唯一性（**站点 + 客户名称**，见 §14.7.1、§30.6 唯一约束）匹配已有客户——命中则跳过并在结果中标注"已存在，未重复创建"，未命中则新建；站点须在操作人权限范围内；负责人默认导入操作人，运营专员 / 系统管理员可在模板内指定权限范围内业务员；新建客户生成跟进记录（备注"批量导入客户"）。
- **结果汇总：** "成功 N 条、跳过 M 条（已存在）、失败 K 条（可下载失败明细）"；记录操作日志（操作类型=导入，见 §23.9、BR-054）。
- **权限：** 业务员（仅本人）、运营专员 / 系统管理员（权限范围内）可导入；**协同人不可（严格只读）**。

### 14.8 异常处理

引用 §29.1、§29.3、§29.5。模块特有：无数据"暂无客户"、搜索无结果"未找到匹配的客户"；同一站点内客户名称相同"该客户已存在，请勿重复创建"（不同站点允许同名各自建档）；批量变更部分成功弹窗汇总。

---

## 15. 客户详情页

### 15.1 模块概述

- **定位：** 展示单客户完整信息的**只读**查看页；客户资产增删改入口均在客户列表 / 合同中心等，**详情页不提供任何写操作按钮**。
- **打开方式：** **新开页签**（工作区多页签）；不占侧边菜单。从客户列表、线索详情关联客户、消息「查看客户」等入口跳转时新开或激活已有同客户详情页签（同客户重复打开复用同一页签）。
- **面向角色：** 全部角色（权限同 14.2；协同人只读查看）。

### 15.2 页面结构

基础信息区 + 标签页导航（客户信息 / 线索记录 / 客户联系人 / 合作合同 / AI 客户画像）。**无顶部操作工具栏**；各标签页默认只读，**例外：AI 客户画像支持人工编辑**（§15.4.5）。

### 15.3 基础信息字段

**客户名称**（大号；客户主体展示名称：企业/个体工商户取企业名称，个人取询盘联系人姓名，见 §2.4）、客户编号（CUS-YYYY-XXXX）、关联线索数、站点、负责人、国家/地区、行业、**客户潜质分级**（字典取值、人工维护，见 §26.3）、客户标签（水平排列超出"+N"，无则"-"）。

### 15.4 标签页内容

> **只读约定：** 客户信息 / 线索记录 / 客户联系人 / 合作合同仅展示数据；不提供【关联线索】【新增/编辑/删除联系人】【新增合同】等操作；客户标签仅在列表【新建客户】/【编辑客户】中维护（写操作入口见 §14.7、合同中心 §16）。**AI 客户画像**为例外，支持人工编辑（§15.4.5）。

#### 15.4.1 客户信息

客户名称（与列表口径一致，见 §2.4）、国家/地区、行业、客户潜质分级、官网、联系方式（取主要联系人电话+邮箱，无则"-"）、负责人、创建时间。**不提供【关联线索】**（关联线索在新建客户或线索转客户流程完成）。

#### 15.4.2 线索记录

筛选（站点、线索状态）；字段：线索编号（跳详情）、站点、来源渠道、询盘时间、线索状态、负责人；默认按询盘时间倒序；空显"暂无关联线索"。

#### 15.4.3 客户联系人

只读列表：客户主体下已有联系人（来源：线索转客户/关联时由询盘联系人归集，见 §27.3；后续新增见列表【添加联系人】§14.7.2a）。字段：姓名、职位、邮箱、电话、WhatsApp、联系角色、主要联系人（⭐）。空显"暂无联系人"。**详情内不提供新增/编辑/删除。**

#### 15.4.4 合作合同

只读展示与该客户有关联的合同记录（`customer_id` 匹配）。统计（累计合同数、累计合同金额、最近签约时间、最近签约合同，统计卡仅取状态=**已签约**，口径见 27.7）；列表字段：合同编号（可跳转合同中心/合同详情查看）、合同名称、金额（¥）、签约时间、合同状态（已签约/失效，按签约时间倒序，**失效合同仍展示**）；空显"暂无合作合同"。**详情内不提供【新增合同】及合同编辑/作废等写操作**（合同增删改查在合同中心 §16）。

#### 15.4.5 客户画像

客户资产阶段的「客户画像」，结构与消息/线索侧一致，统一包含：企业工商信息、过往参展信息、企业风险提示（展示细项可含企业名称/简称/工商信息/参展次数与展会名称/风险提示等）。数据来自转客户或关联客户时按 BR-036 / §7.9 同步：客户侧**尚无**画像记录则写入询盘联系人侧画像；客户侧**已有**画像记录则**整包不碰、绝不覆盖**。企业工商信息仅在识别到企业或个体工商户时展示，个人客户无则显"-"。**详情页支持人工编辑客户画像**（本页提供编辑入口）；编辑保存后以人工为准，后续 AI **不覆盖**（BR-017、BR-016c）。年营收不写入客户标准字段；不自动带入意向总结/意向产品/线索标签。手动创建且未关联 AI 分析、或客户侧尚无结构化画像时，显"**当前客户暂无客户画像**"（可附说明：暂无企业工商信息、过往参展信息与企业风险提示）；无存量时可在本页编辑中首次补录。**禁止**用意向总结 / `aiProfile` 类业务描述文案冒充客户画像，以免与结构化画像混淆。

### 15.5 异常处理

引用 §29.1、§29.3。模块特有：客户不存在"客户不存在或已被删除"；各标签页空态对应提示；联系人已删除关联数据显"-"不阻塞。

---

## 16. 客户中心 · 合同中心

### 16.1 模块概述

- **定位：** 统一管理销售合同，记录成交信息，支撑经营分析与成交追溯。合同必须关联客户，可选关联线索。**合同模块为「合同记录管理」（方案 A）：仅管理合同基本信息、合同金额、签约日期、合同状态；不含审批流程、OA 流程、合同签署流程（非合同审批系统）。不提供合同删除入口。**
- **面向角色：** 全部角色（**协同人仅只读查看合同，默认可导出，不可新增/编辑/录入跟进/作废/删除**）。

### 16.2 数据权限

数据范围遵循 §4.2，按合同负责人及站点判定（合同不设独立站点，继承客户站点），不按合同状态细分。按钮受 `contract:*` 权限码控制（§4.7）；预置协同人默认不勾选写按钮、默认可 `contract:export`。**运行时以角色实际勾选为准**。

### 16.3 页面结构

筛选条件栏 + 表格区（操作 + 分页）。

### 16.4 筛选条件

| 字段 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 合同编号 | 文本 | 空 |
| 客户名称 | 文本 | 空（模糊搜客户名/邮箱） |
| 签约日期 | 日期区间 | 本月 | 按**合同业务发生时间**（`sign_date`）筛选；`signDateStart`/`signDateEnd`；允许只填开始或只填结束；空值不限制 |
| 创建时间 | 日期区间 | 不限制 | 按**合同录入系统时间**（`createdAt`）筛选；`createTimeStart`/`createTimeEnd`；允许只填开始或只填结束；空值不限制 |
| 合同金额 | 数字区间（最小/最大）| 不限制 | 按合同金额范围筛选；`amountMin`/`amountMax`；允许只填一端；最小不大于最大（违例提示"最小金额不能大于最大金额"）|
| 合同状态 | 下拉 | 全部 | 取值：已签约 / 失效（见 §26.2） |
| 负责人 | 下拉 | 全部 |

> **签约日期与创建时间口径区别：** 签约日期=合同业务发生时间（人工录入），创建时间=合同录入系统时间（系统生成）；两字段独立筛选、不混用。重置时恢复默认（签约日期=本月，创建时间=不限制）。

### 16.5 表格字段

合同编号（人工录入，跳详情）、合同名称、客户（展示客户名称，口径见 §2.4）、金额（¥，如 ¥45,200）、签约日期（YYYY-MM-DD）、合同状态（彩色徽章：已签约 / 失效）、负责人、附件（"N 个附件"悬停文件名）、操作（详情、编辑、更多→作废）。**失效合同仍保留在列表中展示**（可筛选）。列表顶部提供【导出】（CSV，按当前筛选/选中，见 §16.6.4）。分页：创建时间倒序，20 条/页，20/50/100。列表顶部提供【批量导入】（见 §16.6.5）。**不提供删除。**

### 16.6 合同操作

#### 16.6.1 新增合同

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 合同编号 | 文本 | 是 | 人工录入，系统不生成，提交后不可改；站点内唯一（合同继承客户站点，重复"该合同编号已存在，请重新输入"） |
| 合同名称 | 文本 | 是 | — |
| 客户 | 下拉搜索 | 是 | 权限内客户；展示**客户名称**（企业/个体工商户取企业名称，个人取询盘联系人姓名，见 §2.4） |
| 关联线索 | 下拉搜索 | 否 | 仅已选客户下线索；未选客户置灰；换客户清空；状态不限 |
| 金额 | 数字 | 是 | ¥，>0 |
| 签约日期 | 日期 | 是 | 默认今日 |
| 合同状态 | 下拉 | 是 | 默认**已签约**（仅已签约 / 失效；录入固定为已签约） |
| 合同附件 | 上传 | 否 | PDF/doc/docx/xls/xlsx/jpg/png；单个≤10MB，建议≤5 个 |

> **无「合同结束时间」字段。**

提交规则：
- 负责人默认当前用户；生成跟进记录（备注"录入合同[编号]"）。
- **线索联动（BR-056）：** 若填写了关联线索 → 该线索状态置为**已成交**（不论原状态为待跟进/跟进中/已转客户等；已是已成交则保持）。未关联线索则不改任何线索。
- 仅驱动**本条关联线索**，不影响同客户其他线索。

#### 16.6.2 编辑合同

- **字段口径：** 编辑表单字段与合同详情展示字段对齐（§16.8）。
- **只读（不可改）：** 合同编号、客户、关联线索。
- **可改：** 合同名称、金额、签约日期、合同状态（已签约 / 失效）、负责人、附件。
- **改为失效：** 须填写失效原因（同【作废】）；确认后合同状态→失效，并触发与 §16.6.3 / BR-056 相同的线索回退联动；【作废】入口保留（快捷作废，不替代编辑改状态能力）。
- **由失效改回已签约：** 允许；若合同有关联线索，按 BR-056 将该线索置为已成交（已是已成交则保持）。
- 保存后生成跟进/操作记录（备注含变更字段）。
- **列表/详情：** 失效合同仍留在列表，可查看详情；编辑入口不因失效单独置灰（与上条状态可回改一致）。

#### 16.6.3 作废

- **入口：** 操作列更多→【作废】（按钮文案为「作废」；状态展示为「失效」）。亦可在【编辑合同】中将合同状态改为失效（见 §16.6.2）。
- **准入：** 仅状态=**已签约**可作废；已失效不可再作废（可经编辑改回已签约后再作废）。
- **弹窗：** 作废原因（必填）；确认后合同状态→**失效**。
- **列表表现：** 失效合同**仍留在合同列表**（可筛选），不隐藏、不物理删除；合同中心**不提供删除**（BR-050）。
- **线索联动（BR-056）：**
  1. 合同**无关联线索** → 仅改合同状态，不动线索。
  2. 合同关联线索 A1，且 A1 当前=**已成交**：若 A1 下仍存在其他状态=**已签约**的合同 → A1 **保持已成交**；若 A1 下已无其他已签约合同 → A1 回退为**已转客户**。
  3. 仅回退本条关联线索 A1，不影响同客户其他线索。
  4. 当线索状态实际发生回退时，在**该线索**跟进记录中追加一条**系统执行**记录，文案示例：`系统：合同[编号]已失效，线索状态由「已成交」变更为「已转客户」`。
- 详情顶部可展示失效原因+时间+操作人；系统日志记录。

#### 16.6.4 合同导出

- **入口：** 合同列表顶部【导出】。
- **字段：** 导出范围（当前选中[默认]/当前筛选结果/当前页）、文件格式（只读 CSV）、导出上限（"最多 10000 条"）。
- **规则：** 导出内容为当前权限范围内合同列表字段（含已签约与失效；**不含合同结束时间**）；超 10000 条提示"数据量过大，请缩小筛选范围"；成功下载本地，失败 Toast"导出失败，请重试"。
- **权限：** 业务员（本人负责）、运营专员（负责站点）、协同人（授权站点只读，默认可导出）、系统管理员（全部）均可导出；记录操作日志（操作类型=导出，见 §23.9、BR-054）。

#### 16.6.5 合同导入

- **入口：** 合同列表顶部【批量导入】；先【下载导入模板】。
- **模板字段（与 §16.6.1 新增合同表单一致，不含附件）：** 合同编号（必填）、合同名称（必填）、客户（必填）、关联线索（可选）、金额（必填，>0）、签约日期（必填）、合同状态（必填，默认已签约；枚举仅已签约/失效）。**无合同结束时间。合同附件不支持批量导入**，导入成功后可在合同详情页单独上传。
- **流程：** 下载模板 → 填写 → 上传（Excel/CSV）→ 系统校验 → 预览（错误行标红与原因）→ 确认导入。
- **匹配与校验：** 客户按（站点 + 客户名称）在操作人权限范围内精确匹配已有客户，未命中整行失败；关联线索填写时须属于已匹配客户，否则行失败；合同编号在站点内唯一（继承客户站点），重复行失败；金额/日期格式校验；模板表头与系统字段不一致整单拒绝，提示"导入模板与系统字段不一致，请下载最新模板"。
- **线索联动：** 导入成功且关联线索、状态=已签约时，按 §16.6.1 / BR-056 将关联线索置为已成交。
- **归属：** 负责人默认归属客户当前负责人（与 §27.5 一致，无系统参数开关）；导入结果汇总"成功 N 条、失败 K 条（可下载失败明细）"。
- **权限：** 业务员（本人负责）、运营专员（负责站点）、系统管理员（全部）可导入；**协同人不可（严格只读）**；记录导入日志（操作类型=导入，见 §23.9、BR-054）。

### 16.7 合同状态流转

合同状态机权威定义见 §26.2（**仅已签约 / 失效**；录入默认已签约；可通过【作废】或【编辑】改为失效；可通过【编辑】由失效改回已签约）。不提供终止、执行中、已完成等状态。

### 16.8 合同详情页

- 入口：操作列【详情】或合同编号；独立页面（原型可为抽屉，字段口径以下表为准）。
- **展示字段（与编辑可编辑项对齐）：** 合同编号、合同名称、合同状态、负责人、客户、金额、签约日期、关联线索、附件；另有操作记录区。
- 布局：顶部信息区（编号/名称/状态徽章/负责人）、基本信息区（客户名跳详情、金额¥、签约日期）、关联信息区（关联线索跳详情）、附件区（下载预览）、操作记录区（**仅合同自身操作记录：创建/编辑/作废/附件操作，不含跟进记录**）。
- **编辑约束回顾：** 编号 / 客户 / 关联线索只读；其余业务字段可在【编辑】中修改（§16.6.2）。
- 操作记录字段：操作时间、操作人、操作类型、操作内容（变更前→后）、关联字段。

### 16.9 异常处理

引用 §29.1、§29.3、§29.5。模块特有：无数据"暂无合同"、搜索无结果"未找到匹配的合同"；合同编号重复拦截；关联客户已逻辑删除时客户名后标"(已注销)"。

---

## 17. 分析中心 · 销售经营

### 17.1 模块概述

- **定位：** 销售过程与成交结果分析，核心回答"销售业务推进效果怎么样？"；沿 **线索 → 跟进 → 高意向 → 客户 → 合同 → 成交** 链路分析。
- **职责边界：** **负责**——销售漏斗、转化效率、销售人员表现、成交结果；**不负责**——询盘来源/渠道分析（归获客分析）、客户价值/画像（归客户经营）、待办与异常（归工作台）。
- **面向角色：** 运营专员、协同人、系统管理员（数据权限遵循 §4.2）。

所有数据指标卡片需增加信息提示角标（i Icon），Hover 后展示 Tooltip 说明。提示内容需包含该指标的统计维度、统计范围、计算规则及数据更新时间，避免用户对指标含义产生歧义。

### 17.2 页面结构

顶部筛选栏 + 指标卡区+ 分析区

### 17.3 筛选条件

统计时间（时间快捷选项：今日/本周/本月/本季度/自定义，默认**本月**，`statTimeStart`/`statTimeEnd`，支持自定义时间范围，允许只填开始或只填结束，空值不限制）、站点筛选（默认全部站点，**按站点统计**销售经营数据）。**统计时间只影响当前分析页**，不影响全局站点、用户权限、角色权限；不修改分析权限模型。切换后除趋势图外的区域同步刷新。

### 17.4 指标卡

| 卡片 | 定义 | 变化量格式 |
| :--- | :--- | :--- |
| 询盘总数 | 周期内线索列表产生的全部线索数量（按创建时间统计；来源：邮件 + WhatsApp + 人工录入 + 导入；含当前有效、已标记无效、已标记丢失及后续状态变化的历史线索）| 较上期 +X,XXX（+X.X%）|
| 有效线索数 | 当前状态仍属于有效跟进范围的线索数量（当前状态 ≠ 无效、≠ 丢失）| 较上期 +X,XXX（+X.X%）|
| 高意向线索| 本人为负责人、当前跟进阶段「是否计入高意向=是」的线索数（字典扩展属性，§23.5；不得按阶段名称硬编码） | 线索列表（按阶段过滤） | 全部 |
| 已建客户数 | 周期内新建（含线索转客户）的客户数 | 较上期 +X,XXX（+X.X%）|
| 已成交客户数 | 周期内有签约合同的客户数（去重，口径见 27.7）| 较上期 +X,XXX（+X.X%）|
| 外销合同数  | 周期内签约合同数（状态=已签约，口径见 27.7）| 较上期 +X,XXX（+X.X%）|
| 合同金额 | 周期内合同金额合计（状态=已签约合同的金额累计，口径见 27.7）| 较上期 +X.XX%（+¥X,XXX）|
| 成交转化率 | 已成交客户数 / 有效线索数 ×100% | 较上期 +X.X 个百分点 |
| 高意向转化率 | 高意向线索数 / 有效线索数 ×100% | 较上期 +X.X 个百分点 |



> **口径说明（§17 线索级）：**  
> - **询盘总数** = 周期内创建的全部线索数量（历史产生量；含后续标记为无效/丢失的线索）。  
> - **有效线索数** = 当前状态不属于【无效、丢失】的线索数量（当前存量）。  
> - 二者时间口径与业务含义不同，**不得合并为同一指标**。邮件 / WhatsApp / 人工录入 / 导入进入线索列表时入库视为有效，无效/丢失仅在后续跟进中判定。  
> - 高意向阶段以字典「跟进阶段」扩展属性「是否计入高意向=是」为准（与「是否允许转客户」分立；预置默认打样/报价/谈判为是，§23.5.7）；**不得**按阶段名称硬编码。  
> **与 §18 获客分析切分：** §18 同时含消息接入效率与全盘获客结构；其中「询盘总数 / 有效线索数」与 §17 **口径同源**，§18 用于渠道/地域/产品/站点等获客结构，§17 用于销售漏斗、人员表现与成交经营。§18 顶部「生成线索数」仅计消息链路产出，不等于询盘总数。

### 17.5 分析区

- **销售漏斗（横向）：** 询盘总数 → 跟进中 → 高意向 → 已转客户 → 已成交；显各阶段数量 + 相邻转化率。
- **线索状态分布：** 按线索状态（待跟进/跟进中/已转客户/无效/丢失）分布（饼图或柱状），反映线索处理结果结构。
- **跟进阶段分布：** 按当前跟进阶段（待首响/已联系/需求确认/打样/报价/谈判）分布，反映跟进推进结构。
- **销售人员排行 TOP10：** Tab 切换（按成交额/按转客户数）；字段：排名、业务员（姓名+授权站点）、线索数量、跟进数、转客户数、成交金额；所辖站点成员行高亮；每页 10 条。
- **站点销售表现：** 按站点统计线索数量、转客户数、成交客户数、合同金额、成交转化率（表格，默认合同金额倒序，每页 10 条）。
- **线索增长与成交金额趋势（双轴）：
根据顶部时间筛选条件动态展示趋势数据，支持按天、月、年三个时间粒度查看。
X 轴根据选择的时间范围及统计粒度自动生成对应时间节点；
左轴展示线索数量（柱状图）；
右轴展示成交金额（折线图）；
按「年」筛选时，按月聚合展示趋势（如 1 月～当前月份）；
按「月」筛选时，按天聚合展示趋势（如 1 日～当前日期）；
按「天」筛选时，展示当天统计结果。
图表需保持与顶部时间筛选条件一致，确保用户查看的数据范围与统计维度统一。


### 17.7 数据更新与异常

指标卡/漏斗/排行榜按所选时间实时统计；趋势图固定本年度实时统计。异常引用 §29.1–§29.3（无数据指标卡"--"、图表空态占位、加载失败提示）。

---

## 18. 分析中心 · 获客分析

### 18.1 模块概述

- **定位：** 分析询盘来源与获客效果，核心回答"客户从哪里来？哪些渠道/地区/产品更有效？"；覆盖**消息接入质量**与**全盘线索获客结构**。
- **职责边界：** **负责**——消息接入效率（消息总量/由消息生成的线索/线索生成率）、来源渠道分布、国家/地区、意向产品、客户关注点、站点价值（含站点维度成交金额）；**不负责**——销售漏斗、人员排行与深度转化经营（归销售经营）、客户价值/画像（归客户经营）。
- **面向角色：** 运营专员、协同人、系统管理员（不面向业务员）。

### 18.2 页面结构

顶部筛选栏（**统计时间**快捷选项默认**本月**，`statTimeStart`/`statTimeEnd`，支持自定义时间范围，允许只填开始或只填结束，空值不限制 + 站点筛选）+ 顶部指标卡 + **消息量趋势** + 来源渠道分布 + 国家/地区分布 + 意向产品分析 + 客户关注点分析 + 站点价值分析。获客/来源分析**按站点统计**。**统计时间只影响当前分析页**，不修改分析权限模型。**以上分析能力均一期交付。**

所有数据指标卡片需增加信息提示角标（i Icon），Hover 后展示 Tooltip 说明。提示内容需包含该指标的统计维度、统计范围、计算规则及数据更新时间，避免用户对指标含义产生歧义。

### 18.3 顶部指标卡

| 卡片 | 定义 |
| :--- | :--- |
| 消息总量 | 所选周期内收到的**消息**总数（邮件消息 + WhatsApp 消息；按站点；含未生成线索的无效消息）|
| 生成线索数 | 所选周期内**由消息链路产生**的线索数（§9.1 来源① AI 自动创建 + ② 消息侧人工生成；**不含**③ 列表手动新建、④ 批量导入）|
| 线索生成率 | 生成线索数 / 消息总量 ×100% |
| 覆盖国家数 | 周期内线索**来源国家/地区**去重数（与 §18.6 国家/地区分布图同源；统一取线索侧来源国家/地区，不用消息侧国家）|

> **口径分层（§18 内部）：**  
> - **消息接入层（本卡 + §18.4）：** 仅消息（邮件消息与 WhatsApp 消息）与由其生成的线索，衡量接入效率。  
> - **全盘获客层（§18.5–§18.9）：** 以线索为统计对象，含消息链路、人工录入、导入等全部入库方式，按来源渠道等归因看结构。  
> **与 §17 切分：** 「询盘总数 / 有效线索数」口径与 §17 **同源**；§18 用于渠道/地域/产品/站点等**获客结构**，§17 用于漏斗、人员与成交经营。高意向等推进类指标仍仅属 §17。

### 18.4 消息量趋势

按所选统计周期将消息按时间分桶（默认按日，跨周期可按周/月），以**折线图**呈现**消息总量**与**生成线索数**双线走势，反映获客规模与线索产出随时间的变化。数据来源为消息（邮件消息与 WhatsApp 消息，按站点）；生成线索数口径同 §18.3；无数据时显空态占位。

### 18.5 来源渠道分布

按线索**来源渠道**（取值见 §23.5 客户域→来源渠道，含邮件、WhatsApp、官网询盘、展会、客户转介绍、其他等）统计周期内**询盘总数**（与 §17 同源：按创建时间，含邮件 + WhatsApp + 人工录入 + 导入）。以**柱状图**展示各渠道询盘数量分布（仅渠道 × 询盘总数，不叠加消息总量）。无该渠道线索时显 0。

### 18.6 国家/地区分布

按线索**来源国家/地区**统计**线索数**（前 10 + 其他），地图或柱状展示，反映获客地域分布。国家口径与 §18.3「覆盖国家数」同源，统一为线索侧来源国家/地区（见 §30.1）。

### 18.7 意向产品分析

按线索字段**意向产品**统计**线索数**（前 N + 其他），反映市场产品需求热度。

### 18.8 客户关注点分析

按**客户关注点**（字典：客户域→客户关注选项，完整预置见 §23.5.7）统计**线索**分布，反映客户在获客阶段的关注热点。与客户经营 §19.4「客户关注点分布」（客户维度、按客户数）并存。

### 18.9 站点价值分析

字段：站点名称、**询盘总数**（与 §17 同源）、**有效线索数量**（与 §17 同源：当前状态 ≠ 无效、≠ 丢失）、**转化率**（已成交客户数 / 询盘总数 ×100%；已成交客户数口径见 §27.7）、成交金额（口径见 §27.7）。排序支持按询盘总数/有效线索数/转化率/成交金额，默认成交金额倒序；每页 10 条。

> **站点统计口径：** 邮件线索按邮箱对应站点计入站点统计；WhatsApp 线索按业务员唯一授权站点计入站点统计；人工录入/导入线索按其线索站点计入。

### 18.10 异常

引用 §29.1–§29.3。模块特有：部分渠道/维度无数据时该项显"-"。


---

## 19. 分析中心 · 客户经营

### 19.1 模块概述

- **定位：** 客户资产质量与客户价值分析，核心回答"客户质量如何？哪些客户值得经营？"
- **职责边界：** **负责**——客户画像、客户生命周期、客户价值、客户活跃度、**客户关注点（客户维度）**；**不负责**——销售漏斗/成交结果（归销售经营）、询盘来源/渠道及线索维度关注点分布（归获客分析）、待办与异常（归工作台）。
- **面向角色：** 业务员（简化版视图）、运营专员、协同人、系统管理员。

### 19.2 页面结构

顶部筛选栏（**统计时间**范围默认**本年**，`statTimeStart`/`statTimeEnd`，支持自定义时间范围，允许只填开始或只填结束，空值不限制 + 站点筛选[业务员不展示]，**按站点统计**客户经营数据）+ 指标卡区（MVP 6 项 + **【二期/延后】** 1 项）+ 图表区 + 客户画像。**统计时间只影响当前分析页**，不新增全局站点切换器、不修改分析权限模型。

### 19.3 指标卡

| 卡片 | 定义 |
| :--- | :--- |
| 客户总数 | 截至当前客户总数 |
| 新增客户数 | 所选周期内新建客户数 |
| 高意向客户数 **【二期/延后】** | 潜质分级="高潜客户"或名下存在高意向线索（跟进阶段「是否计入高意向=是」）的客户数 |
| 成交客户数 | 所选周期内有签约合同的客户数（去重，口径见 27.7）|
| 活跃客户数 | 在「客户活跃度判定天数」内有跟进/消息互动的客户数（阈值默认 90 天，见系统参数 §23.7）|
| 沉默客户数 | 超过「客户活跃度判定天数」无跟进/消息互动的客户数（默认 90 天，见 §23.7）|
| 客户合同金额 | 客户名下合同金额合计（口径见 27.7）|

### 19.4 图表

- **客户潜质分级分布：** 按客户潜质分级（高潜客户/潜在客户/一般客户）分布（饼图）。
- **国家/地区分布：** 按客户所属国家/地区分布（柱状/地图，前 10 + 其他）。
- **客户新增趋势：** 按月统计新增客户数，以**折线图**呈现（X 轴本年 1 月至当前月，固定本年、不受时间筛选影响），反映客户资产的增长节奏。
- **客户标签分布：** 按客户人工标签分布（前 N + 其他）。
- **客户关注点分布：** 按**客户关注点**（字典：客户域→客户关注选项，见 §23.5）统计**客户数**。口径：取客户名下**关联线索**的关注点，在同一客户内**聚合去重**后，该客户计入对应关注点；一客户可落入多个关注点桶。与获客分析 §18.8（线索维度）并存、维度不同。
- **客户活跃度：** 按最近互动时间分桶（活跃/沉默，阈值取系统参数「客户活跃度判定天数」，见 §23.7）分布，反映客户经营健康度。
- **客户成交分析：** 成交客户占比、客户合同金额分布/年度成交趋势（X 轴本年 1 月至当前月）。

### 19.5 客户画像

- **补充说明：** 客户画像用于**沉淀客户价值信息**，是客户资产分析的基础。单客户画像在【客户详情 → AI 客户画像】查看与**人工编辑**（§15.4.5）；本模块从客户资产视角聚合与呈现。数据初始来源 AI；人工编辑后以人工为准、AI 不覆盖（见 §15.4.5、BR-017）。
- **包含维度：** 企业工商信息、AI 参展资料、AI 风险提醒、客户类型（业务口径：企业/个体/个体工商户，见 §30.6，不作独立字段统计）、核心诉求、采购产品、采购量级、沟通语言、国家/地区。

### 19.6 业务员简化版视图

指标卡与全部图表、客户画像均展示，数据限定本人负责客户；不展示站点筛选；时间范围可用；布局与其他角色一致。

### 19.7 异常

引用 §29.1–§29.3（无数据指标卡"--"、图表空态、加载失败提示）。

### 19.8 模块指标边界说明

统一四个页面（工作台 / 销售经营 / 获客分析 / 客户经营）的指标归属，避免重复与职责交叉：

| 指标 | 所属模块 |
| :--- | :--- |
| 待跟进 | 工作台 |
| 邮件未读 | 工作台 |
| 新增线索提醒 | 工作台 |
| 询盘总数 / 有效线索数（漏斗与经营） | 销售经营 |
| 询盘总数（按来源渠道/站点获客结构） | 获客分析 |
| 消息总量 / 生成线索数（消息链路） / 线索生成率 | 获客分析 |
| 高意向线索 | 销售经营 |
| 来源渠道分布 | 获客分析 |
| 国家分析 | 获客分析（线索来源国家/地区）/ 客户经营（客户所属国家/地区）|
| 意向产品 | 获客分析 |
| 客户关注点 | 获客分析（线索维度）/ 客户经营（客户维度：名下线索关注点聚合去重后计客户数） |
| 站点价值（询盘/有效线索/转化率/成交金额） | 获客分析 |
| 客户数量 | 客户经营 |
| 合同金额（销售经营页） | 销售经营 |
| 客户价值 | 客户经营 |
| 客户画像 | 客户经营 |

> **原则：** 工作台只做"今天要处理什么"（待办/异常/我的业务状态），不做经营与趋势分析；销售经营看"推进与成交"，获客分析看"从哪来与获客结构（含消息接入效率）"，客户经营看"客户资产与价值"。同名指标在不同页面按上表维度区分，不跨页重复承担。分析中心若展示「客户名称」，统一按 §2.4 客户名称口径（企业/个体工商户取企业名称，个人取询盘联系人姓名）；MVP 不新增企业/个人客户类型分析维度。

---

## 20. 站点中心 · 站点管理

### 20.1 模块概述

- **定位：** 管理所有销售站点，每站点为**业务运营与数据隔离的基础单位**（非组织架构，不扩展部门/区域/组织树；术语权威见 §2.4）。站点管理负责：站点基础信息、绑定运营人员与主邮箱、AI 规则；回收规则本期**预留展示**（二期可配）；**不承担消息接入/拉取的运行状态展示与管理**。
- **面向角色：** 系统管理员。
- **层级：** 列表页 + 站点详情页（只读）+ **新增/编辑弹窗**（一次保存完整字段；**不单列站点配置页/【配置】操作**）。

### 20.2 列表页

#### 20.2.1 结构

筛选区（按站点名称/站点 ID 搜索 + 创建时间范围筛选）+ 工具栏（新增站点、刷新）+ 表格区。**不设消息拉取状态区**。

| 筛选字段 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 站点名称/站点 ID | 文本 | 空 | 模糊搜站点名称或站点 ID |
| 创建时间 | 日期区间 | 不限制 | 按站点创建时间；`createTimeStart`/`createTimeEnd`；允许只填开始或只填结束；空值不限制 |

> 重置时恢复默认（创建时间=不限制）。

#### 20.2.2 表格字段

| 字段 | 说明 |
| :--- | :--- |
| 站点名称 | 手动填写 |
| 站点 ID | 系统唯一标识，系统自动生成、只读 |
| 站点域名 | 手动填写 |
| 状态 | 站点启用/停用状态 |
| 回收规则 | 摘要展示；本期预留（二期，见 §20.5.1） |
| 绑定运营人员 | 该站点负责运营人员（见 §20.5.3） |
| 主邮箱 | 该站点业务主邮箱（=绑定运营人员的业务邮箱，见 §20.5.3） |
| 操作 | 编辑、启用/停用（**无【配置】**） |

#### 20.2.3 操作

| 操作 | 说明 |
| :--- | :--- |
| 编辑 | 打开编辑弹窗；字段与新增一致，**仅站点 ID 不可改**；不含状态（状态用列表启用/停用） |
| 启用/停用 | 直接切换；停用二次确认"停用后该站点将不再接收新消息、公海回收暂停，历史数据保留。确认停用？" |

### 20.3 新增/编辑站点弹窗

**一次保存、一次校验：** 新增须在同一弹窗填完并保存站点基本信息、绑定运营人员、主邮箱、AI 规则；**不得**依赖列表【配置】等后续操作补齐。校验失败不创建站点。

| 字段 | 新增 | 编辑 | 必填 | 说明 |
| :--- | :--- | :--- | :---: | :--- |
| 站点名称 | 可填 | 可改 | 是 | 非空"请输入站点名称" |
| 站点域名 | 可填 | 可改 | 是 | 非空"请输入站点域名" |
| 站点 ID | 系统自动生成 | **只读不可改** | — | 系统唯一标识 |
| 绑定运营人员 | 下拉 | 可改 | 是 | 仅启用状态的运营专员；协同人不可选 |
| 主邮箱 | 下拉 | 可改 | 是 | 取所选运营人员已绑定业务邮箱；一人多邮箱时择一；一站点一主邮箱、邮箱不可跨站点重复占用 |
| 启用 AI 识别 | 开关 | 可改 | — | 默认开启；关闭后该站点消息不调用 AI 识别（§20.5.2） |
| 回收规则 | 预留只读区 | 预留只读区 | — | **本期不可配**（二期，§20.5.1）；界面可展示说明文案 |
| 状态 | 默认启用（不在表单改） | 不在本弹窗改 | — | 仅列表【启用/停用】 |

新增成功后状态默认"启用"。绑定运营人员 / 主邮箱 / AI 规则规则同 §20.5.2、§20.5.3。

### 20.4 站点详情页

只读展示。顶部导航（返回站点列表 + 站点名称 + 站点 ID + 状态徽章）、统计概览（线索总数/客户总数/合同总数）、信息详情（站点名称/站点 ID/站点域名/创建时间/状态/绑定运营人员/主邮箱/AI 规则/回收规则摘要）。站点不存在"站点不存在或已被删除"；统计为空显"--"。

### 20.5 字段与规则说明（原配置页字段权威，入口已并入 §20.3）

> **MVP 不提供独立「站点配置」页签/列表【配置】按钮。** 下列规则仍为字段权威；维护入口仅为【新增站点】/【编辑】。

#### 20.5.1 回收规则 **【二期/延后】**

> **MVP 不做站点级自动回收配置**，公海回收仅保留运营专员手动回收（见 §10.7）；新增/编辑弹窗内**预留展示**，本期不可编辑。

二期：配置该站点线索回收规则：启用自动回收（开关，默认关闭，仅系统参数未配置时生效）、回收超时时长（数字+单位，启用必填，默认 7 天）。**回收的完整线索生命周期与固定规则以线索中心为准（见 §10.7），本模块不重复定义。**

#### 20.5.2 AI 规则

启用 AI 识别（开关，默认开启，关闭后该站点消息不调用 AI 识别）。在新增/编辑弹窗中维护，**仅保留该开关**。

#### 20.5.3 运营人员与主邮箱

- **定位：** 为当前站点**绑定运营人员**，并以该运营人员的**业务邮箱**作为站点**主邮箱**对外接收询盘。业务关系为「**站点 → 绑定运营人员 → 运营人员绑定业务邮箱 → 邮箱接收消息 → AI 识别有效询盘 → 生成线索**」；**取消「邮箱直接绑定站点」的方式**。
- **面向角色：** 系统管理员。
- **创建必填：** 新增站点时运营人员与主邮箱均为必填，未选不可保存。

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 绑定运营人员 | 下拉 | 是 | 选择负责本站点的运营人员（仅"启用"运营专员可选；协同人不可选）|
| 主邮箱 | 下拉 | 是 | 取所选运营人员已绑定的业务邮箱；该运营人员有多个业务邮箱时选择其一作为本站点主邮箱 |
| 绑定时间 | 系统字段 | — | 只读 |

**规则：**

- **一站点一主邮箱**：当前站点仅能绑定一个运营人员与一个主邮箱；同一业务邮箱不可同时作为多个站点主邮箱、不可重复绑定。
- 保存后，该邮箱即为本站点主邮箱；后续该邮箱收到的有效询盘线索：站点 = 本站点，负责人 = 该运营人员，由运营人员转派给业务员（归属与转派规则见 §7.5.2、§27.1，本模块不重复定义）。
- 更换绑定运营人员/主邮箱仅影响后续新邮件生成的线索，不影响历史邮件与历史线索。
- 校验：邮箱已被占用→"该邮箱已绑定，不可重复绑定"；异常见 §29.11。

### 20.6 站点停用连锁影响

| 影响范围 | 停用后行为 |
| :--- | :--- |
| 消息接收 | 不再接收新消息（邮件消息与 WhatsApp 消息均停收），已有消息永久保留 |
| AI 识别 | 无新消息故不调用，历史结果保留 |
| 公海自动回收 | 暂停（含手动回收对该站点不可用），重新启用后恢复 |
| 现有线索/客户/合同 | 历史数据保留，已有业务可继续流转 |
| 站点筛选可见性 | 停用站点仍在各筛选下拉中（带「已停用」标记），可查历史数据 |
| 站点删除 | 不允许物理删除，仅停用（BR-009） |

### 20.7 异常

引用 §29.1、§29.3、§29.5。模块特有：无数据"暂无站点"、搜索无结果"未找到匹配的站点"；停用存在进行中线索允许停用；主邮箱重复绑定拦截（见 §20.5.3、§29.11）。

---

## 20A. 站点中心 · 站点运营数据



### 20A.1 模块概述

- **定位：** 按**站点 + 时间周期（月/周）**记录站点侧运营指标（广告、流量、询盘、GSC 等），为获客分析（§18）等提供数据来源。数据来源为**手动维护 + 批量导入**。
- **面向角色：** 系统管理员（新增/编辑/删除/导入/导出）；运营专员/协同人在授权站点范围内**只读查看**（菜单可见性见 §4.3）。业务员无该菜单。
- **写权限：** 仅系统管理员可新增/编辑/删除/导入；操作记录导入日志（BR-054）。
- **核心规则：** **列表展示字段 = 导入字段 = 导入模板字段，三者完全一致**（字段顺序、名称一一对应）。

### 20A.2 字段定义

| 序号 | 字段 | 建议列名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| — | 站点 | `site_id` | BIGINT | 是 | 维度；FK→站点；导入模板用站点名称/编码匹配 |
| — | 统计周期类型 | `period_type` | TINYINT | 是 | 维度；月 / 周 |
| — | 统计周期 | `period` | VARCHAR(16) | 是 | 维度；月=YYYY-MM，周=YYYY-Www（如 2026-W27） |
| 1 | 广告花费 | `ad_spend` | DECIMAL(14,2) | 否 | 允许空 |
| 2 | 网站浏览次数 | `website_visits` | BIGINT | 否 | 允许空 |
| 3 | 询盘量 | `inquiry_count` | BIGINT | 否 | 允许空 |
| 4 | 高潜询盘量 | `high_intent_inquiry_count` | BIGINT | 否 | 允许空 |
| 5 | 询盘转化率 | `inquiry_conversion_rate` | DECIMAL(7,4) | 否 | 允许空；百分比 |
| 6 | 单个询盘成本 | `cost_per_inquiry` | DECIMAL(14,2) | 否 | 允许空 |
| 7 | 高潜询盘成本 | `cost_per_high_intent_inquiry` | DECIMAL(14,2) | 否 | 允许空 |
| 8 | 平均访问时长 | `avg_visit_duration` | DECIMAL(10,2) | 否 | 允许空；秒 |
| 9 | 网站跳出率 | `bounce_rate` | DECIMAL(7,4) | 否 | 允许空；百分比 |
| 10 | GSC 曝光量 | `gsc_impressions` | BIGINT | 否 | 允许空 |
| 11 | GSC 点击次数 | `gsc_clicks` | BIGINT | 否 | 允许空 |
| 12 | GSC 出词数 | `gsc_keywords` | BIGINT | 否 | 允许空 |
| 13 | GSC 平均排名 | `gsc_avg_position` | DECIMAL(8,2) | 否 | 允许空 |

> **唯一约束：** `UNIQUE(site_id, period_type, period)`——同一站点同一周期仅一条记录。

### 20A.3 列表页

- **顶部筛选：** 站点（下拉，仅授权站点）、统计周期类型（月/周）、统计周期区间。
- **列表字段：** 与 §20A.2 完全一致（站点、周期类型、周期 + 13 项指标），末列操作（编辑、删除）；空值显"--"。
- **操作按钮：** 【新增】、【批量导入】、【下载导入模板】、【导出】。
- **分页：** 按周期倒序，20 条/页，20/50/100。

### 20A.4 新增/编辑弹窗

- 字段同 §20A.2；维度字段（站点、周期类型、周期）编辑时只读、新增时必填；13 项指标均允许空。
- 校验：站点/周期类型/周期非空；`(site_id, period_type, period)` 重复时拦截"该站点该周期数据已存在，请编辑或使用导入覆盖"；数值字段格式校验（非负、百分比 0~100）。
- 保存记录操作日志。

### 20A.5 批量导入

- **入口：** 列表页【批量导入】；先【下载导入模板】。**模板字段必须与列表字段完全一致**（顺序：站点、周期类型、周期 + 13 项指标）。
- **流程：** 下载模板 → 填写 → 上传（Excel/CSV）→ 系统校验 → 预览（含错误行标红与原因）→ 确认导入。
- **校验规则：** 站点须存在且在操作人权限范围内；周期类型/周期格式合法；数值格式合法；模板表头与系统字段一致，否则整单拒绝并提示"导入模板与系统字段不一致，请下载最新模板"。
- **重复处理：** 命中唯一键 `(site_id, period_type, period)` 时**按行覆盖**（更新已有记录），非重复行新增；导入结果汇总"成功 N 条、覆盖 M 条、失败 K 条（可下载失败明细）"。
- **权限：** 写操作仅系统管理员（见 §20A.1）；运营专员/协同人只读查看授权站点数据。

### 20A.6 权限

| 操作 | 业务员 | 运营专员 | 协同人 | 系统管理员 |
| :--- | :--- | :--- | :--- | :--- |
| 查看 | — | ✓(授权站点) | ✓(管理范围) | ✓(全部) |
| 新增/编辑/删除/导入/导出 | — | — | — | ✓ |

### 20A.7 异常

引用 §29.1、§29.3、§29.5。模块特有：无数据"暂无站点运营数据"；导入模板不匹配整单拒绝；导入部分失败可下载失败明细并重试；唯一键冲突按覆盖处理。

---

## 21. AI 能力管理

### 21.1 模块概述

- **定位：** 统一管理 AI 服务商及模型能力（新增/编辑/启停），通过「业务关联」将能力绑定到固定业务场景并按场景路由调用；定义 AI 输出结构、同步规则与降级规则。
- **面向角色：** 系统管理员。

### 21.2 AI 能力列表

字段：能力名称、AI 服务商、AI 模型、业务关联、API 端点、状态（启用/停用）、创建时间、操作（编辑、启用/停用、删除[仅停用状态可删，启用置灰]）。搜索（能力名/服务商）、按状态筛选、创建时间倒序、每页 10 条、刷新。业务关联列以标签展示已绑定场景（多场景逗号/多标签均可）。

### 21.3 AI 能力字段

| 字段 | 类型 | 必填 | 编辑可改 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| 能力名称 | 文本 | 是 | 是 | — |
| AI 服务商 | 下拉 | 是 | 是 | 取值字典「AI 服务商」（§23.5）；服务商+模型组合唯一；仅可选启用项 |
| AI 模型 | 下拉 | 是 | 是 | 取值字典「AI 模型」，随所选服务商联动（父子）；**不保留「自定义」手填**；未预置须先在字典新增 |
| 业务关联 | 多选 | 是 | 是 | 选项=字典「AI 业务场景」展示名（四场景编码固定，§23.5 / §21.6）；至少选 1 项 |
| API 端点 | 文本 | 是 | 是 | 合法 URL |
| API Key | 文本 | 是 | 是（脱敏 sk-****xxxx，改需重输） | ≥20 字符 |
| 启用状态 | 开关 | 是 | 是 | 保存立即生效；停用后立即释放其业务关联占用（§21.6） |

**服务商 / 模型预置（字典初始化，可在字典管理增补服务商及其下模型）：**

| AI 服务商 | 联动模型（下拉单选） |
| :--- | :--- |
| OpenAI / Azure OpenAI | GPT-4o、GPT-4o-mini、GPT-4.1、GPT-4.1-mini |
| 通义千问 | Qwen-Max、Qwen-Plus、Qwen-Turbo、Qwen-Long |
| 智谱 AI | GLM-4、GLM-4-Plus、GLM-4-Flash |
| DeepSeek | DeepSeek-V4-Pro、DeepSeek-V4-Flash、DeepSeek-Chat、DeepSeek-Reasoner |
| 豆包 | Doubao-1.5-Pro、Doubao-Pro、Doubao-Lite |
| 月之暗面 | Kimi-K2、moonshot-v1 |
| 文心一言 | ERNIE-4.0、ERNIE-Speed |

> 服务商或模型字典项停用后：已引用的 AI 能力保留但视为不可用（§23.5.6、§21.5）；编辑时不可再选已停用项。

### 21.4 AI 输出结构与同步

> 与 §7.9 面板结构对齐。权威交互与复用规则以 §7.9 为准；本节为对象级同步契约。

#### 21.4.1 输出结构（最小集）

- **意向总结**（消息级）
- **意向产品**
- **客户画像**（统一模块）：企业工商信息、过往参展信息、企业风险提示
- **询盘标签**（消息层 AI 标签；写入线索后成为线索标签的 AI 来源部分，见 §25）

#### 21.4.2 同步关系

| 对象 | 写入/展示 | 明确不同步 |
| :--- | :--- | :--- |
| 消息 | 意向总结、意向产品、客户画像（分析过程产出；画像落询盘联系人资料）、询盘标签 | — |
| 线索 | 意向产品（正式字段）；询盘标签→追加线索标签；客户画像经询盘联系人资料**引用展示** | **意向总结**（不写线索字段） |
| 客户 | 转客户/关联时：① 客户侧**无**画像记录则写入联系人侧客户画像；客户侧**已有**画像记录则整包不碰；② **国家/地区、所属行业**从工商画像预填客户标准字段（新建预填、关联仅补空；字典匹配不上留空；可人工改） | 意向总结、意向产品、询盘标签/线索标签（客户仅人工标签） |

#### 21.4.3 关键规则

1. **触发：** 消息接入时分析一次并持久化；打开详情不重跑；MVP 无重新分析入口（§7.9）。
2. **意向总结：** 仅消息 AI 结果，不进线索、不进客户。
3. **意向产品：** 生成线索时写入线索字段；不进客户（BR-036）。
4. **客户画像：** 存询盘联系人资料；首次生成、有则复用存量、人工编辑优先（BR-016c、BR-017）；转客户时客户无画像才写入、有则不覆盖（BR-036）。
4a. **国家/地区、所属行业：** 转客户/关联时从工商画像预填客户标准字段（新建预填、关联仅补空；字典对齐，匹配不上留空）；之后仅人工【编辑】修改，AI 不因后续消息自动改客户档案这两字段。
5. **询盘标签：** 生成线索时追加到线索标签；同名去重；人工删除过的同名禁止 AI 再追加（§25、BR-016a）。
6. **所属企业名：** AI 识别的企业名称可作为线索「所属企业」字段，与客户画像模块配合展示；年营收不写入客户标准字段。

### 21.5 AI 降级规则

AI 不可用/调用失败时**不自动识别询盘、不自动创建线索**，业务不中断（BR-015）；消息保留、进入人工处理，由人工兜底创建线索、归属当前操作人；AI 失败不影响 CRM 运行。当前业务场景无人绑定启用能力（见 §21.6），或所绑能力引用的服务商/模型字典项已停用（§23.5.6），均视同 AI 不可用。**AI 属辅助能力，不阻断业务流程：AI 成功时自动识别有效询盘并自动创建线索、写入 AI 结果；AI 不可用/失败时消息保留、进入人工处理、由人工兜底创建线索（见 §21.8、§27.1）；一期不设规则引擎自动识别。**

### 21.6 业务关联

四个业务场景编码与数量由系统固定（字典类型 `ai_business_scenario`，§23.5：**不可新增/删除/启停场景项**，仅可改展示名称与排序）；在 AI 能力新增/编辑表单中以「业务关联」多选配置，**取消「系统默认 AI 能力」统一调用**。运行时按场景**编码**路由到对应已绑定且启用的 AI 能力（展示名变更不影响路由）。

| 场景编码 | 默认展示名称 | 调用入口 |
| :--- | :--- | :--- |
| `mail_intent` | 邮件意向分析 | 邮件消息接入时的意向分析 / 有效询盘识别（§7.9、§21.8） |
| `wa_intent` | WhatsApp 意向分析 | WhatsApp 消息接入时的意向分析 / 有效询盘识别（§8、§21.8） |
| `company_extract` | AI 自动提取企业信息 | 客户画像·企业工商等信息提取（§7.9、§21.4） |
| `batch_refine` | 批量 AI 提炼 | 邮件列表【批量 AI 提炼】（§7.8） |

#### 21.6.1 绑定与互斥规则

1. **一条能力可绑多个场景：** 同一 AI 能力可多选若干场景（如豆包同时勾选「邮件意向分析」「WhatsApp 意向分析」）。
2. **一场景仅一条启用能力：** 每个场景同一时间只能被**一条处于启用状态**的 AI 能力占用。若能力 A（启用）已绑定「邮件意向分析」，能力 B 再勾选该场景并保存/启用时**直接拦截**，提示「该场景已被「{能力名称}」占用，请先调整对方业务关联或停用对方后再试」；**不**允许抢占、不从 A 自动摘除。
3. **必填：** 业务关联至少选择 1 项；未选拦截保存。
4. **停用即释放：** 能力停用后，其已绑定场景**立即释放**，其他能力可选；仅「启用」状态计入占用。启用时若目标场景已被其他启用能力占用，同样拦截。
5. **总开关优先：** AI 功能启用总开关在系统管理→系统参数（业务规则配置）控制（见 §23.7）；总开关关闭时全部场景不调 AI，与是否绑定无关。

#### 21.6.2 运行时路由与降级

对某一业务场景：若「AI 功能启用」开启，且存在绑定该场景且状态为启用的 AI 能力 → 调用该能力；若**无人绑定**，或绑定能力均已停用/不可用/调用失败 → **不调 AI**，按 §21.5 / §29.7 降级（不自动识别询盘、不自动创建线索，消息保留转人工兜底；批量提炼失败提示重试等）。**不强制**四个场景必须始终各有一条启用能力。

### 21.7 操作与异常

新增/编辑按 21.3 字段校验（必填、URL 合法、Key≥20、服务商+模型唯一、业务关联至少 1 项且不与其他**启用**能力场景冲突）；启用/停用立即生效无二次确认（停用释放场景占用）；删除仅停用状态可删+二次确认，历史记录保留审计。API 端点连续 N 次调用失败自动标记异常，不影响其他能力，系统管理员可手动停用。AI 运行期异常的用户提示与降级引用 §29.7。

### 21.8 AI 有效询盘识别与自动生成线索（业务流程）

**AI 是线索生产入口。** 消息进入系统后 → AI 自动分析 → 识别有效询盘 → 提取业务信息 → 自动创建线索 → 绑定负责人 → 进入线索中心。人工创建线索为异常兜底（见 §21.8 第 4 点）。

1. **有效询盘识别标准（AI 判定）：** 消息（邮件消息或 WhatsApp 会话消息）满足以下**任一类型**即判定为有效询盘 → 自动生成正式线索 → 进入线索列表。

   | 类型 | 判断说明 | 示例 |
   | :--- | :--- | :--- |
   | 产品咨询 | 询问某类产品、型号、规格、功能、参数 | "你们有 XX 产品吗？" |
   | 价格咨询 | 询问报价、价格区间、采购成本 | "请发一下报价单。" |
   | 样品需求 | 询问样品、打样、试用、寄样 | "可以寄样品给我们测试吗？" |
   | 采购需求 | 表达采购、订购、批量购买意向 | "我们想采购 500 件。" |
   | 交期咨询 | 询问交货时间、生产周期、库存 | "最快什么时候可以发货？" |
   | 定制需求 | 提到 OEM、ODM、Logo、包装、定制规格 | "能否按我们的尺寸定制？" |
   | 合作代理 | 询问代理、分销、区域合作 | "我们想成为代理。" |
   | 项目询盘 | 项目背景、采购计划、招投标需求 | "我们有项目需要采购。" |
   | 复购/新需求 | 老客户提出新的产品、数量、报价需求 | "想再采购另一款产品。" |
   | 明确联系方式交换 | 请求销售联系、会议、资料 | "请安排销售联系我。" |

   - **业务规则：** 命中上述任意一种类型 → 判定有效询盘 → 自动生成正式线索 → 进入线索列表。
   - **非有效消息（不生成线索）：** 自动回复、广告、无业务内容消息、垃圾信息。
2. **判断结果存储（方案 B，不落库）：** AI 判断过程**不持久化**；**不新增** AI 有效询盘标识 / AI 置信度 / AI 判断原因等字段；AI 仅输出最终业务结果（线索及其提取字段与 AI 分析信息），判定有效性仅作为流程分支依据。
3. **AI 自动提取字段：**
   - **联系信息：** 联系人姓名、邮箱、电话、WhatsApp 账号。
   - **企业信息：** 企业名称、国家/地区、企业基本信息；其中**国家/地区写入线索「来源国家/地区」**（`source_country`，供获客分析 §18，可人工改）。
   - **业务信息：** 意向产品、产品需求、采购描述。
   - **AI 分析信息：** 意向总结、意向产品、客户画像（企业工商信息/过往参展信息/企业风险提示）、询盘标签（字段与同步口径见 §7.9、§21.4、§25，不重复定义）。
4. **AI 失败兜底：** AI 成功→自动创建线索；AI 不可用/失败→消息保留→进入人工处理→人工创建线索（见 §21.5）；**AI 失败不阻断业务**。人工创建线索仅用于：AI 服务不可用、AI 无法识别、历史数据补录、特殊业务场景。
5. **负责人归属与异常：** 归属规则与"收件邮箱/账号未绑定、用户停用、用户不存在"的待分配兜底见 §27.1、BR-020。

**邮件 — AI 识别有效询盘后：**

- 自动生成正式线索，站点 = 收件邮箱对应站点。**两类邮箱：** ① 业务员个人邮箱→**负责人=业务员本人**，直接进入本人线索列表；② 站点主邮箱（运营人员业务邮箱，`mail_site_id`，见 §20.5）→**负责人=该站点绑定的运营人员**，由其手动**转派给业务员、不进公海**（转派 T1–T3 见 §7.5.2；无线索可只投递）。
- 站点未绑定运营人员/主邮箱：其邮件不参与自动生成线索。

**WhatsApp — AI 识别有效询盘后：**

| 场景 | 站点 | 负责人 | 进入位置 |
| :--- | :--- | :--- | :--- |
| 业务员（单一授权站点） | 业务员唯一授权站点 | WhatsApp 绑定业务员 | 线索列表 |

> WhatsApp 仅业务员可绑（见 §24.3.2），线索站点必定明确、创建即"待跟进"、**不进公海池**。

---

## 22. 通知管理

### 22.1 模块概述

- **定位：** 管理端配置**通知场景对象**与**推送渠道**（站内信 / 钉钉）：场景系统内置不可自定义新增，仅配置渠道、对象、模板内容、启停状态。
- **面向角色：** 系统管理员/运营人员（侧边菜单「通知管理 → 通知场景」）。
- **与接收端区分（唯一权威）：**
  - **配置端（本模块）：** 通知场景列表 + 编辑弹窗；配置「何时推、推给谁、走哪些渠道、模板文案」。
  - **接收端：** 实际通知内容在 Header **头像左侧铃铛**打开的站内信面板中查看；钉钉渠道另达钉钉客户端。
  - **无独立承接页面：** 不设「通知中心」业务列表页、不设侧边菜单收件箱路由；铃铛为站内信唯一入口。

### 22.2 通知场景配置页

#### 22.2.1 结构

顶部筛选（通知场景[模糊搜]、状态[全部/开启/关闭]）+ 表格。

#### 22.2.2 表格字段

通知场景（只读）、通知渠道（顿号分隔，如"站内信、钉钉"）、通知对象（顿号分隔）、状态（彩色徽章）、操作（编辑）。

### 22.3 编辑通知场景弹窗

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 通知渠道 | 多选 | 是 | 站内信、钉钉（同时启用同时送达，无优先级）；本字段即推送渠道配置 |
| 通知对象 | 多选 | 是 | 角色类型、创建人、分配人、站点运营专员、指定用户 |
| 通知内容 | 富文本 | 是 | **场景模板**（标题、正文），触发后按模板生成送达内容；**不是**铃铛里的历史通知列表 |
| 状态 | 开关 | 是 | 开启/关闭 |

校验：渠道≥1、对象≥1、内容非空；保存立即生效；关闭后不再触发推送。钉钉推送通道凭证在系统管理→集成配置（钉钉应用配置）维护，是否启用见 §23.7；站内信为系统内置通道。

### 22.4 通知场景清单（系统内置）

> 通知场景清单为以下 8 项，系统内置、不可增删。

| 通知场景 | 触发时机 | 默认通知对象 |
| :--- | :--- | :--- |
| 新线索生成 | **AI 自动识别有效询盘并生成线索时**（即时触发） | 当前负责人（业务员个人邮箱=业务员本人；站点主邮箱=运营专员；WhatsApp=绑定业务员） |
| 7 天邮件客户未回复提醒 | 邮件客户来信后超 7 天本方未回复 | 当前负责人 |
| 负责人变更 | 执行变更负责人（含运营专员转派站点主邮箱线索给业务员） | 新负责人、原负责人 |
| 邮件未读数量提醒 | 邮箱存在未读复邮件 | 当前邮箱归属用户 |
| 邮件未读超时提醒 | 邮件未读超时 | 当前邮箱归属用户 |
| 邮件未回超时提醒 | 邮件未回复超时 | 当前邮箱归属用户 |
| 邮件未回数量提醒 | 邮箱存在未读或未回复邮件 | 当前邮箱归属用户 |
| 跟进到期提醒 | 线索下次跟进时间到期（到达/超过今日） | 当前负责人 |

### 22.5 Header 铃铛（站内信接收，不占菜单）

- **入口：** 全局 Header 头像左侧铃铛图标；全部角色可见（本人收到的通知）。
- **形态：** 下拉/浮层面板展示站内信列表（标题、摘要、时间、已读/未读等）；**无独立路由页、无侧边菜单项**。
- **与 §22.2 关系：** 铃铛展示的是按场景配置触发后送达的**实例内容**；场景配置页不展示、不管理历史收件记录。

### 22.6 异常

同一场景仅一份配置；场景内置不可增删；对象含失效用户"通知对象中包含已失效的用户，请重新选择"；推送通道未配置时跳过并记日志。

---

## 23. 系统管理

### 23.1 模块概述

- **定位：** 平台级配置与管理统一入口，含用户/角色/菜单/字典/系统参数/集成配置/系统日志共 7 个二级菜单（「系统参数」以 **Tab** 分登录安全、密码设置、通用参数、业务规则四组维护，见 §23.6）。
- **面向角色：** 系统管理员。

### 23.2 用户管理

#### 23.2.1 表格字段

姓名、登录账号（唯一）、手机号、邮箱、角色、授权站点（多站点显前 2 个+"等 N 个"）、状态、操作（编辑、启用/停用、重置密码）。搜索（关键词、状态、角色、授权站点 + **创建时间范围筛选**，`createTimeStart`/`createTimeEnd`，默认不限制，允许只填开始或只填结束，空值不限制）；创建时间倒序，20 条/页。重置时恢复默认（创建时间=不限制）。

#### 23.2.2 新增/编辑弹窗

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 姓名 | 文本 | 是 | — |
| 登录账号 | 文本 | 是 | 唯一，编辑不可改 |
| 手机号 / 邮箱 | 文本 | 否 | 格式校验 |
| 角色 | 下拉 | 是 | **单选**唯一角色（含预置与自定义，§4.1、§23.3）；用户权限=该角色当前菜单/按钮勾选 + 数据档位 |
| 授权站点 | 下拉多选 | 是 | 用户可访问的数据站点范围（`user_site_permissions`）；数据档=全部时仍建议配置但可见不受限；**预置业务员强制仅单站点**（§4.8）；预置运营/协同/管理员及数据档为负责站点或授权站点（只读）的自定义角色可多站点；为数据权限判定依据（区别于业务对象的"站点"，见 §2.4） |
| 所属团队 | 下拉 | 否 | 用户所属销售团队，可为空；权限预留维度，为空时按授权站点计算数据权限（见 §4.2）；当前版本不提供团队管理入口 |
| 绑定钉钉员工 | 下拉 | 否 | 已同步钉钉员工，用于扫码登录识别；未绑定不可扫码登录 |
| 状态 | 开关 | 是 | 启用/停用 |

#### 23.2.3 操作

新增（自动生成初始密码 123456，；编辑（登录账号不可改）；重置密码（二次确认，恢复 123456）；启用/停用（二次确认）。

#### 23.2.4 停用用户规则

无法登录；名下数据保持原状态（不标记无效/不转移/不冻结）；待跟进/跟进中线索超时按公海回收自动释放；运营专员/系统管理员可手动重新分配/转移（协同人只读不可）；停用用户不出现在分配/转移目标下拉；可重新启用（BR-004e）。

### 23.3 角色管理

> **对齐 §4.6：** MVP 支持角色 CRUD（受保护规则见下）与**菜单/按钮权限勾选**；保存后权限即时生效（无需发版）。权限资源清单见 §4.7；预置角色专用硬约束见 §4.8。角色继承不提供。用户仅可绑定一个角色（§23.2）。

**预置角色：** 系统管理员 / 运营专员 / 业务员 / 协同人（初始化带 §4.3 / §4.7 默认权限包，可改展示名与非保护项）。  
**自定义角色：** 支持新增；须选择数据权限档位（§4.2 四选一）并勾选菜单/按钮权限点。

**内置保护：**
- 预置「系统管理员」：不可删除/停用/降权；**菜单与按钮权限勾选只读不可改**；系统至少保留一名启用的系统管理员用户。
- 其他预置角色：不可删除（可停用需校验无用户引用或先迁移用户）；可调整名称、排序、数据档位、菜单/按钮勾选。
- 自定义角色：可编辑/停用/删除（删除前须无用户引用）。

#### 23.3.1 列表页

工具栏：【新增】、修改、删除、导出；筛选：角色名称/权限字符关键词、状态。

| 字段 | 说明 |
| :--- | :--- |
| 勾选 | 批量修改/删除；系统管理员不可勾选删除 |
| 角色编号 | 系统编号 |
| 角色名称 | 可点进详情/编辑；预置名称可改展示文案 |
| 权限字符 | 角色编码（预置如 `admin` / `supervisor` / `sales` / `collaborator`；自定义创建时填写，建议稳定） |
| 数据权限 | 全部 / 负责站点 / 仅本人 / 授权站点（只读）；与 §4.2 对齐，彩色标签展示 |
| 显示顺序 | 列表排序 |
| 角色状态 | 开关；系统管理员不可停用 |
| 创建时间 | — |
| 操作 | 编辑、删除（受保护规则约束）；**系统管理员行无删除；配权入口进入编辑** |

#### 23.3.2 新增 / 编辑弹窗

| 字段/区域 | 说明 |
| :--- | :--- |
| 角色名称 | 必填 |
| 权限字符 | 必填；预置角色建议勿改编码 |
| 数据权限 | 必填，四档单选（§4.2） |
| 显示顺序 / 状态 | 同列表 |
| 菜单权限 | 树形勾选 §4.7.1 菜单权限点（父子联动可选） |
| 按钮权限 | 按菜单分组勾选 §4.7.2 按钮权限点；**不展示** §4.8 专用能力作为可选项 |
| 系统管理员 | 上述权限勾选区只读展示，不可改 |

保存：校验至少勾选一个菜单权限（系统管理员除外只读）；写入角色-权限关联；**立即生效**（已登录用户刷新权限或重新登录后按新包执行，见 §4.6）。记录操作日志（操作类型=配置）。

### 23.4 菜单管理

> MVP 菜单树与按钮权限点**随版本预置入库**（权威清单 §4.7），**不支持**在后台新增/删除菜单节点或新建权限点。角色侧分配在【角色管理】完成（§23.3）。菜单停用（隐藏）后入口不可见，已配角色权限保留；不可停用导致当前登录管理员失去全部菜单。

#### 23.4.1 列表页（树表）

工具栏：新增（**MVP 不可用**）、修改、保存排序、展开/折叠；筛选：菜单名称/请求地址/权限标识关键词。

| 字段 | 说明 |
| :--- | :--- |
| 选择 | 单选，配合工具栏【修改】 |
| 菜单名称 | 树形缩进；目录可展开/折叠；展示图标 |
| 排序 | 行内数字可改，点【保存排序】生效 |
| 请求地址 | 目录为 `#`；菜单为路由路径（如 `/leads`） |
| 类型 | **目录**（蓝）/ **菜单**（绿）/ **按钮**（可选展示，对应 §4.7.2） |
| 可见 | **显示** / 隐藏（标签展示） |
| 权限标识 | 对齐 §4.7 权限码（如 `lead:view`、`lead:export`） |
| 操作 | 编辑；新增子项/删除（**MVP 不可用**，预置不可删） |

菜单树权威来源对齐《菜单结构》与侧边栏，权限码对齐 §4.7。

> **站点运营数据：** 含按钮权限点 `site-operation-data:import` 等（见 §4.7）；预置默认勾选见 §4.3 / §4.7。

#### 23.4.2 修改弹窗（预置可改范围）

可改：菜单名称、显示顺序、可见。请求地址/权限标识/类型**原则上只读**（与预置种子一致，避免与代码路由脱节）；确需调整须随版本发版同步。**不提供**后台新建按钮权限点。

### 23.5 字典管理

> **权威声明：** 本章为全系统业务枚举/下拉/状态展示名的**唯一配置来源**（业务模块只引用编码、展示名称）；询盘标签（AI 分析标签）**不进字典**（§25）。不提供按站点覆盖。权限：**仅系统管理员**。

#### 23.5.1 治理分层

| 层级 | 说明 | 字典类型（编码示意） |
| :--- | :--- | :--- |
| **业务可配字典** | 可新增/编辑字典项、启停、排序；编码创建后不可改 | 来源渠道 `source_channel`、跟进方式 `follow_method`、跟进阶段 `follow_stage`、客户潜质分级 `customer_potential`、客户关注选项 `customer_focus`、行业 `industry`、国家/地区 `country_region`、客户标签 `customer_tag`、线索手动标签 `lead_manual_tag`、联系角色 `contact_role`、AI 服务商 `ai_provider`、AI 模型 `ai_model` |
| **受控预置字典** | 项不可新增/删除/启停；可改**展示名称、排序**；编码与流转关系受系统保护（§26） | 线索状态 `lead_status`、合同状态 `contract_status` |
| **系统基础字典** | 项不可新增/删除/启停；可改**展示名称、排序**；业务取值由系统写入或固定引用 | 登录方式 `login_method`、操作类型 `operation_type`、录入方式 `entry_method`、AI 业务场景 `ai_business_scenario` |

**禁止：** 管理员新增/停用/删除**字典类型**；一期不做「恢复系统默认」；一期不做展示颜色字段。

#### 23.5.2 页面形态

1. **字典类型列表：** 字典名称、字典编码、更新时间、更新人、操作（编辑类型说明、【字典项管理】）。类型清单系统预置，无【新增字典类型】。
2. **字典项管理：** 进入某类型后维护字典项（名称、编码、排序、状态、父级、扩展属性等，随类型可见字段不同）。
3. 系统内置项显示「系统内置」标识。低频页，暂不强制时间筛选。

#### 23.5.3 字典类型字段

| 字段 | 必填 | 说明 |
| :--- | :---: | :--- |
| 字典名称 | 是 | 如「来源渠道」 |
| 字典编码 | 是 | 全局唯一，**创建后不可修改** |
| 字典说明 | 否 | 使用范围与业务含义 |
| 排序 | 是 | 类型列表展示顺序 |

#### 23.5.4 字典项字段

| 字段 | 必填 | 说明 |
| :--- | :---: | :--- |
| 字典项名称 | 是 | 页面展示文案（可改） |
| 字典项编码 | 是 | 同一字典类型内唯一，**创建后不可修改**；流程/统计/接口认编码 |
| 排序 | 是 | 数字越小越靠前；「其他」类默认靠后（如 999） |
| 是否默认 | 条件 | 同一使用场景最多一个默认项；一期至少 **客户潜质分级** 默认=`potential`（潜在客户） |
| 是否系统内置 | 是 | 初始化标识；内置项不可物理删除 |
| 是否允许删除 | 是 | 按类型性质与引用情况控制 |
| 状态 | 条件 | 启用/停用；**受控/基础字典项无启停**（恒为启用） |
| 父级字典项 | 否 | 支持最多 **2 级**父子（父→子）；见 §23.5.5 |
| 备注 | 否 | 管理说明 |
| 展示分类 | 否 | **仅客户关注选项**：商务/交付/产品/质量/供应能力/服务/合规/其他等，**仅 UI 分组**，不形成父子 |

**跟进阶段扩展属性（一期）：**

| 扩展属性 | 类型 | 说明 |
| :--- | :--- | :--- |
| 是否允许转客户 | Boolean | 处于该阶段是否允许人工执行【转客户】；**仅允许触发，绝不自动转化**（BR-038b） |
| 是否计入高意向 | Boolean | 是否计入工作台/销售经营「高意向线索」等指标；**不得**用阶段名称硬编码判断 |

> 「是否为终止阶段」、展示颜色：**一期不做**。  
> **「跟进中」属线索状态（§26.1），不是跟进阶段**，二者不混用。跟进阶段取值由字典维护，业务模块不写死固定枚举。

#### 23.5.5 父子层级规则（一期）

- 字典项通用支持最多 **2 级**（父→子）。预置行业/国家/地区等仍为**扁平**；管理员可自行加父子，不强制多级。
- **AI 服务商 → AI 模型**：模型必须挂在某一服务商下；AI 能力表单先选服务商再选模型（联动）。
- **业务表单选型（非 AI 联动）：** 仅可选**叶子**；无子项的节点视为叶子可选；为其挂上子级后，原父级不可再选（仅子级可选）。
- **历史数据：** 若业务已保存父级值后该节点下挂子级，历史值**原样保留展示**；再编辑该字段时只能改选叶子，**不强制迁移**。

#### 23.5.6 管理与引用规则

1. 字典编码、字典项编码创建后不可修改。
2. 系统内置项不可物理删除；自定义项未被业务引用可删；**已被引用只能停用、不可删除**。
3. **停用：** 新增数据下拉隐藏不可选；存量历史数据仍正常展示原名称。
4. 同一字典内编码不可重复；默认不允许重名。
5. 「其他」类排序靠后。
6. 所有新增/编辑/启停/删除写入**操作日志**（§23.9、BR-054）；配置变更独立 Tab 仍为二期。
7. 客户标签、线索手动标签与 AI 询盘标签分存分展，不得混用（§25）。
8. **AI 服务商/模型停用：** 已引用的 AI 能力记录保留，但视为不可用（绑定场景按 §21.5/§21.6 降级）；不自动改能力启停状态；编辑能力时不可再选已停用项。

#### 23.5.7 字典类型总表与预置项

| 字典类型 | 层级 | 权威取值 / 说明 |
| :--- | :--- | :--- |
| 来源渠道 | 业务可配 | 预置：`email` 邮件、`website_inquiry` 官网询盘、`organic_inquiry` 自然询盘、`whatsapp` WhatsApp、`exhibition` 展会、`customer_referral` 客户转介绍、`other` 其他。消息自动写入仅邮件/WhatsApp；**「手动」不是来源渠道**（见录入方式） |
| 录入方式 | 系统基础 | 仅改名/排序。预置：`manual_create` 手动新建、`batch_import` 批量导入、`inquiry_conversion` 询盘转入、`api_sync` API 同步、`system_generated` 系统生成。仅**线索**落库；写入映射见下 |
| 跟进方式 | 业务可配 | `phone` 电话、`email` 邮件、`whatsapp` WhatsApp、`meeting` 会议、`online_meeting` 线上会议、`offline_visit` 线下拜访、`note` 备注、`other` 其他 |
| 跟进阶段 | 业务可配 | 见下表（含扩展属性） |
| 客户潜质分级 | 业务可配 | `high_potential` 高潜客户、`potential` 潜在客户（**新建默认**）、`general` 一般客户；仅人工维护（§26.3） |
| 客户关注选项 | 业务可配 | 见下表（含展示分类）；完整预置 |
| 行业 | 业务可配 | 预置扁平：工业制造、机械设备、电子电器、汽车配件、玩具礼品、家居用品、纺织服装、五金工具、医疗器械、化工原料、农副产品、其他 |
| 国家/地区 | 业务可配 | 预置扁平：中国、美国、加拿大、墨西哥、德国、英国、法国、意大利、阿联酋、日本、韩国、印度、巴西、澳大利亚、其他；线索「来源国家/地区」同源 |
| 客户标签 | 业务可配 | 重点客户、高潜客户、复购客户、长期合作客户、待维护客户、价格敏感客户、新品客户、已流失风险客户（§25.3） |
| 线索手动标签 | 业务可配 | 北美市场、欧洲市场、东南亚市场、中东市场、拉美市场、老客户介绍、展会线索、大额采购、样品优先、复购询盘、紧急订单、价格敏感（§25.2） |
| 联系角色 | 业务可配 | `decision_maker` 决策人、`purchasing_manager` 采购经理、`key_contact` 关键联系人、`execution_contact` 执行联系人、`technical_contact` 技术联系人、`financial_contact` 财务联系人、`other` 其他 |
| 线索状态 | 受控预置 | 编码与流转见 §26.1；可改展示名/排序 |
| 合同状态 | 受控预置 | 编码与流转见 §26.2；可改展示名/排序 |
| 登录方式 | 系统基础 | 预置对齐 §5：账号密码、钉钉扫码；仅改名/排序 |
| 操作类型 | 系统基础 | 预置对齐 §23.9：新增/编辑/删除/导出/导入/登录/配置；仅改名/排序 |
| AI 业务场景 | 系统基础 | 四场景固定（编码不可增删启停），仅改展示名/排序：`mail_intent` 邮件意向分析、`wa_intent` WhatsApp 意向分析、`company_extract` AI 自动提取企业信息、`batch_refine` 批量 AI 提炼；业务关联与互斥仍见 §21.6 |
| AI 服务商 | 业务可配（父） | 预置：OpenAI/Azure OpenAI、通义千问、智谱 AI、DeepSeek、豆包、月之暗面、文心一言；**可新增服务商**；**不保留「自定义」**；未预置须先增字典再选用（§21.3） |
| AI 模型 | 业务可配（子） | 必须属于某一服务商；可在已有服务商下新增模型；联动预置模型名沿用 §21.3 表 |

**录入方式写入映射（系统自动，用户不可改）：**

| 线索创建路径 | `entry_method` |
| :--- | :--- |
| 列表【新增线索】 | `manual_create` |
| 线索批量导入 | `batch_import` |
| 邮件/WhatsApp AI 自动生成 | `inquiry_conversion` |
| 邮件/WhatsApp 人工兜底【生成线索】 | `inquiry_conversion` |

**批量导入 · 来源渠道：** 模板填写了来源渠道则按导入值；**未填写则留空**（展示 `—`），不得写「手动」；录入方式固定 `batch_import`。

**跟进阶段预置（扩展属性默认）：**

| 编码示意 | 名称 | 是否允许转客户 | 是否计入高意向 |
| :--- | :--- | :---: | :---: |
| `awaiting_first_response` | 待首响 | 否 | 否 |
| `contacted` | 已联系 | 否 | 否 |
| `need_confirmed` | 需求确认 | 可配置（默认否） | 否 |
| `sampling` | 打样阶段 | 是 | 是 |
| `quotation` | 报价阶段 | 是 | 是 |
| `negotiation` | 谈判阶段 | 是 | 是 |

**客户关注选项预置：**

| 编码 | 名称 | 展示分类 |
| :--- | :--- | :--- |
| `price_quotation` | 价格与报价 | 商务 |
| `minimum_order_quantity` | 最小起订量 | 商务 |
| `payment_terms` | 付款方式与账期 | 商务 |
| `delivery_time` | 交货周期 | 交付 |
| `logistics` | 物流与运输 | 交付 |
| `product_specification` | 产品规格与参数 | 产品 |
| `sample` | 样品与打样 | 产品 |
| `customization` | 定制/OEM/ODM | 产品 |
| `quality_standard` | 质量标准 | 质量 |
| `qualification_certificate` | 企业资质与产品认证 | 质量 |
| `production_capacity` | 生产能力 | 供应能力 |
| `after_sales_service` | 售后服务 | 服务 |
| `compliance` | 合规与可持续要求 | 合规 |
| `other` | 其他 | 其他 |

### 23.6 系统参数

> **页面形态（唯一权威）：** 「系统参数」为单一二级菜单，页内以 **Tab 页签** 分组维护，**禁止**把全部参数堆在同一滚动页展示。  
> **Tab 清单（固定顺序）：** ① 登录安全（§23.6.1）② 密码设置（§23.6.2）③ 通用参数（§23.6.3）④ 业务规则（§23.7）。  
> 切换 Tab 仅切换当前组参数视图，互不混排；默认进入「登录安全」Tab。多数参数立即生效；session 相关及强制改密下次登录生效；变更记录系统日志。  
> **转派参考匹配键**（「站点 + 客户原始发件邮箱」）为代码内置规则，**不在系统参数页展示或配置**，口径见 §7.5.2。

#### 23.6.1 Tab「登录安全」

| 参数名称 | 参数键 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 登录超时时长 | session_timeout | 1440 分钟 | 无操作自动退出时长（下次登录生效）|
| 登录失败锁定次数 **【二期/延后】** | login_fail_lock_threshold | 5 次 | MVP 不做；二期：连续登录失败达该次数锁定账号；0 表示不锁定（见 §5.4）|
| 账号锁定时长 **【二期/延后】** | login_lock_duration | 30 分钟 | MVP 不做；二期：触发锁定后禁止登录的时长；期满自动解锁，系统管理员可提前重置密码解锁 |
| 单端登录限制 **【二期/延后】** | single_login | 关闭 | MVP 不做；二期：开启后同一账号仅允许单设备在线 |
| 会话刷新间隔 **【二期/延后】** | session_refresh_interval | 5 分钟 | MVP 不做；下次登录生效 |

#### 23.6.2 Tab「密码设置」

| 参数名称 | 参数键 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 密码最小长度 | password_min_length | 8 位 | — |
| 密码复杂度 | password_complexity | 数字+大小写字母 | — |
| 首次登录强制改密 **【二期/延后】** | first_login_change_password | 关闭 | MVP 不做，默认关闭（与 §5.4 一致）；二期支持系统管理员手动开启，开启后下次登录生效 |


#### 23.6.3 Tab「通用参数」

| 参数名称 | 参数键 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| 列表默认每页条数 | list_page_size | 20 条 | — |
| 单次导出上限 | export_max_rows | 10000 条 | — |


### 23.7 Tab「业务规则」（属系统参数页）

> 与 §23.6 同属「系统参数」二级菜单下的 **第四个 Tab**，配置业务规则类开关与参数；**不**单独占二级菜单。

| 规则 | 当前配置 | 说明 |
| :--- | :--- | :--- |
| 未跟进自动回收天数 **【二期/延后】** | 14 天 | MVP 不做；二期：待跟进/跟进中超时自动回收公海（公海回收最高优先级，见 10.7）|
| 渠道定义规则 | 系统默认 | 获客分析消息渠道分类映射（邮件/WhatsApp）|
| 客户活跃度判定天数 | 90 天 | 客户在该天数内有跟进/消息互动记为"活跃"，超过记为"沉默"；用于客户经营·客户活跃度分析（见 §19.3、§19.4）|
| AI 功能启用 | 开启 | AI 总开关；关闭后消息意向分析（邮件意向分析、WhatsApp 意向分析）、AI 自动提取企业信息等全部停用，不自动识别询盘、不自动创建线索，转人工兜底创建（BR-015）|
| 钉钉推送启用 | 开启 | 关闭后不再发送钉钉推送（推送通道见 §23.8.2）|

编辑弹窗：规则名称/说明只读，当前配置按类型（开关/数字 1-365/下拉）动态变化；开关立即生效无二次确认，记录系统日志。

### 23.8 集成配置

Tab 切换外部服务接入配置：邮箱服务、钉钉应用、文件存储。

> **WhatsApp 接入口径：** 企业 whatsapp 二期实现；当前系统内仅提供业务员个人中心 WhatsApp 绑定入口（§24.3.2）及会话收发能力。

#### 23.8.1 Tab1 邮箱服务配置

**定位：** 全系统共用的**全局邮件协议**配置；多邮箱账号复用本页参数收发/拉取。

| 字段 | 必填 | 说明 |
| :--- | :---: | :--- |
| IMAP 服务器 / 端口 / SSL | 是 | 收件协议 |
| SMTP 服务器 / 端口 / SSL | 是 | 发件协议 |
| 拉取间隔（秒） | 是 | 定时拉取间隔，默认 1800 |


操作：保存、同步。MVP 仅维护**单一全局协议配置**。

#### 23.8.2 Tab2 钉钉应用配置

AppKey（必填）、AppSecret（必填）、扫码回调地址（必填）、企业 CorpId（可选）、启用状态。支撑扫码登录与消息推送。操作：保存、同步钉钉员工（写入候选表供用户管理绑定）、测试扫码。

> **推送通道口径：** 钉钉推送复用本 Tab 凭证（钉钉机器人 Webhook）；站内信为系统内置通道（保留天数默认 30 天）。钉钉推送是否启用见 §23.7；钉钉与站内信同时启用同时送达、无优先级。

#### 23.8.3 Tab3 文件存储

**定位：** 全系统文件存储位置的全局配置；**同一时间仅可选用一种**存储类型。生效范围：全系统业务附件（邮件/WhatsApp/跟进/合同等上传文件）及导入/导出等临时文件，统一走当前选用存储。权限：仅系统管理员。

| 字段 | 必填 | 说明 |
| :--- | :---: | :--- |
| 存储类型 | 是 | 单选：本地磁盘 / 华为云 OBS；全局二选一 |
| Endpoint | 条件必填 | 仅存储类型=华为云 OBS 时展示；如 `obs.cn-southwest-2.myhuaweicloud.com` |
| Bucket | 条件必填 | 仅 OBS 时展示；存储桶名称 |
| Access Key | 条件必填 | 仅 OBS 时展示 |
| Secret Key | 条件必填 | 仅 OBS 时展示；**已配置时留空表示不修改** |
| Region | 否 | 仅 OBS 时展示；如 `cn-southwest-2` |
| 全局前缀 | 否 | 仅 OBS 时展示；对象键前缀，如 `crm-prod/` |

**本地磁盘：** 选中后无额外表单项；本地目录由部署环境决定，不在本页配置。

**操作：** 保存；存储类型=华为云 OBS 时另提供【测试连接】（**可选**，未测试也可保存）。

> **AI 服务接入** 在【AI 能力管理】维护（见 §21），不在集成配置中；AI 业务场景调用条件：§23.7「AI 功能启用」开启 + 该场景存在绑定且启用的 AI 能力（§21.6），否则不调 AI、不自动识别、转人工兜底创建线索（BR-015）。

### 23.9 系统日志

> **MVP 保留「登录日志、操作日志」2 Tab**；`【二期/延后】`「配置变更」Tab 延后（配置变更事件仍写入操作日志兜底，见操作类型"配置"）。

标签页：登录日志、操作日志（**【二期/延后】** 配置变更）。**各 Tab 分别使用不同时间筛选名称，不统一写成含义不清的"时间"**；默认时间范围均为**近 7 天**。

- **登录日志：** 时间（按**登录时间**筛选，`loginTimeStart`/`loginTimeEnd`，默认近 7 天）、账号、姓名、IP、方式、结果、浏览器、操作系统（BR-055）。
- **操作日志：** 时间（按**操作时间**筛选，`operateTimeStart`/`operateTimeEnd`，默认近 7 天）、操作人、操作类型（新增/编辑/删除/导出/导入/登录/配置）、业务对象、对象名称、操作内容、IP（BR-054）。
- **【二期/延后】配置变更：** 时间（按**变更时间**筛选，`changeTimeStart`/`changeTimeEnd`，默认近 7 天）、操作人、变更类型、配置项、变更前、变更后、IP（敏感字段脱敏）。

> 各 Tab 时间筛选独立、互不混用；允许只填开始或只填结束；空值不限制该时间条件；重置时恢复该 Tab 默认时间范围（近 7 天），不改变当前 Tab 选择。

导出：各 Tab 均支持，当前筛选条件，CSV，上限 10000 条，异步执行完成后站内信通知。保留 1 年，超期自动物理清理。脱敏：密码→`****`，API Key/Token→`sk-****xxxx`，身份证/银行卡→前 4 后 4，邮箱/手机默认不脱敏。

### 23.10 系统级状态页

系统级状态页（404/403/500/Loading/维护中/网络异常）唯一权威定义见《全局异常处理规范》§29.6，本模块不再单列。

---

## 24. 个人中心

### 24.1 模块概述

- **定位：** 维护当前用户个人信息、账号绑定、密码修改。
- **面向角色：** 全部角色。
- **MVP：** 弹窗形态承载"我的资料（只读）+ 账号绑定 + 修改密码"；独立页面化布局延二期。

### 24.2 我的资料

字段：头像（jpg/png，建议 200×200，≤2MB）、姓名、手机号、邮箱（二期可编辑保存）。保存成功提示"个人信息已更新"。

### 24.3 账号绑定

#### 24.3.1 邮箱绑定（支持多个）

- **面向角色：** **业务员**（可绑定多个个人邮箱，收发处理本人邮件）与**运营专员**（可绑定多个邮箱，当其被绑定为某站点运营人员时，可由系统管理员在【站点管理·新增/编辑】选定其一为该站点主邮箱）均可在个人中心绑定邮箱账号；**协同人不绑定邮箱**（严格只读）。
- 新增绑定、编辑绑定、删除绑定（二次确认）。**个人中心邮箱绑定不选择站点**；站点主邮箱由系统管理员在【站点管理·新增/编辑】通过「为站点绑定运营人员并选定其业务邮箱」的方式确定（见 §20.5）。
- **协议与凭据分工：** 主机/端口/SSL/拉取间隔取自【集成配置 → 邮箱服务】全局协议（§23.8.1）；**每个绑定邮箱单独填写密码/授权码**，不可共用全局主账号。

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 邮箱账号 | 输入框 | 是 | 绑定邮箱 |
| 密码/授权码 | 密码框 | 新增必填；编辑选填 | 该邮箱独立凭据；编辑时留空表示不修改；界面不回显明文；存储脱敏 |
| 关联站点| 下拉选择 | 新增必填；编辑选填 | 选项来自用户已授权的站点， |
| 绑定状态 | 系统字段 | 是 | 已绑定 / 已解绑 |

**规则：**

- 业务员 / 运营专员均可绑定**多个**邮箱账号；邮箱**归属绑定用户**；**一邮箱一凭据**。
- 收发与定时拉取使用「§23.8.1 全局协议 + 本绑定密码/授权码」；**不做「测试连接」**。
- **业务员个人邮箱：** 有效询盘线索直接归业务员本人（站点=业务员唯一授权站点，见 §7.10、§27.1）。
- **运营专员被绑定为站点运营人员、其业务邮箱设为站点主邮箱后（一站点一主邮箱）：** 一个业务邮箱只能作为一个站点的主邮箱、一个站点只能有一个主邮箱；同一邮箱**不可重复作为多个站点主邮箱**（不可跨站点/跨用户重复）；站点主邮箱线索归该运营人员、转派业务员（见 §20.5、§27.1）。
- 校验：邮箱格式合法、不可重复绑定、同一邮箱不可被多用户绑定；新增须填写密码/授权码。列表字段：邮箱地址、类型（个人邮箱 / 站点主邮箱）、对应站点（站点主邮箱展示）、绑定时间、操作（编辑/删除）；列表**不展示**密码/授权码。

#### 24.3.2 WhatsApp 绑定（仅一个）

- **面向角色：** **仅业务员**可绑定 WhatsApp；运营专员/协同人/系统管理员**不支持**绑定 WhatsApp（见 §4.5、§8.1）。非业务员不展示 WhatsApp 绑定入口。
- 绑定（扫码绑定）、解绑（二次确认）。校验：账号非空、不可被多用户绑定。

**规则：**

- 一个业务员仅允许绑定**一个** WhatsApp 账号。
- WhatsApp 只绑定业务员，**不绑定固定站点**；因业务员恒为单一授权站点，WhatsApp 线索站点 = 业务员唯一授权站点（见 §8.5.2、§27.1）。

### 24.4 修改密码

字段：新密码（≥8 位、含数字和大小写字母）、确认新密码（与新密码一致）。校验对应提示；成功后提示"密码已修改，请重新登录"并自动退出跳登录页。

### 24.5 异常

引用 §29.3、§29.5。模块特有：头像格式/大小超限提示；邮箱/WhatsApp 已被绑定提示；两次密码不一致拦截。

---

## 25. 标签体系

> 本章为全系统**标签体系唯一权威定义**。人工标签选项由字典管理维护（见 §23.5），AI 分析标签不进字典；业务模块只引用不重复定义。全系统标签统一为**三层**：询盘标签 / AI 分析标签（消息 AI 输出）→ 线索标签（业务使用）→ 客户标签（客户资产，人工独立）。

### 25.1 标签分类总览

| 标签类型 | 字段 | 归属/存储 | 来源 | 维护方式 | 同步规则 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 询盘标签（AI 分析标签） | `ai_analysis_tag` | 消息分析层，AI 面板「询盘标签」展示 | AI 分析生成 | AI 生成（面板只读） | 有效询盘生成线索时追加写入线索标签；同名去重；人工删过的同名禁止再追加；不进客户；不进字典 |
| 线索标签 | `lead_tag` | 线索 | AI（来自询盘标签）+ 人工 | 业务员增/改/删 | 追加不覆盖人工；转客户不写入客户 |
| 客户标签 | `customer_tag` | 客户 | 仅人工 | 客户管理增/删/改 | 独立维护；不继承线索标签；不受 AI 影响 |

> 人工标签（线索标签的人工部分、客户标签）选项由字典管理维护，业务侧新输入同步写回字典；询盘标签（AI 分析标签）不进字典。全系统不单设"标签管理"菜单。

### 25.2 线索标签

- **来源（两类，混排为统一业务标签）：** ① AI 来源——询盘标签在有效询盘生成线索时自动写入/追加（蓝底+圆角+AI 图标）；② 人工——灰底+圆角，选项来自字典线索域→线索手动标签。
- **AI 追加策略（BR-016a）：** 生成线索（或同线索下后续有效消息写入标签）时，询盘标签**追加**到线索标签；**同名（完全一致）去重**；**不删除**已有标签；**不覆盖**人工修改；**人工删除过的同名标签禁止 AI 再追加**（对齐 BR-017）。MVP 无消息「重新分析」入口。
- **人工维护：** 支持新增/修改/删除（AI 来源、人工来源标签均可增删改）；人工修改后视为业务标签，AI 不再覆盖。
- 展示：每线索最多 3 个，超出"+N"（悬停全部）；点击标签快速筛选；筛选区多选逻辑=OR。
- 热门标签：站点下使用频率最高 Top10，仅展示未被当前线索添加的；操作详见 13.5。
- 标签输入：中英文/数字/下划线，≤20 字，同线索不可重复。

### 25.3 客户标签

仅人工标记，无 AI 标签，选项由字典客户域→客户标签维护。**独立于线索标签：线索转客户不自动继承线索标签（BR-016b），且不受消息 AI / 询盘标签追加影响**。维护入口为客户列表【新建客户】/【编辑客户】（**不提供列表独立【打标签】弹窗**）；客户详情仅只读展示同一份标签数据。超 3 个"+N"，悬停全名。操作详见 §14.7.1、§14.7.2。

### 25.4 询盘标签（消息 · AI 分析标签）

- **定义：** AI 对消息内容分析输出的标签（如价格敏感、关注某产品、采购意向强、交期关注），展示于 AI 智能分析面板「询盘标签」（只读），不是长期业务管理标签。
- **写入线索（BR-016a）：** 有效询盘生成线索时自动追加为线索标签的 AI 来源部分；同名去重；人工删过的同名禁止再追加；不覆盖人工标签。
- **不作为客户资产标签：** 不进客户中心、不进字典；客户标签始终人工维护。

---

## 26. 状态流转

> 本章为全系统**状态机唯一权威定义**（线索/合同/客户潜质/站点/用户）。业务模块只展示当前状态并引用本章，不重复描述状态流转。

### 26.1 线索状态机

> **线索状态机与跟进阶段分别维护、互不映射。** 线索状态描述线索的**业务生命周期**；跟进阶段（见 §23.5）描述销售**推进过程**；"转客户"是业务人员**人工主动动作**（见 §13.6），三者不混用。跟进阶段不映射为线索状态，也不自动改变线索状态。

**状态值：** 待分配（公海专用）、待跟进、跟进中、已转客户、已成交、无效、丢失。

**核心流转链路：** 待跟进 → 跟进中 → 已转客户 → 已成交；**已成交可因关联合同全部失效回退为已转客户**（见 BR-056）。

**状态含义与操作角色：**

| 状态 | 含义 | 进入条件 | 操作角色 |
| :--- | :--- | :--- | :--- |
| 待分配 | 公海池中等待分配的线索 | 公海回收（自动/手动）或标记异常后保留公海 | 运营专员 / 系统管理员（协同人只读不可分配） |
| 待跟进 | 已归属负责人、尚未开始跟进 | 渠道生成线索 / 手动新建 / 公海分配 | 负责人本人 |
| 跟进中 | 负责人已开始跟进推进 | 首次录入跟进（BR-038a） | 负责人本人 |
| 已转客户 | 已沉淀为客户资产、线索保留 | 人工主动执行【转客户】成功（BR-038b/033）；或已成交因关联合同全部失效回退（BR-056） | 负责人本人 / 运营专员（协同人只读不可）；回退由系统执行 |
| 已成交 | 已有关联合同且状态=已签约 | 录入/导入已签约合同并关联本线索（BR-056；不要求原状态必须为已转客户） | 由合同动作驱动 |
| 无效 / 丢失 | 异常终态（无跟进价值/流失） | 标记异常 | 负责人本人 / 运营专员（协同人只读不可） |

> **禁止主动回退：** 禁止人工将已转客户→跟进中/待跟进、跟进中→待跟进等；**公海回收（→待分配）为异常回收机制、非链路回退**（见 §10.7）。**唯一允许的成交回退：** 关联合同作废为失效且该线索下已无其他已签约合同时，系统将已成交→已转客户（BR-056）。数据权限口径见 §4.2。

| 当前状态 | 可流转至 | 触发动作 |
| :--- | :--- | :--- |
| 待分配 | 待跟进 | 公海池分配 |
| 待分配 | 无效 / 丢失 | 标记异常（保留在公海池列表展示） |
| 待跟进 | 跟进中 | 首次录入跟进（BR-038a） |
| 待跟进/跟进中 | 已转客户 | 人工主动执行【转客户】成功（BR-038b、BR-033） |
| 待跟进/跟进中 | 无效 / 丢失 | 标记异常 |
| 待跟进/跟进中 | 待分配 | 公海回收（自动/手动） |
| 待跟进/跟进中/已转客户 | 已成交 | 录入/导入已签约合同并关联本线索（BR-056） |
| 已转客户/无效/丢失 | 待分配 | 公海**手动**回收（除"已成交"外均可，自动回收不触发） |
| 已转客户 | 无效 / 丢失 | 标记异常 |
| 无效 / 丢失 | 已转客户 | 重新激活并执行【转客户】成功 |
| 已成交 | 已转客户 | 关联合同作废为失效，且该线索下已无其他已签约合同（BR-056，系统执行并写线索跟进记录） |
| 已成交 | — | 不可再次转客户；仍可录入跟进/更新阶段；不可公海回收 |

**关键规则：**
- **不自动转化：** 进入打样/报价/谈判等阶段仅表示"允许触发"（转客户规则），是否转化由人工主动执行；系统不因阶段变化自动创建客户。
- **转客户准入** = 待跟进/跟进中/无效/丢失（"已成交""已转客户""待分配"不可）；转化成功→"已转客户"（客户/客户联系人已沉淀、保留原线索）。
- **已成交**由「录入已签约合同且关联本线索」驱动（BR-056），不由转客户动作直接产生；原线索即使尚未转客户，关联合同后也可直接置已成交。
- **合同失效回退：** 仅当合同有关联线索、该线索当前=已成交、且该线索下已无其他已签约合同时，回退为已转客户；一线索多合同且仍有已签约合同则保持已成交；未关联线索的合同作废不影响任何线索；只动本条关联线索。
- **公海回收：** 手动回收适用除"已成交"外全部状态（待跟进/跟进中/已转客户/无效/丢失）；**【二期/延后】** 自动回收（超时未跟进）仅针对待跟进/跟进中（MVP 不做，见 §10.7）；"已成交"不回收。
- **公海回收 ≠ 状态回退：** 已转客户经手动回收进入"待分配"是**异常回收机制**（数据回到公海重新分配，见 §10.7），不属于跟进链路回退。
- 已成交/已转客户录入跟进仅追加记录（已成交回退除外由系统改状态）；转客户为必然成功动作，异常按系统异常处理（BR-033）。

### 26.2 合同状态机

**状态值：** **已签约**、**失效**。录入默认「已签约」，无「待签约 / 执行中 / 已完成 / 已终止 / 已作废」。无「合同结束时间」字段。

| 当前状态 | 允许流转至 | 触发 |
| :--- | :--- | :--- |
| 已签约 | 失效 | 更多→【作废】；或【编辑合同】将状态改为失效（均须填原因） |
| 失效 | 已签约 | 【编辑合同】将状态改回已签约（关联线索按 BR-056 置已成交） |

> 改为失效须填原因；合同中心不提供删除（BR-050）。线索联动见 BR-056、§16.6.2、§16.6.3。编辑时合同编号 / 客户 / 关联线索只读，其余字段可改（§16.6.2）。

### 26.3 客户潜质分级（设计说明）

**CRM 定位为客户资料库，客户不设独立"客户状态"字段**（一客户可绑多线索，各线索状态独立，不宜聚合成单一客户生命周期状态）；**系统不设"潜在客户/合作中/流失"等客户生命周期状态**。

经营情况由 **客户潜质分级** 反映：

- **字典维护：** 取值来自字典「客户域 → 客户潜质分级」（§23.5：高潜客户 / 潜在客户 / 一般客户）；选项由系统管理员在字典管理中维护，业务表单以下拉引用编码、展示名称。
- **新建默认：** 未选择时默认「潜在客户」（`potential`，字典项「是否默认」）。
- **仅人工维护：** 在【新建客户】/【编辑客户】中由业务人员选择或调整；**系统不因线索状态、合同签约、跟进行为等自动变更**。
- **入口：** 仅客户新建/编辑表单中的「客户潜质分级」字段；不单列独立「调整潜质」操作。
- **潜质分级 ≠ 客户状态：** 仅表示经营价值/跟进优先级判断，不代表客户生命周期阶段。
- **不展示「关联线索状态分布」：** 客户详情不以线索状态计数作为客户经营标识；线索过程仍可在「线索记录」页签按条查看。

### 26.4 站点状态机

启用 ↔ 停用。站点不允许物理删除仅停用（BR-009）；停用停止接收新消息、暂停公海自动回收，历史数据保留、已有业务可继续；停用站点在筛选下拉仍可见（带「已停用」）。连锁影响见 20.6。

### 26.5 用户状态机

启用 ↔ 停用。用户不允许物理删除仅停用（BR-004e）；停用后无法登录、名下数据保持原状态；停用用户待跟进/跟进中线索超时按公海回收释放，可手动重新分配/转移；不可作为分配/转移目标。连锁影响见 23.2。

> 消息不设独立状态机：消息接收 → AI 识别有效询盘 → **有效则自动生成线索（主流程）**；AI 不可用/无法识别时人工兜底生成；无效消息不生成。相关规则见 §21.8、§27.1、第 28 章。

---

## 27. 数据流转与同步规则

> 本章为**业务对象生命周期与数据流转唯一权威定义**（消息→线索→客户→合同 的创建来源、创建条件、归属规则、同步规则、状态变化影响）。业务模块只描述入口与页面行为，数据流转以本章为准；AI 字段级同步以 §21.4 为准。

### 27.1 线索生成与归属

- **生成时序（主流程）：** 消息 → **AI 分析识别有效询盘**（标准见 §21.8）→ 自动提取业务信息 → **自动创建线索** → 绑定负责人 → 进入线索中心；无效消息不生成线索。AI 不可用时消息保留、转人工兜底创建（见 §21.5、§21.8）。
- **站点归属口径：** 站点（`site_id`）= 线索数据归属、权限判断、经营/获客分析的统一依据；**线索创建即有明确站点**。
- **负责人归属（两类邮箱）：** ① **业务员个人邮箱**线索负责人 = **业务员本人**，直接进入本人线索列表；② **站点主邮箱（运营人员业务邮箱）**线索负责人 = **该站点绑定的运营人员**，由运营人员点击【转派给业务员】一步完成投递与负责人变更（T1–T3、BR-053，见 §7.5.2）、不进公海；WhatsApp 线索负责人 = WhatsApp 绑定业务员；人工兜底生成归属当前操作人（BR-008）。
- **负责人异常兜底：** 站点主邮箱对应运营专员**已停用/缺失**、业务员个人邮箱对应业务员停用、或 WhatsApp 绑定业务员**已停用/不存在**时，**线索仍然创建**（负责人为空、状态"待分配"、进入公海池），由运营专员 / 系统管理员分配（协同人只读不可）；**禁止因负责人无法匹配导致线索创建失败**。

**邮件线索 — 业务员个人邮箱（AI 自动生成）：**

```text
来源渠道 = 邮件
录入方式 = inquiry_conversion（询盘转入）
站点 = 该邮箱对应站点（业务员唯一授权站点）
负责人 = 业务员本人
线索状态 = 待跟进
进入位置 = 本人线索列表（无需转派）
```

**邮件线索 — 站点主邮箱（运营人员业务邮箱，AI 自动生成）：**

```text
来源渠道 = 邮件
录入方式 = inquiry_conversion（询盘转入）
站点 = 站点主邮箱对应站点（mail_site_id，新增/编辑站点时绑定运营人员选定其业务邮箱）
负责人 = 该站点绑定的运营人员（运营点击【转派给业务员】一步完成投递与负责人处理，T1–T3，不进公海，BR-053）
线索状态 = 待跟进
进入位置 = 线索列表
```

**WhatsApp 线索（AI 自动生成，仅业务员）：**

```text
来源渠道 = WhatsApp
录入方式 = inquiry_conversion（询盘转入）
站点 = 业务员唯一授权站点
负责人 = WhatsApp 绑定业务员
线索状态 = 待跟进
进入位置 = 线索列表
```

- 邮件：每封邮件独立生成线索（BR-021，**入库不做按发件邮箱合并**）、同封多需求合并（BR-023）；关闭 AI 由人工维护；无效消息可人工转线索（BR-008/BR-038）。站点未绑定运营人员/主邮箱时其邮件不参与自动生成线索。转派参考匹配键（站点 + 客户原始发件邮箱）**代码写死**，仅用于 T2/T3 选人提示，见 §7.5.2；不进系统参数页。
- WhatsApp：仅业务员可绑（见 §24.3.2），**不绑定固定站点**；线索站点 = 业务员唯一授权站点，创建即明确、不进公海池。

### 27.2 AI 信息同步

详见 §7.9、§21.4。客户画像落询盘联系人、首次/存量/人工优先（BR-016c）；转客户时客户无画像才写入、有则不覆盖；国家/地区与所属行业从工商画像预填客户标准字段（新建预填、关联仅补空，字典匹配不上留空，可人工改）（BR-036）；意向总结不进线索/客户；客户仅人工标签；年营收不写入客户标准字段（BR-017）。

### 27.3 联系人归集（BR-035）

**询盘联系人 → 客户联系人**：手动关联线索时归集；**转客户动作触发时同步归集**（创建客户联系人）。询盘联系人转客户成功后，归集为该客户下的客户联系人；若客户名称来源于询盘联系人姓名，该联系人仍作为客户联系人保存，**不**额外创建独立个人客户联系人对象。保持：客户联系人必须归属于一个客户；不允许独立联系人；不允许跨客户共享联系人。

1. **去重边界：** 仅同一客户内去重；跨客户（含跨站点）不去重；客户匹配范围限当前线索站点（BR-034，按站点 + 客户名称精确匹配客户）。**一个客户可拥有多个联系人**。
2. **匹配字段（关联 or 新建）：** 关键资料（邮箱 / 手机号 / 姓名）与客户已有联系人**一致**则**自动关联已有联系人**；关键资料**存在冲突（不一致）**则**创建新的客户联系人**；关键资料均缺失直接新增。
3. **合并策略：** 关联已有时合并（以客户已有为主，补充缺失字段不覆盖非空）；无匹配或关键资料冲突则新增；归集的客户联系人保留「AI 识别」标记（只读）。
4. **主要联系人：** 归集不自动变更客户已有主要联系人，新归集默认非主要。
5. **询盘联系人影子对象（BR-012）：** 消息进入时创建询盘联系人影子对象（尚未归集为客户联系人），已有同源则关联；手动关联线索时按上述规则归集为客户联系人。

### 27.4 客户负责人变更同步

- 变更后客户后续新生成线索**固定**自动分配新负责人，历史线索不变（无系统参数开关，代码写死）。

### 27.5 合同归属

新增合同负责人默认归属**客户当前负责人**（无系统参数开关，代码写死）；导入合同时同口径。

### 27.6 回复状态与待回复判定

| 维度 | 适用渠道 | 粒度 | 含义 | 工作台卡片 |
| :--- | :--- | :--- | :--- | :--- |
| 已读/未读 | 仅邮件 | 单封邮件 | 是否查看过 | 邮件未读 |
| 消息回复状态（已回复/未回复） | 消息（邮件消息 + WhatsApp 消息） | 逐条消息（邮件按封、WhatsApp 按会话实现） | 是否回复过 | —（不驱动工作台「待回复线索」卡） |

- 两维度独立："已读"≠"已回复"。
- **消息回复状态**（BR-019b）：按单条消息维护，默认"未回复"；首次回复发送成功后**仅更新当前消息**为"已回复"，不影响同一线索下其他未回复消息。
- **工作台「待回复线索」卡口径（唯一权威，§6.4）：** 本人为负责人、权限范围内线索状态=**待跟进**的线索数；**不以**消息未回复状态计数。
- 待回复判定（BR-019c，消息侧）：线索关联消息存在≥1条回复状态为"未回复"且线索非终态即计入；与下次跟进时间无关；所有消息已回复或线索终态则清除。**仅用于消息侧统计/列表筛选等，不驱动工作台「待回复线索」卡。**
- 与跟进超时区别：工作台待回复基于线索状态=待跟进；跟进超时基于下次跟进时间<今日（与消息回复状态无关）。

### 27.7 统一取数口径（最近签约合同 / 成交金额）

适用：客户列表「最近签约合同」、客户详情合作合同统计、分析中心外销合同数/合同金额/已成交客户数/站点成交金额。

- 筛选范围：合同状态=**已签约**（**排除失效**）。
- 排序：按签约时间倒序。
- 取数：最近 1 份（「最近签约合同」）或全部累计（「合同金额 / 外销合同数」）；已成交客户数=周期内至少有 1 份已签约合同的客户去重。
- 边界：无任何合同或全部为失效→「—」；多份则取签约时间最近一份。

### 27.8 时间范围筛选统一交互规则

适用于所有列表页/管理页/日志页/分析页的时间范围筛选项（各页面具体时间字段见对应章节）。

1. 支持开始时间和结束时间两个字段。
2. 允许只选择开始时间（不限制结束）。
3. 允许只选择结束时间（不限制开始）。
4. 开始时间不能晚于结束时间（前端校验，违例提示"开始时间不能晚于结束时间"）。
5. 时间为空时表示不限制该时间条件（不参与过滤）。
6. 时间比较口径：开始时间含当天起始、结束时间含当天结束（按日期粒度，YYYY-MM-DD）。
7. 点击重置时恢复该页面默认时间范围（见各页面说明）；**重置不改变用户权限范围、不改变站点/角色权限**。
8. 时间筛选只影响当前页面查询，不影响全局站点、用户权限、角色权限。
9. 时间范围展示格式与项目已有日期组件风格保持一致；API 参数格式与项目现有时间参数格式保持一致（YYYY-MM-DD）。
10. **不同业务含义的时间字段不能混用**（创建时间、更新时间、入池时间、签约日期、跟进时间、下次跟进时间、最近跟进时间、邮件时间、最近消息时间、登录时间、操作时间、变更时间、统计时间各自独立）。

**默认值建议（按业务合理性设置，不强行统一）：**

| 页面类型 | 默认时间范围 |
| :--- | :--- |
| 工作台 / 分析中心 | 本月（客户经营为本年，见 §19.2） |
| 线索列表 | 创建时间=本月，其余时间字段=不限制 |
| 公海池 | 入池时间=本月 |
| 跟进日志 | 跟进时间=本月，下次跟进时间=不限制 |
| 邮件 / WhatsApp | 不限制 |
| 系统日志 | 近 7 天 |
| 客户 / 合同 | 创建时间=不限制；合同签约日期=本月 |
| 站点 / 用户 / 字典管理 | 不限制 |

---

## 28. 业务规则汇总（BR 索引）

| 编号 | 规则说明 | 引用位置 |
| :--- | :--- | :--- |
| BR-004a~d | 数据权限粒度判定（业务员/运营专员/协同人/系统管理员） | §4.2 及各模块数据权限 |
| BR-004e | 用户停用后无法登录，名下数据保持原状态，线索超时按公海回收释放，可手动重分配/转移，不可作为目标，重启恢复 | §23.2、§26.5 |
| BR-008 | 邮件生成线索（两类邮箱）：① 业务员个人邮箱→归业务员本人、直接进入本人线索列表；② 站点主邮箱（站点绑定运营人员、以其业务邮箱作主邮箱）→归该运营人员，由其点击【转派给业务员】一步完成投递与负责人处理、不进公海（T1–T3、BR-053；无线索可只投递）；站点=邮箱对应站点；人工兜底生成归属当前操作人 | §7.5、§7.10、§20.5、§27.1 |
| BR-009 | 站点不允许物理删除，仅停用 | §20.6、§26.4 |
| BR-010 | 消息永久保存，不得物理删除 | §7.4、§7.5 |
| BR-012 | 消息进入时创建/关联询盘联系人影子对象（尚未归集为客户联系人） | §7.10、§8.5、§27.3 |
| BR-014 | 关闭 AI 时邮件转线索由人工维护，站点=收件邮箱对应站点、归属当前操作人 | §7.10 |
| BR-015 | AI 不可用/失败时不自动识别询盘、不自动创建线索；消息保留转人工兜底创建、归属当前操作人；业务不中断（一期不设规则引擎） | §21.5、§21.8 |
| BR-016 | AI 输出：意向总结、意向产品、客户画像（工商/参展/风险）、询盘标签；意向产品写入线索、不同步客户；客户画像落询盘联系人并按 BR-016c/036 流转 | §7.9、§21.4、§25 |
| BR-016a | 询盘标签在有效询盘生成线索时追加写入线索标签；同名去重；不删除已有；不覆盖人工；人工删过的同名禁止再追加；MVP 无重新分析入口 | §7.9、§8.5、§13.5、§21.4、§25 |
| BR-016b | 客户标签仅人工维护（入口为新建/编辑客户，无独立打标签弹窗）；线索转客户不继承线索标签、不受 AI 影响 | §14.7.1、§14.7.2、§25.3 |
| BR-016c | 客户画像（工商/参展/风险）落询盘联系人资料：仅首次生成；已有则用存量不覆盖；人工编辑优先；消息接入时分析一次，打开不重跑 | §7.9、§21.4 |
| BR-017 | 手动修改字段后 AI 不再覆盖 | §13.3 |
| BR-019a | 有效询盘定义：AI 判定命中 10 类之一即有效（产品咨询/价格咨询/样品需求/采购需求/交期咨询/定制需求/合作代理/项目询盘/复购或新需求/明确联系方式交换）→ 自动生成线索；自动回复/广告/无业务内容/垃圾信息为无效（不生成）；关闭 AI 时人工判定；判定过程不落库、不新增判定字段 | §7.10、§17.4、§21.8 |
| BR-019b/c | 回复状态（逐消息）；BR-019c 为消息侧未回复判定（不驱动工作台「待回复线索」卡；该卡以 §6.4 状态=待跟进为准） | §7.10、§27.6、§6.4 |
| BR-020 | 有效询盘由 AI 自动创建线索（主流程），业务员个人邮箱归业务员本人、站点主邮箱归运营专员（可转派业务员）、WhatsApp 归属绑定业务员，默认"待跟进"、不进公海池；归属异常（对应运营专员/业务员停用/不存在）时线索仍创建、负责人空、状态"待分配"进公海，禁止因归属失败导致创建失败 | §7.10、§8.5、§21.8、§27.1 |
| BR-021 | 一条消息最多生成一个线索（**入库不做按发件邮箱自动合并**）；**例外**：站点主邮箱【转派给业务员】产生的投递件到达业务员邮箱时不建线索——已关联线索则挂回原线索，未关联线索则仍不建、不自动挂旧线索（BR-053、T1–T3） | §7.10、§8.5、§7.5.2 |
| BR-053 | 站点主邮箱【转派给业务员】：一步完成邮件投递（底层转发至目标业务员绑定邮箱，运营无需再手动转发；页面无「转发」标签）+ T1–T3（有线索则按规则处理负责人；无线索仅投递；T2 锁定同人不可换；T3 默认可改）；投递件不触发 AI 建线索 | §7.3、§7.5.2、§7.10、§27.1 |
| BR-022 | 一个线索可关联多个消息 | §13.9 |
| BR-023 | 同一邮件内多需求合并为一个线索 | §27.1 |
| BR-024 | 线索创建默认"待跟进"，不进公海池 | §7.10、§8.5 |
| BR-025 | 线索任意时刻仅一个负责人 | §13.8 |
| BR-028 | 运营专员（负责站点内）、系统管理员可重新分配/变更线索负责人；业务员与协同人不可（协同人严格只读） | §13.8 |
| BR-030 | 每次关键沟通或状态变化必须新增跟进记录 | §12.6、§13 |
| BR-031 | 跟进记录仅新增，不改历史、不删除 | §11.5、§12.6 |
| BR-033 | 转客户在客户名称可生成（或已人工补充）后为必然成功动作，成功后线索置"已转客户"（沉淀客户/客户联系人、保留并关联原线索）；企业名称与询盘联系人姓名均为空时拦截并要求补充客户名称；"已成交"由后续录入已签约合同并关联线索驱动（BR-056）；系统异常按异常处理 | §13.6、§26.1 |
| BR-034 | MVP 客户匹配：当前线索站点内按**站点 + 客户名称**精确匹配已有客户（客户名称=企业/个体工商户取企业名称，个人取询盘联系人姓名）；不做模糊匹配、不做联系人归集匹配；命中关联、未命中新建 | §13.6、§27.3、§30.6 |
| BR-035 | 询盘联系人归集为客户联系人：关键资料（邮箱/手机/姓名）一致则关联已有、冲突则新建；同客户内去重、跨客户可重复；一客户可多联系人；手动关联线索与转客户动作均触发 | §13.6、§14.7、§15.4、§27.3 |
| BR-036 | 转客户/关联客户：① 客户侧无客户画像记录则写入联系人侧客户画像；客户侧已有画像记录则整包不碰、绝不覆盖；② 从工商画像预填客户标准字段「国家/地区」「所属行业」（新建预填、关联仅补空；与字典对齐，匹配不上留空；可人工改）；③ 不同步意向总结/意向产品/询盘标签；年营收不写入客户标准字段 | §7.9、§13.6、§14.7、§15.4、§21.4 |
| BR-037 | 线索不物理删除，仅关闭、标记异常 | §13.7 |
| BR-038 | 无效消息经人工干预手动转线索，归属当前操作人及邮箱对应站点 | §7.10 |
| BR-038a | 待跟进线索首次录入跟进后→"跟进中" | §13.4、§26.1 |
| BR-038b | 转客户规则：跟进阶段扩展属性「是否允许转客户」仅决定该阶段是否**允许**人工手动触发转化，**不自动转化**；转化成功线索置"已转客户" | §13.4、§13.6、§23.5、§26.1 |
| BR-038c | 高意向口径：以跟进阶段扩展属性「是否计入高意向」为准，与「是否允许转客户」分立；不得按阶段名称硬编码 | §6.4、§17.4、§23.5 |
| BR-057 | 来源渠道与录入方式分立：来源渠道描述客户从哪来；录入方式描述数据如何进入；「手动」不是来源渠道；线索必落 `entry_method`（映射见 §23.5.7）；导入未填来源渠道留空 | §9.7、§23.5、§30.1 |
| BR-043 | 客户本身不可删除；联系人可逻辑删除，但**客户详情只读、不提供联系人增删改**（新增入口见列表【添加联系人】§14.7.2a） | §14.7.2、§14.7.2a、§15 |
| BR-050 | 合同不提供删除；作废或编辑改为失效后状态=失效，仍留在列表可筛选；可通过【编辑】改回已签约；任何状态均不物理删除 | §16.6、§26.2、§29.8 |
| BR-052 | 邮箱两类：业务员个人邮箱（归业务员本人）；站点主邮箱（站点绑定运营人员、以其业务邮箱作主邮箱，一站点一主邮箱、不可重复绑定，归该运营人员、可转派业务员，转派规则 T1–T3） | §7.5、§20.5、§24.3.1、§27.1 |
| BR-053 | WhatsApp 仅业务员可绑（1 个/人），运营/协同/管理员不绑；WhatsApp 线索站点=业务员唯一授权站点 | §8.5、§24.3.2 |
| BR-054 | 操作日志记录新增/编辑/删除/导出/导入/登录/配置 | §23.9 |
| BR-055 | 登录日志记录时间/IP/浏览器/方式/结果 | §5.4、§23.9 |
| BR-056 | 合同↔线索状态联动：① 录入/导入已签约合同且关联线索 → 该线索→已成交（可从待跟进/跟进中/已转客户直接进入）；② 作废→失效时，若关联线索当前=已成交且该线索下已无其他已签约合同 → 回退为已转客户，并在该线索写系统跟进记录；若仍有其他已签约合同则保持已成交；未关联线索则不动线索；只影响本条关联线索 | §16.6、§26.1、§26.2 |

---

## 29. 异常处理与兜底规范

> 本章为全系统**异常处理唯一权威定义**（空态、加载、错误、权限、并发、系统级状态页、AI 异常、删除、审计、合规）。业务模块只引用本章，并仅保留"该模块发生异常时的业务影响与特殊处理"。

### 29.1 通用空态

列表无数据显对应空态占位图（"暂无线索/客户/合同"等）；搜索无结果"未找到匹配的 XX，请调整筛选条件后重试"；指标卡/数值无数据"--"；图表无数据保留坐标系显"暂无数据"。

### 29.2 通用加载态

列表首次加载 5 行骨架屏；筛选变化 3 行骨架；全局路由切换顶部进度条（不阻挡）；表单骨架；异步任务提交按钮 Loading + Toast"任务已提交"。

### 29.3 通用错误态

加载失败/网络异常/服务端 5xx："数据加载失败，请刷新重试" + 【重新加载】（5xx 可显"系统繁忙，请稍后再试"）；表单提交失败 Toast 具体原因并保留内容；接口超时"请求超时，请稍后重试"。

### 29.4 权限校验失败

无菜单权限（缺少对应 `*:view`）→403 页面；无按钮权限（缺少对应按钮权限码）→按钮置灰或隐藏，接口拦截时 Toast「您没有该操作权限」；数据权限越界→列表自动收敛不展示越权数据。判定依据为登录下发的权限码集合（§4.6），**不得**仅根据角色名称判断。

### 29.5 并发与冲突

公海线索并发分配"该线索已被其他操作分配，请刷新后重试"；变更负责人新负责人已停用阻止并提示；合同编号重复拦截；同一站点内客户名称相同客户拦截；站点主邮箱重复绑定拦截（见 §20.5.3、§29.11）。

### 29.6 系统级状态页

系统级状态页唯一权威定义（各模块只引用，不再单列）：

| 状态页 | 展示与操作 |
| :--- | :--- |
| 404 | 页面不存在 + 返回首页 |
| 403 | 无权限 + 返回首页（隐藏"返回上一页"） |
| 500 | 系统繁忙 + 重新加载 + 联系管理员 |
| Loading | 骨架屏 / 进度条 |
| 维护中 | 维护提示 + 预计恢复时间，无操作按钮 |
| 网络异常 | 网络连接异常 + 重新连接 |

### 29.7 AI 服务异常（AI 异常唯一权威定义）

各业务模块 AI 能力异常统一引用本节，模块内不再单列 AI 异常文案，仅说明"AI 不可用时的业务影响"。

| 异常类型 | 触发条件 | 用户提示文案 | 前端展示 | 是否阻断业务 |
| :--- | :--- | :--- | :--- | :--- |
| 未配置 Token | AI 能力未配置 API Key | 服务尚未完成配置，请先完成配置 | AI 面板灰占位 | 否 |
| 服务开关未启用 | 系统参数（业务规则）「AI 功能启用」关闭 | AI 服务当前未启用，请联系管理员开启服务 | AI 面板灰占位 | 否 |
| Token 无效/过期 | API Key 校验失败 | AI 服务配置异常，请检查配置 | AI 面板灰占位 | 否 |
| 网络异常 | AI 请求连接失败 | AI 服务连接异常，请稍后重试 | AI 面板灰占位 | 否 |
| 服务商接口异常 | AI 服务商返回错误 | AI 服务响应异常，请稍后重试 | AI 面板灰占位 | 否 |
| 调用超时 | AI 响应超时 | AI 服务响应超时，请稍后重试 | AI 面板灰占位 | 否 |
| 批量提炼失败 | 批量 AI 提炼任务失败 | AI 提炼失败，请重试 | Toast | 否 |

- **统一降级：** AI 不可用/失败时不自动识别询盘、不自动创建线索，业务不中断（BR-015，降级机制见 §21.5）；消息保留、由人工兜底创建线索归属当前操作人；一期不设规则引擎自动识别。
- **统一原则：** AI 仅辅助，任何 AI 异常均不阻断消息查看、生成线索、跟进等核心业务；消息【生成线索】弹窗字段集与新增线索一致（§13.2）；AI 失败时业务字段全部不预填、由用户手填；站点/来源/负责人默认带入可改；仅 WA 会话电话只读（§13.2.1）。


### 29.8 数据删除规则（软删除唯一权威定义）

**核心业务对象统一采用软删除（逻辑删除）或不提供删除入口，任何情况下不物理删除。** 适用对象：线索、客户、客户联系人、合同。

| 对象 | 删除方式 | 删除后表现 | 关联规则 |
| :--- | :--- | :--- | :--- |
| 线索 | 软删除（关闭/标记异常，不提供删除入口） | 数据不可见，历史记录保留 | BR-037 |
| 客户 | 软删除（客户本身不可删，仅停用/注销标记） | 数据不可见，历史记录保留；关联处显"(已注销)" | BR-043 |
| 客户联系人 | 二次确认后逻辑删除 | 列表不再展示，历史关联显"-" | BR-035、§15.4.3 |
| 合同 | 不提供删除；作废/编辑→失效 | 仍留在列表可筛选；可通过编辑改回已签约 | BR-050、§16.6.2、§16.6.3 |

**统一规则：** ① 删除后数据不可见但记录保留可审计；② 不做物理清除（消息永久保存见 BR-010；MVP 邮件草稿箱/垃圾箱删除即软删、不做 30 天恢复与自动清理，**【二期/延后】** 见 §7.4）；③ 站点/用户为停用非删除（BR-009/BR-004e）；④ 个人数据删除请求走匿名化（见 §29.10 GDPR）。

### 29.9 操作日志与审计

登录、业务操作、配置变更、导出、附件下载均记录；敏感字段脱敏（见 23.9）；日志保留 1 年。

### 29.10 GDPR 合规

预留数据匿名化能力：用户提出删除个人数据请求时，系统管理员可匿名化（脱敏姓名/邮箱/电话），保留业务数据完整性。MVP 预留。

### 29.11 站点与渠道绑定异常

| 场景 | 处理 |
| :--- | :--- |
| 站点配置选定主邮箱：邮箱已被占用 | 该业务邮箱已作为其他站点主邮箱：阻止保存；提示"该邮箱已绑定，不可重复绑定" |
| 站点配置选定主邮箱：站点已有主邮箱 | 该站点已有主邮箱：阻止保存；提示"该站点已绑定主邮箱，请先解绑" |
| 站点未绑定运营人员/主邮箱 | 该站点无对应主邮箱：邮件不参与自动生成线索（不影响 WhatsApp 线索） |
| 非业务员绑定 WhatsApp | 运营/协同/管理员无 WhatsApp 绑定入口；如越权调用则拦截，提示"仅业务员可绑定 WhatsApp" |

---

## 30. 对象字段模型

> 本章为**核心业务对象字段的契约层数据字典**，供研发建模建表与测试对照使用；字段展示与交互以各业务模块为准，字段取值规则以对应唯一来源章节为准（状态见 §26，标签见 §25，字典见 §23.5，数据流转见 §27）。**所有核心对象统一软删除（见 §29.8）。**
>
> **类型约定：** `BIGINT`（主键/外键，雪花或自增）、`VARCHAR(n)`、`TEXT`、`DECIMAL(p,s)`、`DATE`、`DATETIME`、`TINYINT`（枚举/布尔）。枚举值统一**存 code**，展示值由对应章节/字典映射。
>
> **通用审计字段（各表均含，下表不再重复）：** `id` BIGINT 主键；`created_at`/`updated_at` DATETIME 非空；`created_by`/`updated_by` BIGINT；`is_deleted` TINYINT 默认 0（软删除标记，§29.8）；`deleted_at` DATETIME 可空。

### 30.1 线索 Lead

**无线索名称字段，展示统一使用系统生成的线索编号。**

| 字段 | 建议列名 | 类型 | 必填 | 长度/格式 | 校验/约束 | 说明·关联 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 线索编号 | `lead_no` | VARCHAR(32) | 是 | LEAD-YYYY-XXXX | 全局唯一、系统生成、只读 | 唯一标识与展示名 |
| 询盘联系人 | `inquiry_contact_id` | BIGINT | 是 | — | FK→询盘联系人 | 线索 1:1 询盘联系人（姓名/邮箱/电话/WhatsApp 存于该对象）|
| 联系人姓名 | `contact_name` | VARCHAR(64) | 是 | ≤64 | 非空 | AI 提取可改 |
| 联系人邮箱 | `contact_email` | VARCHAR(128) | 条件 | ≤128 | 邮箱格式；**邮箱/电话/WhatsApp 至少一个** | AI 提取可改 |
| 联系人电话 | `contact_phone` | VARCHAR(32) | 条件 | ≤32 | 数字/+/-/空格；**邮箱/电话/WhatsApp 至少一个** | AI 提取可改 |
| WhatsApp 账号 | `contact_whatsapp` | VARCHAR(32) | 条件 | ≤32 | **邮箱/电话/WhatsApp 至少一个** | 可人工维护；WA 渠道可来源会话号 |
| 企业信息（所属企业）| `company_name` | VARCHAR(128) | 否 | ≤128 | 可空 | 线索阶段所属企业/个体工商户名，AI/人工识别，可改；**不等同于客户名称**；空时转客户可用询盘联系人姓名生成客户名称 |
| 客户画像（工商等） | `company_profile` 等 | TEXT/JSON | 否 | — | 询盘联系人侧客户画像；首次生成后复用（BR-016c）| 含工商/参展/风险；线索引用展示，见 §7.9 |
| 来源渠道 | `source_channel` | VARCHAR/BIGINT | 否 | 字典 code/FK | 字典「来源渠道」；消息自动仅邮件/WhatsApp；导入未填留空；**无「手动」项** | §23.5 |
| 录入方式 | `entry_method` | VARCHAR(32) | 是 | 字典 code | `manual_create`/`batch_import`/`inquiry_conversion` 等；系统按创建路径写入，用户不可改 | 仅线索；映射见 §23.5.7 |
| 站点 | `site_id` | BIGINT | 是 | — | FK→站点 | 邮件线索=收件邮箱对应站点；WhatsApp 线索=业务员唯一授权站点；线索创建即有明确站点；数据权限依据，见 §4.2、§9.2 |
| 负责人 | `owner_user_id` | BIGINT | 否 | — | FK→用户；**待分配时为空** | 邮件=业务员个人邮箱→业务员本人／站点主邮箱→运营专员（可转派业务员）；WhatsApp=绑定业务员；归属规则见 §27.1 |
| 线索状态 | `lead_status` | TINYINT | 是 | 枚举 code | 待分配/待跟进/跟进中/已转客户/已成交/无效/丢失 | 默认待跟进（公海=待分配），定义见 §26.1 |
| 跟进阶段 | `follow_stage_id` | BIGINT | 否 | — | FK→字典（跟进阶段）| 字典管理、与状态互不映射，见 §23.5 |
| 意向产品 | `intent_product` | VARCHAR(255) | 否 | ≤255 | — | AI 提取可改，写入线索不同步客户（BR-036）|
| 关注点 | `focus_points` | JSON/VARCHAR | 否 | — | 字典项集合；跟进追加去重、编辑可增删 | 客户域→客户关注选项 |
| 备注 | `remark` | TEXT | 否 | — | — | 人工备注；详情展示 |
| 来源国家/地区 | `source_country` | VARCHAR(64) | 否 | ≤64 | 字典/下拉 | 线索侧获客地域；AI 提取可改；获客分析国家口径唯一来源（§18.3/§18.6），与客户 `country` 区分 |
| 客户ID | `customer_id` | BIGINT | 否 | — | FK→客户；**未转化为空** | 线索 N:1 客户 |

### 30.2 客户 Customer

| 字段 | 建议列名 | 类型 | 必填 | 长度/格式 | 校验/约束 | 说明·关联 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 客户ID | `id` / `customer_no` | BIGINT / VARCHAR(32) | 是 | CUS-YYYY-XXXX | 主键；编号全局唯一、只读 | 编号对外展示 |
| 客户名称 | `customer_name` | VARCHAR(128) | 是 | ≤128 | 非空；与 `site_id` 组成**唯一约束** | **客户主体展示名与唯一性字段**；企业/个体工商户取企业名称，个人取询盘联系人姓名（见 §2.4） |
| 站点 | `site_id` | BIGINT | 是 | — | FK→站点；创建后只读 | 唯一归属，权限依据，见 §4.2 |
| 客户潜质分级 | `potential_level` | VARCHAR/TINYINT | 否 | 字典 code | 未选默认 `potential`（潜在客户） | 人工维护、不自动变化，见 §26.3、§23.5 |
| 负责人 | `owner_user_id` | BIGINT | 是 | — | FK→用户 | 客户负责人 |
| 国家/地区 | `country` | VARCHAR(64) | 否 | ≤64 | 字典/下拉 | 转客户可从工商画像预填（BR-036）；可人工改 |
| 行业 | `industry` | VARCHAR(64) | 否 | ≤64 | 字典/下拉 | 即「所属行业」；转客户可从工商画像预填（BR-036）；可人工改 |
| 客户联系人 | （子表 contact）| — | — | — | 客户 1:N 客户联系人 | 见 §30.3 |

> **唯一约束：** `UNIQUE(site_id, customer_name)`（MVP 客户唯一性 = 站点 + 客户名称）；同一客户名称允许因站点不同存在多个客户档案。客户名称按统一规则生成（企业/个体工商户取企业名称，个人取询盘联系人姓名）。**客户不设生命周期状态**，经营情况由**客户潜质分级**（字典、人工维护）反映（见 §26.3）。

### 30.3 客户联系人 Contact

| 字段 | 建议列名 | 类型 | 必填 | 长度/格式 | 校验/约束 | 说明·关联 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 联系人ID | `id` | BIGINT | 是 | — | 主键 | — |
| 客户ID | `customer_id` | BIGINT | 是 | — | FK→客户，非空 | **联系人 N:1 客户；必属且仅属一个客户** |
| 姓名 | `name` | VARCHAR(64) | 是 | ≤64 | 非空 | — |
| 邮箱 | `email` | VARCHAR(128) | 否 | ≤128 | 邮箱格式；**同客户内去重** | 归集匹配关键资料（BR-035）|
| 手机 | `phone` | VARCHAR(32) | 否 | ≤32 | 数字/+/-/空格；**同客户内去重** | 归集匹配关键资料（BR-035）|
| WhatsApp | `whatsapp` | VARCHAR(32) | 否 | ≤32 | — | — |
| 职位 | `title` | VARCHAR(64) | 否 | ≤64 | — | — |
| 联系角色 | `contact_role` | VARCHAR/BIGINT | 否 | 字典 code/FK | 字典「联系角色」（§23.5） | 见 §14.7.2a、§15.4.3 |
| 主要联系人 | `is_primary` | TINYINT | 否 | 0/1 | 同客户内唯一为 1 | 设新则原自动取消 |

> **禁止独立联系人（必属于客户）、禁止一个联系人同时归属多个客户**；同一自然人在不同客户下为独立联系人记录（跨客户可重复，见 BR-035）；一个客户可拥有多个联系人；归集去重规则见 §27.3。

### 30.4 合同 Contract

| 字段 | 建议列名 | 类型 | 必填 | 长度/格式 | 校验/约束 | 说明·关联 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 合同ID | `id` / `contract_no` | BIGINT / VARCHAR(32) | 是 | — | 主键；编号全局唯一、只读 | 编号对外展示 |
| 客户ID | `customer_id` | BIGINT | 是 | — | FK→客户，非空 | 合同 N:1 客户；继承客户站点 |
| 线索ID | `lead_id` | BIGINT | 否 | — | FK→线索 | 合同 N:1 线索（可空）|
| 合同金额 | `amount` | DECIMAL(14,2) | 是 | >0 | 非负且 >0 | 成交口径见 §27.7 |
| 合同日期 | `sign_date` | DATE | 是 | YYYY-MM-DD | 非空 | 签约日期 |
| 合同状态 | `contract_status` | TINYINT | 是 | 枚举 code | 已签约 / 失效 | 录入即"已签约"，状态机见 §26.2 |

> 合同不提供删除；作废后状态=失效，仍留列表；不物理删除（BR-050、§29.8）。**无 `end_date`（合同结束时间）字段。**线索联动见 BR-056。

### 30.5 对象规则与异常流程（引用）

以下为已确认规则的统一固化，细则以各唯一来源为准，本节不重复定义：

- **客户创建方式：** ① 线索转客户（§13.6）；② 客户中心直接新建客户（§14.7.1）。
- **AI 分析失败（不阻断业务）：** AI 成功→自动识别有效询盘并自动创建线索；AI 不可用/失败→消息保留→进入人工处理→人工创建线索；AI 仅辅助不阻断（见 §21.5、§21.8、§29.7、BR-015）。
- **客户匹配异常：** 生成线索时匹配到已有客户仅提示、不阻止创建、不创建重复客户，转客户时关联已有客户（见 BR-034、§13.6）。
- **联系人匹配异常：** 按姓名 + 关键联系资料一致自动匹配归集；任一关键资料不一致则新建联系人（见 BR-035、§27.3）。

### 30.6 实现口径说明（研发对齐）

> 以下为落地口径澄清，**不改变业务规则**，用于消除研发交付歧义。

| 项 | 实现口径 |
| :--- | :--- |
| 客户唯一性 | **MVP 客户唯一性 =（站点 + 客户名称）**，唯一约束 `UNIQUE(site_id, customer_name)`：同一站点内客户名称相同视为同一客户、不重复创建；不同站点即使客户名称相同也允许各自建档。客户名称按统一规则生成（企业/个体工商户取企业名称，个人取询盘联系人姓名），自由文本、**精确匹配**（不做域名归一、不做模糊匹配）。MVP **不设独立企业（Company）对象 / `company_id` 字段**。 |
| 客户类型 | 客户主体可能为企业 / 个体工商户 / 个人；**MVP 仅作为业务口径描述，不设置客户类型字段，不提供筛选或统计**（见 §13.6、§2.4 客户名称口径）。 |
| 编号生成 | 线索 `LEAD-YYYY-XXXX`、客户 `CUS-YYYY-XXXX`、合同编号 人工录入；系统生成编号按**年度序列、库级唯一、并发生成加锁/序列器**，XXXX 为年度内递增序号（位宽不足自动扩展）。 |
| AI 有效询盘判定可测性 | AI 判定为模型输出，验收以 §21.8 的**有效/无效清单**为准则，测试采用**样例集回归**（典型有效/无效样本命中率）；AI 不可用时不自动识别、转人工兜底创建线索，判定过程不落库。 |
| "收件邮箱归属异常"触发场景 | 指站点主邮箱对应站点的**运营人员已停用/缺失**（含站点未绑定运营人员/主邮箱、原运营人员停用）；此时线索仍建、置"待分配"进公海，由运营专员/系统管理员分配（协同人只读不可）（见 §27.1、BR-020）。 |
| 邮件线索归属与转派 | 业务员个人邮箱线索归业务员本人（无需转派）；站点主邮箱线索归运营专员，运营点击【转派给业务员】一步完成：自动投递邮件至业务员绑定邮箱 + T1–T3（有线索：运营负责可改派；业务员 A 负责则锁定 A；无线索：仅投递，默认可改选人，不建线索、不自动挂旧线索）；页面无「转发」标签；投递件不触发 AI 建线索（BR-053，见 §7.5.2）。 |

### 30.7 站点运营数据 SiteOperationData

| 字段 | 建议列名 | 类型 | 必填 | 长度/格式 | 校验/约束 | 说明·关联 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 主键 | `id` | BIGINT | 是 | — | 主键 | — |
| 站点 | `site_id` | BIGINT | 是 | — | FK→站点；与周期组成唯一键 | 维度 |
| 统计周期类型 | `period_type` | TINYINT | 是 | 枚举 code | 1=月/2=周 | 维度 |
| 统计周期 | `period` | VARCHAR(16) | 是 | 月 YYYY-MM / 周 YYYY-Www | 与 site_id/period_type 组唯一 | `UNIQUE(site_id, period_type, period)` |
| 广告花费 | `ad_spend` | DECIMAL(14,2) | 否 | ≥0 | 允许空 | — |
| 网站浏览次数 | `website_visits` | BIGINT | 否 | ≥0 | 允许空 | — |
| 询盘量 | `inquiry_count` | BIGINT | 否 | ≥0 | 允许空 | — |
| 高潜询盘量 | `high_intent_inquiry_count` | BIGINT | 否 | ≥0 | 允许空 | — |
| 询盘转化率 | `inquiry_conversion_rate` | DECIMAL(7,4) | 否 | 0~100 | 允许空 | 百分比 |
| 单个询盘成本 | `cost_per_inquiry` | DECIMAL(14,2) | 否 | ≥0 | 允许空 | — |
| 高潜询盘成本 | `cost_per_high_intent_inquiry` | DECIMAL(14,2) | 否 | ≥0 | 允许空 | — |
| 平均访问时长 | `avg_visit_duration` | DECIMAL(10,2) | 否 | ≥0 | 允许空 | 秒 |
| 网站跳出率 | `bounce_rate` | DECIMAL(7,4) | 否 | 0~100 | 允许空 | 百分比 |
| GSC 曝光量 | `gsc_impressions` | BIGINT | 否 | ≥0 | 允许空 | — |
| GSC 点击次数 | `gsc_clicks` | BIGINT | 否 | ≥0 | 允许空 | — |
| GSC 出词数 | `gsc_keywords` | BIGINT | 否 | ≥0 | 允许空 | — |
| GSC 平均排名 | `gsc_avg_position` | DECIMAL(8,2) | 否 | ≥0 | 允许空 | — |

> 列表展示字段、导入字段、导入模板字段三者一一对应（见 §20A）。

---
